# 0.30.0 — 기본 제작 흐름 기준판

상태: **IN_PROGRESS**. 구현 브랜치 `release/0.30.0`을 생성했다. QA·릴리스는 미완료다. 시작 기준: `codex/daw-integration`의 build152 및 검증 기록 commit `4abf43c`. 계획 도입 commit `7a1ba7f`에서 구현 브랜치를 생성했다. 목표 앱 버전은 0.30.0, 현재 후보는 build157이다. 높은 콘솔의 페달 수치 가림·Tab 자동 노출을 수정하고 재검증했다.

## 결과와 범위

사용자가 새 곡에 섹션을 만들고 드럼/신스 MIDI와 오디오를 배치한 뒤, 효과와 automation을 적용하고 송폼을 바꾸며 실제 재생·바운스·저장/재열기까지 수행한다. 작업은 기존 단일 다크 캔버스에서 이어져야 한다.

포함: 기존 제작 기능의 연속 사용 중 발견되는 데이터·범위·입력·가시성·파일/재생 오류, 키보드 포커스, 명확한 대기/실패/취소, 현재 schema 호환, 검증된 실제 패키지.

후속 범위: 새 악기 엔진의 대규모 재설계, 녹음 기능 확장, 새로운 일반 CC/plugin automation, 앱 내 계정 대화, 아티스트 catalog, 발매 수준 데모 완성. 기존 녹음/영상 기능에 회귀를 만들지 않아야 하지만 그 전체 품질 인증은 0.40/0.90의 필수 조건이다.

## 설계 불변 조건

- 서클은 시간 궤도이며 화면 배치 변경이 노트 시간·연결 의미를 임의로 바꾸지 않는다. 새로운 고정 좌우·하단 편집 패널을 추가하지 않는다.
- 이번 use·섹션 원본·공유 패턴을 명시한다. GUI·키보드·MCP가 같은 편집/Undo 계약을 사용한다.
- 수치·이름 초안과 IME 입력을 늦은 포커스가 가져가지 않는다. 파일 읽기 대기는 완료/실패와 구분하고 취소 후 입력 가능한 상태를 회복한다.
- 기존 노트·원본 오디오·라이선스 자산을 보존한다. 저장 schema는 불필요하게 올리지 않는다.
- 물리 I/O를 차단한 QA와 실제 재생을 구분한다. 장치 검증을 실행할 수 없으면 출고 gate가 열린 상태로 남는다.

## 작업 분해와 담당 경로

각 작업의 완료 기록은 아래 표를 갱신한다. 관련 파일은 출발점이며 새 소유 범위가 필요하면 구현 전에 표에 추가한다.

| ID | 작업·진입 조건 | 주 소유 경로 | 완료 조건 | 상태 |
|---|---|---|---|---|
| R30-01 | 최소 재현 곡과 기준 상태 고정 | `qa/`, `Tests/`, 이 문서 | 3개 섹션·일반/공유 MIDI·직접 작성 오디오로 기대 송폼/노트/자산 정의 | DONE |
| R30-02 | 생성→배치→연결→송폼 왕복의 막힘 수정. 01 뒤 | `Sources/CirclrApp/AlbumCanvas.swift`, `CanvasCommands.swift`, `CanvasPorts.swift`, `CanvasCableEditing.swift`; 의미 변경 시 `Sources/CirclrCore/HierarchyEditing.swift` | Q01 통과, 데이터/다중 IN·OUT 의미 유지 | TODO |
| R30-03 | MIDI/오디오/automation 진입·키보드·compact 작업. 01 뒤 | `Sources/CirclrApp/EditorFocusNavigation.swift`, `MIDIWorkspaceToolbar.swift`, `SustainWorkspace.swift`, `StepEditor.swift`, `AudioWorkspace.swift`, `AutomationEditor.swift`, `CommittedNumberField.swift` | Q02·Q03 통과, 전환 직후 키 재현 원인/수정·IME 보호 | DOING |
| R30-04 | 파일 읽기·범위·취소·재열기. 01 뒤, 03과 파일 소유 분리 | `Sources/CirclrApp/MIDIImportView.swift`, `MediaImportWorkspace.swift`, `MediaLibraryView.swift`, `SavedWorkspace.swift`, `RootView.swift`(리드: 가져오기 상태/취소 안내); `Sources/CirclrCore/ProjectStore.swift` | Q04·Q05·Q08 통과, 취소/오류 이후 작업 가능 | DOING |
| R30-05 | 기본 실제 재생/정지·바운스 통합. 01 뒤 | `Sources/CirclrAudio/PlaybackTransport.swift`, `PlaybackOutputConnection.swift`, `AuditionTransport.swift`; `Sources/CirclrApp/PlaybackOutputStatus.swift` | Q06·Q07, 내장 신스/직접 작성 오디오를 실제 출력·정지·재사용 | DOING |
| R30-06 | GUI/MCP 동일 결과와 충돌·Undo | `Sources/CirclrCore/AgentProtocol.swift`, `Sources/CirclrApp/AgentWorkspace.swift`, `mcp/server.py` | Q05·Q09, 잘못된 revision/범위가 음악을 바꾸지 않음 | TODO |
| R30-07 | 후보 버전/build·패키지 준비 → 회귀·native 통합·독립 검토 | `Resources/Info.plist`, `Resources/Codex/`, `scripts/`, `Tests/`, `qa/`, 이 문서 | 같은 후보의 모든 필수 QA PASS, 검토 차단 결함 0 | TODO |
| R30-08 | 승인 문서·commit/tag/push·사용 패키지 안내 | `README.md`, `CHANGELOG.md`, `docs/releases/` | APPROVED→원격 RELEASED 확인 | TODO |

리드가 01 기준 계약과 통합을 맡는다. fixture 생성은 r30_fixture, 03 입력은 r30_ui, 04 파일 대기는 r30_files, 05 오디오 진단은 r30_audio가 담당한다. 03/04는 표의 서로 겹치지 않는 파일에 한정하고 추가 파일은 리드와 소유 범위를 먼저 조정한다. 실제로 독립적인 05/06 또는 읽기 전용 감사만 병렬화한다. 한도에 맞춰 에이전트를 채우지 않는다. 테스트 담당은 완료 계약을 검증하며 구현 코드를 몰래 수정하지 않는다.

실행 순서: 01 → 02/03/04의 사용자 동선 및 독립 가능한 05/06 → 07에서 후보 버전/build·패키지 고정 → 같은 후보로 전체 동선/QA·리뷰 → 08. 05의 장치 경계 때문에 대기하면 다른 작업만 진행하고 출고 완료는 미룬다.

## 필수 QA matrix

기준 곡은 제품 샘플로 배포하지 않는 QA fixture다. Intro/Verse/Chorus 3개 섹션, 드럼·신스·오디오 각 1개 이상, 공유 MIDI 한 쌍, 일반 use 변형 한 개, 효과 한 개와 gain/pan/cutoff 중 지원 automation을 포함한다. source IDs·기대 길이·asset hash를 R30-01에서 고정한다. 주 화면은 1440×900, compact는 1024×768과 높은 콘솔 조건을 목표로 하며 실제 가능한 창 크기를 증거에 기록한다.

| ID | 실행 시나리오 | PASS 조건 | 상태 |
|---|---|---|---|
| Q01 | 빈 곳 우클릭 생성·정렬·8방향 연결·섹션 순서/반복·색상 변경 | 대상과 IN/OUT 명확, 정렬/색상 변경은 음악 시간 불변, 순서/반복만 의도대로 변경 | PARTIAL |
| Q02 | 노트/스텝/벤드/페달↔오디오↔automation, 빠른 전환·Tab/Shift-Tab·Home/End·Escape | 최종 대상만 키를 받고 노트 우발 생성/키 유실 재현 없음, 선택·입력 위치가 보임 | PARTIAL |
| Q03 | 잘못된 수치·입력 도중 모드 전환·한글 이름 조합, compact 휠 | 오류·조합 입력 보존, 유효 변경 한 번만 적용, 화면 밖 필수 필드 없음, 줌/스크롤 구분 | PARTIAL |
| Q04 | MIDI/오디오 가져오기 정상·미지원·취소·접근 대기 | 대상/상태/오류 명확, 취소 후 재입력 가능, 원본/기존 MIDI 유지 | PARTIAL |
| Q05 | 이번 use·원본·공유 변경, Undo/Redo·다른 편곡 왕복 | 범위별 예상 대상만 변경, 복구 후 동일 음악·연결·자산 | PARTIAL |
| Q06 | 실제 장치에서 내장 신스/오디오 곡 시작·정지·끝까지 재생·다시 재생, 실패 복구 | 가청 결과·정지·note-off 정상, 소유 helper 정리, 실패 후 앱 재사용. 장치/rate/buffer/후보 hash 기록 | NOT_RUN |
| Q07 | 효과/automation 포함 바운스·tail·원본 복원 | PCM/길이/템포·실제 청취 정상, 원본 복원·Undo 후 데이터/자산 보존 | PARTIAL |
| Q08 | 편집 후 저장·앱 종료·재실행·프로젝트 열기 | 음악·파일 참조·선택/모드 보존, 이전 schema fixture 읽기와 새 저장 재열기 정상 | PARTIAL |
| Q09 | GUI와 MCP 같은 명령, stale revision·실패 batch·STOP | 예상 결과 동일, 실패 원자성·STOP 뒤 늦은 변경 없음 | PARTIAL |
| Q10 | 앱+5 helper 동일 소스 빌드, kit 버전/hash·서명, 실제 패키지 smoke와 이전 앱/프로젝트 복구 | 버전/build/패키지 식별 일치, 정상 시작·프로젝트 재열기, 이전 패키지 복구 실행/확인 | PARTIAL |

컴퓨터 입력 도구가 IME를 재현하지 못하면 실제 사용자 입력 확인을 별도로 요청하고 Q03을 미실행으로 남긴다. 하드웨어/권한 때문에 Q06을 실행하지 못해도 PASS로 바꾸지 않는다. 문제가 재현된 화면과 결과 화면을 우선 남기고 모든 클릭을 촬영하지 않는다.

## 검증 명령과 증거 기록

[공통 QA 명령](README.md)을 사용한다. 수정한 Audio test class의 이름/필터와 물리 I/O 여부를 실행 전에 이곳에 추가한다. 최종 후보 build에서 Core·MCP와 영향받는 Audio 회귀, 위 native matrix를 실행한다. 전체 테스트 반복 횟수를 완료 기준으로 삼지 않는다.

증거 행은 `Q ID | build/commit 또는 candidate SHA | 실행 환경 | 명령/조작 | 예상/실제 | PASS/FAIL/NOT_RUN | 증거 위치`로 기록한다. 로그는 민감정보를 제거하고 재현 가능한 요약은 commit한다. 큰 WAV·QA 앱·스크린샷 묶음은 로컬에 보관한다. 실패를 수정한 경우 최초 실패와 재검사 범위를 모두 남긴다.

실행 근거는 [0.30.0 후보 QA 기록](0.30.0-qa.md)에 모은다. 부분 검증은 전체 gate PASS가 아니다.

## 위험과 대응

- 기존 HAL 대기의 원인은 미확정이다. 장치·프로세스·timeout 기록으로 원인을 좁히고, 사용자 앱을 변경하지 않는 진단부터 시작한다. 원인 확인 전 엔진 전면 재작성으로 범위를 넓히지 않는다.
- 공통 포커스 변경은 IME·수치 draft 회귀 위험이 있다. 대상 identity와 async 요청 수명을 함께 검증한다.
- 공유 패턴과 use 변형은 데이터 누수 위험이 있다. 정상 대상과 비대상 모두 비교한다.
- package-app은 dist 앱을 바꾼다. release checkout과 사용자 앱 경로를 먼저 구분하고 이전 앱/프로젝트 백업을 보존한다.
- 필수 결함은 이 버전에서 해결한다. 새 기능은 다음 버전으로 보내며 일정 때문에 완료 기준을 낮추지 않는다.

## 검토·릴리스 기록

계획 독립 검토: release_process_audit PASS. 승인 전 QA와 승인 후 원격 tag 확인이 순환하던 지적을 수정했고, 후보 패키지 준비→QA/검토→문서/commit/tag/push 순서의 재검토를 통과했다. 링크·소유 경로·작업 8개·필수 QA 10개와 diff 검사를 통과했다.

제품 구현 독립 검토: build155 범위에서 release_process_audit PASS. 비동기 MIDI 읽기 동안 라이브러리 접근 권한이 해제되는 지적을 수정하고 재검토했다. build157의 높은 콘솔 결함 수정도 소스 재검토 PASS, native Tab·오류 표시·Escape 복구를 확인했다.

출고 결정: 미승인. 실제 후보 버전/build·artifact SHA·호환·백업/복구·필수 QA를 확인한 뒤 리드가 APPROVED로 기록한다.

출고 후 필수 확인: R30-08에서 원격 release branch와 annotated tag의 peeled SHA가 승인 commit과 일치하는지 확인한다. 이는 승인 전 Q10과 분리된 RELEASED gate다.

원격 릴리스: 없음. 계획을 문서 commit/push한 것은 0.30.0 완료나 tag 발행이 아니다. 릴리스 태그 예정: `v0.30.0`.
