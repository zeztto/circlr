# 0.50.1 · build169 QA

상태: RELEASED. 최종 native·패키지·원격 게시 검사 완료.

## 자동 검사·독립 검토

- 전체 Core **638/638 PASS**, `.build/r501-full-core-final.log`.
- 자동 정렬 targeted **11/11 PASS**. 동일 결과 반복·음악/시간/연결 보존·원자 실패·그룹/freeform·비선택 sibling 충돌 회피·fork/merge·sidechain 분리를 검사했다.
- 실제 f0r h3r64노드에서 가로/세로 각각 주 신호 연결49개가 같은 lane을 유지한다. 방사형 bounds3130×3417,1440×900 framing 기준 최소 지름19.67px.
- 기본 방사형은 역할별 반경·100world-unit 여백·sidechain 유무별 위치 불변과 연결 보존을 검사했다. 사용자 지정 offset은 의도적으로 겹칠 수 있으며 자동으로 덮어쓰지 않는다.
- MCP feature **35/35**, server **28/28 PASS**. 서버 테스트의 오래된24개 기대값을 실제26개·고유 이름·필수 도구 검사로 수정했다. 제품 API 변경은 없다.
- 독립 코드 검토 PASS. 최종 build169 변경까지 확인, blocking finding 없음.

## 후보에서 수정한 문제

- build167: 우클릭한 다른 섹션 정렬 후 카메라만 이동하고 이전 곡 context에 남는 문제. 선택과 context를 함께 전환하도록 수정했다.
- build168: 가로/세로 rank마다 독립 ID 순으로 줄을 잡아 연결이 과도하게 교차하는 문제. 주 신호 chain lane 상속·fork/merge 처리와 sidechain 분리로 수정했다.
- build167/168 화면은 최종169 성공 증거로 재사용하지 않는다.

## 검증 경계

별도 QA 앱 `com.circlr.integrationqa`와 데모 사본을 사용한다. `qa/prepare-outward-orbit-qa.py --version 0.50.1 --build 169`로 준비하고 `qa/verify-outward-orbit-native.py NAME --build 169`로 정확한 앱·프로젝트 identity를 검사한다. 로컬 증거는 `qa/generated/outward-orbit/build169/`에 보존한다.

물리 장치 녹음·한글 IME·장시간 녹화 FPS·사용자 청취는 이번 배치 수정에서 재검증하지 않는다. 이전 버전 유예를 이번 PASS로 바꾸지 않는다. 자동 정렬은 시각 배치에만 적용하며 재생/음악 데이터 변경을 허용하지 않는다.

## 최종 build169 native

- macOS26.5.1, Apple Silicon. 세로 창700×1000(canvas700×851), 가로 창2560×1355(canvas2560×1256)에서 확인했다. 계획의1440폭 대신 실제 가로 디스플레이 전체 창을 사용했다.
- `horizontal`, `vertical`, `radial` snapshot:64개 서클 모두 비겹침. 최소 화면 본체 간격은 각각14.63 /13.78 /17.15pt. 역할별 반경 차이를 실제 화면에서 확인했다.
- 우클릭한 Intro64개 가로 정렬의 선택과 orbitContext 일치. 클릭부터 접근성 응답까지 약3.16초로 완료했다. 이 값은 CUA 통신을 포함하며 순수 연산 벤치마크가 아니다.
- 상단 compact 도구에서 세로·방사형 명령 실행, wide 그리드 메뉴에서 곡7개 섹션 방사형 실행 PASS. 메뉴에 적용 범위·개수가 표시된다.
- 각각 한 번 Undo 후 album/arrangements/tracks/assets/patterns/global이 baseline과 일치했다. 연결·음악 시간을 변경하지 않았다.
- 새 실제 JPEG: `portrait-horizontal.jpg`, `portrait-radial.jpg`, `landscape-song-radial.jpg`. [공개 촬영 기록](../images/README.md).
- 화면 제어 중 정상 종료 요청 후 재실행·복구 화면을 관측했다. AppKit 로그에 applicationShouldTerminate/NSTerminateNow·termination approved가 있고 새 앱 crash report는 없었다. 종료 요청의 주체는 확정하지 않는다. 복구 데이터는 baseline 음악 데이터와 일치했고 검증 fixture에 다시 저장했다. 이 관측을 앱 crash로 판정하지 않았다.
- 최종 `saved`: 정확한 QA fixture·build169, dirty=false, playing=false. 이후 검증 앱 종료.
- 독립 최종 native 증거 검토 PASS, blocking finding 없음.

## 패키지

- Release 앱과5helpers build PASS (`.build/r501-build169.log`). arm64·version0.50.1/build169, ad-hoc strict/deep 서명 PASS.
- `circlr-0.50.1-build169-macos-arm64.zip` SHA256: `58e95d5ab6336d32f8847b8cf5e08f822035d82b1ff7d4acd8cb0eb6d8cd817f`.
- 새 임시 디렉터리에 ZIP을 풀어 모든 앱 파일 hash가 원본과 동일함을 확인했다. Agent kit25파일 hash와version일치, 데모4곡 전체 리소스가 소스와 동일하다. `archive-verification.json` 보존.

## 원격 게시

- [v0.50.1](https://github.com/zeztto/circlr/releases/tag/v0.50.1): latest, draft=false, prerelease=false.
- release tag와 게시 시 main SHA는 `058312579836d1601c861f33ab5fcf692dcdd659`로 일치했다. 이후 main 문서 commit은 게시 완료 상태만 갱신한다.
- ZIP·SHA256SUMS 자산을 새 디렉터리로 다시 내려받아 `shasum -a 256 -c SHA256SUMS` OK 및 위 패키지 hash 일치를 확인했다. `download-verification.json` 보존.
