# 0.60.0 QA

상태: TECHNICAL_QA_PASSED / ARTIST_REVIEW_PENDING · build170 · 2026-09-12.

## 자동 검사

- MCP35+28 PASS.
- v6 변환4개 PASS. 기존 ID·컬러·배치·MIDI 시작/피치/velocity·드럼·미디어 보존 및 중복 변환 거부.
- 동봉 demo120 BPM/96마디/15트랙/2562notes/10voices 검사 PASS.
- CC0 percussion6개 allowlist, external paths0, unlisted files0. 클래식3곡 유지.
- Drive:9kHz 입력의21kHz foldback20dB 이상 감소, fundamental85% 이상 보존. nil/1/2는 구버전, 새3만 oversampling. FIR overshoot는 floating mix에서 보존하며 임의 hardclip하지 않는다.

## 데모 렌더

`music/f0r-h3r/v6/audio-analysis.json`:197.002초,48kHz stereo24bit, sample peak−1.8487dBFS, 추정4×peak−1.7014dBFS,−17.5321LUFS,clipped samples0,stereo correlation0.9595. WAV SHA256 `09f811ca84f2e9da640bfa9c599b6af73a9f0e0c29aa0485f005faf6f093f683`.

후렴15개 stem은 section effect와 track gain 이후/album bus 이전 출력이다. 인증 meter가 아니며 수치만으로 음악적 완성도·물리 장치 청취를 통과 처리하지 않는다. 최종 사용자 청취 답변은 대기 중이다.

## 독립 검토

초기 검토의 구버전Drive version2 충돌과 factory→기본 음색 선택 오류를 수정했다. 최종 독립 코드 검토 PASS, blocking findings0.

## 실제 앱 검증

build170 QA bundle `com.circlr.integrationqa`. 시작 화면의4곡 표시와 f0r h3r v6 로드, 다른 project ID의 편집 사본 생성 확인. ‘벨 전자피아노’ 검색·선택 후 cutoff12500, 한 번 Cmd-Z 후 원래 patch cutoff7200 및 전체 tracks 동일성 확인. 앱에서 새 위치에 저장한 프로젝트의 재열기와 project ID·tracks 보존 PASS.

후렴 재생 snapshot에서 playing=true,20.2347초 위치(표시20.16초), 현재 섹션과 신호 edge 값 갱신. Stop 뒤 playing=false. 물리 스피커 청취를 모델이 수행한 것으로 기록하지 않는다. QA 앱은 저장 후 종료했다.

가로2560×1355 및 세로 비율1281×1355 창에서 상단·프리셋 검색·필터 곡선 확인.700폭·실제 한글 IME 조합·외부 AU 실청취·마이크 녹음·장시간 녹화는 이번 변경 범위에서 재검증하지 않았다. 한국어 검색은 paste로 검증했다.

외부 프로세스가 Documents 아래 만든 QA fixture의 open은 Foundation 파일 open에서 대기했다. 취소 후 앱이 직접 저장한 `demo-saved.circlr`는 재열기 성공했다. macOS 파일 접근 경계는 해결 완료로 주장하지 않는다. 증거: `qa/generated/outward-orbit/build170/`의 demo-loaded/before-preset/after-preset/undo-preset/reopened/playback-start/playback-stopped JSON,3개 실제 스크린샷.

최종 Swift1,007개 중1,004 PASS/장치 환경3 SKIP/0 failures,183.3초. MCP63 PASS. 세 skip은 AU effect worker, AU instrument worker, 패키지 기반 render/playback 통합이며 별도 장치 runner가 필요한 검사다.

전체 검사 첫 실행에서 검색 결과 확장에 따른 fixture 기대값과 기존 XCTest helper 탐색 실패를 발견했다. 검색은 bank2개/전체4개의 정확한 ID로 수정했다. 장치 통합 test는 XCTest가 실제로 찾는 Xcode usr/bin의 helper 부재를 명시적 skip으로 처리하며, helper 존재 시 render/playback 오류는 여전히 실패한다. 이 skip은 실제 앱 검증을 대체하지 않는다.

## 패키지

새 ZIP을 임시 디렉터리에 풀고65개 파일의 경로·SHA256이 dist 앱과 동일함을 확인했다. 버전0.60.0/build170, 실행파일 arm64, strict deep ad-hoc 서명, 동봉 데모4곡 확인. ZIP SHA256 `fd0146c1fa15462a0a780106cb1d56dd9d497f5ef9a0f42ee7ade1b4edbdd6cf`. 정식 Release 게시 후 별도 디렉터리에 공개 파일을 다시 다운로드해 SHA256SUMS 검증 PASS. latest=v0.60.0,draft=false,prerelease=false,원격 peeled tag SHA=32beb50b69699dd4044843584901f821febf20eb.

프리셋12개의 실제 무이펙트/무정규화 PCM 렌더 PASS. 테스트 화음에서 최대 sample peak0.511. 이 값이 모든 연주·겹침에서 clipping이 없음을 보증하지 않는다.

이번 배포는 음색 도구·데모 표현 개선의 기술 릴리스다. 아티스트의 최종 음악 승인과 발매 수준 완성 목표는 OPEN이며 이 릴리스로 완료 처리하지 않는다. 사용자에게 전체 WAV 청취를 요청했고 답변은 아직 없다.
