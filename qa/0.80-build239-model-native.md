# 0.80 build239 · 외부 Codex 모델→MCP→실제 앱 제작 QA

```text
TEMPLATE_VERSION: qa-response-v1
STATUS: PASS — 지정된 외부 Codex→MCP 1회 MIDI edit, 앱 콘솔 표시, 독립 Undo·저장·재열기·WAV 동선
BUILD: 0.80.0/build239, com.circlr.build239modelqa
NOT_VERIFIED: 앱 안 계정 대화, trusted AI STOP, 물리 청취·마이크, 발매 품질, 전체 0.80 출고 gate
EVIDENCE: qa/generated/r80-model-build239/ (Git-ignored)
```

2026-09-25 KST. 최종 패키지 `dist/써클러.app`의 본체 SHA-256은 `f87136cef6f5453bcfd7aa034cb75bf4d49873efea267a6aad8d0b166ace9314`였다. strict deep 서명 통과 후 별도 앱 `써클러 build239 모델 검증.app`을 복제하고 document/URL type 등록을 제거, bundle ID `com.circlr.build239modelqa`로 ad-hoc 재서명했다. QA 본체 SHA-256은 `511a5379d80fb27192ad7e46bc913d61d818b2854b7eef48a9d1770c7227a29a`다. 원본의 본체·Codex/Demos manifest·라이선스 리소스가 복제와 종료 전후 불변이고, 원본/QA 앱의 strict 서명도 재확인했다. 준비 기록은 [preparation.json](generated/r80-model-build239/preparation.json)에 있다.

앱 전용 `~/Library/Application Support/circlr-build239model-qa/fixtures/model-bach.circlr`는 패키지의 Bach Invention 8 demo를 새로 복사하고 project ID만 `4DCC1070-1FD3-4A65-A52C-A8985D21E6B3`로 바꾼 사본이다. 번들 원본 manifest SHA-256은 `a0350b4802b192bf3d7ba207a9a031f166c0c5092e60faf0f82f766da3257c34`, 모델 전 QA 사본은 `8b472f3b328e5568bfb6c463e4ec16f147819ef059761661f7cb8ede83a88d88`였다. QA 앱을 이 사본으로 직접 열어 저장된 revision **0**, `dirty=false`를 확인했다. 사용자 곡·설치 앱·production Agent socket은 열지 않았다.

`qa/run-0.80-model-e2e.py prepare`는 QA `selected.json`의 동일 UID/0600, socket inode, bundle ID, build **239**, runID `59370A83-D35D-4B1A-B896-9E922DE6E40A`와 실제 `circlr_snapshot.runtime`을 대조했다. QA 앱에 **동봉된** circlr-studio kit를 전용 client에 설치했고 kit manifest SHA-256은 `6fbe03d3bd33605560798715ab5c5076864a1277e50ae7b0a332caa1b8159d60`이다. **MCP 자식의 `HOME`에만** private `/tmp/cq80.*`의 QA Agent symlink를 설정했다. `--socket`/`CIRCLR_SOCKET` 직결과 사용자 Codex config·로그인 변경은 하지 않았다. `codex-cli 0.149.1`은 `Logged in using ChatGPT`였고 자격증명 자체는 읽거나 기록하지 않았다.

`codex exec` 자체는 현재 사용자 환경·인증을 상속했고 `--approve-for-me`로 실행됐다. 사전 조회와 모델 turn은 별도 MCP 프로세스였으므로 runID·fixture 경로가 매 write에 강제 고정되는 프로세스 격리를 증명하지 않는다. 이 실행의 전후 runtime·경로·revision을 관찰해 동일 QA 앱과 사본에 반영됐음을 확인했다. trace에서 관찰된 shell 호출은 아래의 두 bundled reference 읽기 한 건으로, 프로세스 전체의 파일 접근 제한을 뜻하지 않는다.

모델 turn은 **한 번만** `codex exec --json --ephemeral --ignore-user-config --approve-for-me`로 실행했다. trace의 실제 MCP 순서는 `circlr_context → circlr_inspect → circlr_apply`이고 세 호출 모두 완료/성공이다. 모델의 유일하게 관찰된 MCP write는 첫 use의 lane `24C21F96-35B5-5CB5-81F9-84C50AF35F2A`에 `set_step(stepIndex=0, pitch=65, velocity=80, gate=1)` 한 건이었다. 기존 76개 note를 그대로 보존하고 beat **0**, length **0.25**, pitch **65**, velocity **80** note 하나를 추가해 revision **0→1**, target lane **76→77**이 됐다. 동일 use의 다른 MIDI lane은 불변이었다. 모델이 실행한 shell 명령은 설치된 skill reference 두 파일을 `sed`로 읽는 한 건뿐이었다. 최종 응답의 자기보고로 합격시키지 않았고 JSONL 요청/성공 결과와 앱 data를 별도로 대조했다. trace SHA-256 `a97e8b57949cc999de08b4cdcfb88ed74e5f03d6c5baf99b0878cea9b2a37c69`; 실제 모델 사용량은 input **151,569**(cached **92,416**), output **1,123** tokens다. 모델 ID는 trace에 명시되지 않아 CLI 기본 모델로만 기록한다.

**모델 직후, 독립 QA 후속 작업 전** 열린 앱 콘솔의 AX와 실제 화면에서 `12:20:35 에이전트 실행 · apply`, `에이전트 편집 · 1개 완료`, 상태 `r1`을 확인했다. 앱 `circlr_events`에도 `에이전트/실행 · apply`(revision 0)와 `앱/에이전트 편집 · 1개 완료`(revision 1)가 있었다. 관련 AX 발췌는 [console-model-only.ax.txt](generated/r80-model-build239/console-model-only.ax.txt), 활동 원본은 ignored `e2e/model-activity.json`에 있다. CUA 화면 자체는 시각적으로 확인했으나 PNG 파일로 저장하지 않았으므로 AX 발췌와 activity JSON을 재검토용 파일 증거로 남긴다.

이후 **독립 QA client가 수행한 작업**은 모델 작업과 분리한다. 추가 note와 기존 lane 보존을 검사한 뒤 Undo로 두 lane 배열의 정확한 원복(**r2**), 동일 step의 QA 재적용(**r3**), 같은 QA 문서 저장, native `open/job` 완료 후 같은 note/revision/manifest 지속, master `export/job` 완료를 확인했다. 저장된 manifest SHA-256은 `83361ad8c895ce481cd33fa7e6f80e0b4a304b8d4a7cf5cf04728500a7ecffb4`다. WAV는 **3,360,000 frames/70초, 48 kHz, stereo, 24-bit, nonzero PCM**이며 SHA-256 `eeb2b4f9c369455eea40176155a447f1a033d0fbe371e8e61b5eadb31928f47c`다. 기계 검사 원본은 ignored [report.json](generated/r80-model-build239/e2e/report.json)에 있다. 그 파일의 `consolePixelOrAX=PENDING`은 하네스 자체가 UI를 조작하지 않는다는 뜻이며, 위의 별도 AX/화면 확인으로 이 QA의 콘솔 gate를 닫았다.

검사 후 QA 앱은 Cmd+Q로 종료했고 해당 앱 PID/helper는 **0**, 고유 `agent.sock`은 사라졌다. `selected.json`의 비활성 manifest는 남지만 연결할 socket은 없다. 임시 MCP 자식 HOME symlink도 정확한 QA 경로를 검증한 뒤 제거했다. 시스템 오디오 장치 목록의 전후 `system_profiler SPAudioDataType` SHA-256은 둘 다 `d55bbd94be25fa82a48b006bbb9d06b4730113120b7fe9b60d3aa7892e21650d`로 같았고, 기본 입력·출력·시스템 출력은 모두 `Scarlett 6i6 USB` 그대로다. Play/Record나 시스템 장치 선택을 호출하지 않았다. 이는 실제 스피커 청취나 Scarlett의 현 macOS 호환성 판정이 아니다.

사후 독립 보안 검토에서 QA 하네스의 완료된 MCP 호출만 세던 검사를 강화했다. 현재 검증기는 모든 MCP/shell 호출의 시작·완료 순서, 읽기 완료 후 write, 예상 대상·인자, 예상 bundled reference를 읽는 정확한 shell 명령 한 건, 추가 write/file change 부재를 확인한다. 기존 JSONL SHA-256 `a97e8b…`를 **모델 재호출 없이** standalone으로 재검사해 `circlr_context → circlr_inspect → circlr_apply` PASS였고, 적대적 회귀 **11/11**이 통과했다. 새 하네스는 같은 QA fixture의 재호출 marker와 설치된 kit 전체 파일 해시도 검사하지만, 기존 실행의 `plan.json`에는 그 새 해시 필드가 없어 **그 gate를 소급 통과했다고 주장하지 않는다**. 실제 모델 세션의 write 직전 QA 계획 runID/path 강제 고정과 Codex 프로세스 파일 접근 제한도 이 결과로 증명되지 않는다.
