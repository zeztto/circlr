# 0.80.0 build217 — Open 패널·저장/재열기 native QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build217-native
STATUS: passed (지정된 Open·저장·재열기 범위)
COVERAGE: 무선택 Open의 앱 오류, manifest.json 직접 선택, .circlr 폴더 선택, QA 사본 저장·재열기, 문서/미디어 보존, 1440×900/720×900 실제 창
FAILURES: 이번 범위에서 없음
BLOCKERS: 없음
RISK_CALLS: 파일명 행 클릭만으로 selected가 되지 않는 macOS 패널 동작의 이유는 이 검사 범위 밖. 재생·청취·사용자 프로젝트·출시 전체 gate는 다루지 않음
VERIFIED_ENVIRONMENTS: macOS 26.5.1, circlr 0.80.0/build217, 격리 com.circlr.build217openqa
ARTIFACTS: qa/generated/r80-build217-native/ (Git 제외)
DELIVERABLE: Open UI AX/경고, 앱 콘솔, 전후 파일 해시, README 후보 원본 창 PNG 2개; 다음 owner development lead
```

2026-09-24 KST. `dist/써클러.app` build217 실행 파일 SHA-256은 `24caeee9029a5929559af6b6acc236bbdc6c153979188194bdfbb3cacae20930`이다. 앱을 `qa/generated/r80-build217-native/circlr-build217-open-qa.app`으로 복사하고 bundle ID를 `com.circlr.build217openqa`로 변경해 ad-hoc 재서명했다. 동봉 `f0r-h3r.circlr`을 같은 QA 폴더로 복사하고 QA 사본의 프로젝트 UUID만 바꿨다. 원본 데모와 사용자 프로젝트는 쓰지 않았다.

## 실제 Open·저장 흐름

| native 조작 | 관찰 |
| --- | --- |
| Open 패널에서 `r80-build217-native` 상위 폴더로 이동, **선택된 행 없이 Open** | 앱 alert: **`선택한 폴더에 manifest.json이 없습니다. .circlr 곡 폴더를 선택하세요`**. build216의 generic macOS `The file “manifest.json” couldn’t be opened because there is no such file.` 경고 대신 구체적인 안내를 표시했다. `unselected-before.ax.txt`와 `unselected-alert.ax.txt`/`.png`에 남겼다. |
| `⌘⇧G`로 QA 사본 내부의 **`manifest.json` 파일**을 명시적으로 선택 | AX는 `manifest.json` 행 `(selected)`와 정확한 file URL을 보였다. Open 후 `f0r h3r 열기 완료`가 표시됐다(`manifest-selected.ax.txt`). |
| 열린 QA 곡에서 실제 `⌘S` | `저장 완료 · f0r-h3r-build217-open-qa.circlr`. QA 프로젝트 루트의 manifest가 갱신됐고 `manifest.json/manifest.json` 같은 중첩 파일은 없었다(`after-save.json`). |
| `⌘O → ⌘⇧G`로 저장된 **`.circlr` 폴더 자체**를 명시적으로 선택 | AX는 폴더 행 `(selected)`와 `.circlr/` URL을 보였다. Open 후 다시 `f0r h3r 열기 완료`(`folder-selected.ax.txt`, `after-reopen.ax.txt`). |

앱 콘솔에는 **08:48:15 구체적 오류 → 08:48:42 첫 열기 완료 → 08:49:05 저장 완료 → 08:49:33 재열기 완료**가 순서대로 남았다(`after-reopen.ax.txt`). 저장 후 재열기까지 QA 사본은 manifest 1개와 미디어 6개, 총 7개 파일이었다. 저장 전후 미디어 **6/6개 SHA-256 동일**, 저장 후와 재열기 후 **7/7개 SHA-256 동일**이다. 저장으로 manifest 해시는 `dba8f92d5462c08a34c6f2be79c6f0b65b617029225a57f14da8705b90b51d86`에서 `4097dfe20d8b4af6c06ccff83308808a9cf116cb5f8aff832a5d1eaee463be93`로 바뀌었다. 원본 데모 **7/7개 SHA-256**과 배포/QA 실행 파일은 검사 전후 동일하다(`baseline.json`, `final-check.json`).

저장 manifest의 이름 `f0r h3r`, schema 6, 트랙 15개, 섹션 7개, 편곡안 1개, asset 6개는 원본과 수량이 같다. asset 경로 6개는 모두 상대 경로이고 파일이 존재하며, manifest에 기록된 checksum과 실제 SHA-256이 일치한다. QA 프로젝트 밖 미디어 참조, 여분 프로젝트 파일, 남은 save staging/backup 폴더가 없다(`document-safety.json`). 실제 앱 재열기 성공과 이 디스크 검사를 합쳐 이번 저장·재열기 범위의 문서/미디어 보존을 확인했다.

## README 후보 전체 창

native 앱의 곡 궤도에서 일곱 섹션 이름·순번이 보이도록 축소하고 실제 창을 **1440×900** 및 **720×900**으로 캡처했다. CUA 원본 screenshot bytes는 JPEG이므로 `.source.jpg`로 그대로 보존했고, README에서 사용할 수 있도록 `sips`로 **자르기·크기 변경 없이 PNG 변환**했다. `sips`로 두 PNG 픽셀 크기를 확인했다.

| PNG | 픽셀 | SHA-256 |
| --- | --- | --- |
| `build217-song-orbit-landscape-1440x900.png` | 1440×900 | `1be011ffcdb6e89ab038c27407bf0a63b91929d570500229f8672db9362d1521` |
| `build217-song-orbit-portrait-720x900.png` | 720×900 | `c375ffc54245c5451e06579587202ad97d31c816af89a1d369f8d92228cd2e46` |

양쪽 앱은 종료 후 `codesign --verify --deep --strict`를 통과했다. 앱과 관련 helper PID는 0개다. 읽기 전용 CoreAudio 조회의 시스템 기본 출력은 전후 모두 같은 Scarlett 6i6 장치였으며 장치 설정·재생을 실행하지 않았다. 이 QA 단계에서는 소스·README·CHANGELOG 변경, 커밋·푸시는 하지 않았다.
