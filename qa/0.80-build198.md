# 0.80 build198 · 세로형 케이블 도구 native QA

상태: **좁은 창의 도구 표시·선택·편집 확인 / 종료 복구 결함 발견** · 2026-09-24.

`./scripts/build-app.sh`로 0.80.0/build198 arm64 앱과 5개 helper를 패키징했다. `codesign --verify --deep --strict`가 통과했고 빌드 로그 `qa/generated/r80-build198-build.log`의 SHA-256은 `1c20c81fead5c1f52fc33fadde855274a75535a2b4ad01ef815737181dc0ce91`이다. [격리 QA 준비 도구](prepare-port-focus-qa.py)로 `com.circlr.portfocusqa` 사본을 만들었다.

실제 앱의 약 702×900pt 창에서 `f0r h3r` 데모와 **0.80.0 · 빌드 198**을 확인했다. 케이블을 선택하자 제목 아래 첫 줄에 **재연결·위치 이동·편집·해제**, 둘째 줄에 **OUT·IN**이 표시됐다. 접근성 트리에도 각 버튼이 나타났고 IN을 선택하면 해당 끝점의 강조가 옮겨졌다. **편집**을 누르면 `Club Chorus` IN의 연결 편집 화면이 열리고 기존 연결 2개·대상 검색 6개가 표시됐다. 이 결과는 약 700pt 창의 두 줄 배치와 해당 케이블의 기본 조작을 뒷받침한다.

별도 종료 재현에서 QA 폴더의 `recovery.json`이 01:06:12에 저장된 것을 확인한 뒤 앱의 **변경 버리기**를 선택했다. 프로세스는 종료됐지만 QA 복구 파일이 같은 시각·크기로 남았다. 다음 실행에서 버린 곡을 다시 복구하라고 제안할 수 있는 결함이다. 기본 사용자 폴더 `circlr/recovery.json`은 QA 전후 모두 00:37:28이었고 수정하지 않았다. 이 종료 결함은 후속 빌드에서 수정·재검증한다.

RootView의 최소 폭은 700pt이므로 340/375pt 창 자체를 이 QA로 검증했다고 주장하지 않는다. 다른 케이블 길이·라벨·방향, 전체 키보드 경로, 실제 Scarlett 입력과 물리 청취도 열려 있다.
