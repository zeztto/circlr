# 0.80 QA232 · 실제 Codex 모델의 격리 MCP 제작 동선

```text
TEMPLATE_VERSION: qa-response-v1
STATUS: PASS — 외부 Codex CLI 모델→MCP→실제 앱의 지정 음악 편집·Undo·저장/재열기·WAV에 한함
BUILD: 0.80.0/build230, com.circlr.qa232qa
NOT_VERIFIED: 앱 내 계정 채팅, trusted AI STOP, 모델 ID, 물리 청취·녹음·Scarlett, 발매 품질
EVIDENCE: qa/generated/r80-qa232-model-native/ (Git-ignored)
```

2026-09-24 KST. 서명된 `dist/써클러.app` build230을 고유 QA bundle ID로 복사·ad-hoc 재서명했다. 원본 본체 SHA-256은 `68541bd055c9a52494ee9b7c059e79996b59c4a4a8a9a3f73c6b591d15227b2c`이며 원본/QA 앱 모두 strict deep 서명 검사에 합격했다. Resources 57/57개 byte-identical이다. QA 앱은 `~/Library/Application Support/circlr-qa232-qa/fixtures/bach-qa232-model.circlr` 사본만 열었다. 복사 전 원본/QA manifest SHA-256은 `a0350b4802b192bf3d7ba207a9a031f166c0c5092e60faf0f82f766da3257c34`였다. 사용자 곡·production bundle·시스템 오디오 설정을 건드리지 않았다.

CLI는 `codex-cli 0.149.1`이고 `codex login status`는 `Logged in using ChatGPT`였다. 인증정보 자체는 읽거나 기록하지 않았다. 동봉 `circlr-studio` kit를 QA 전용 client 폴더에 설치하고, MCP 자식 프로세스의 `HOME`만 짧은 private `/tmp/circlr-qa232-home`으로 지정했다. 그 홈의 Agent symlink는 고유 QA Support의 Agent를 가리킨다. QA 앱 `selected.json`은 권한 0600, bundle ID `com.circlr.qa232qa`, runID `B436101E-330D-4686-80D6-CAE8FE927FD7`였고 실제 `circlr_snapshot.runtime`의 bundle/build/runID와 일치했다. 이 경로는 `EndpointResolver`를 사용하며, 별도 [경로 조사](0.80-qa232-ai-route.md)의 실제 UNIX socket 시험에서 버전 2 요청의 `expectedRunID` 전송과 socket 교체 거절을 확인했다. 앱 안의 계정 대화가 아니라 **외부 Codex CLI가 MCP client인 경로**다.

첫 `codex exec --json --ephemeral --ignore-user-config -s read-only` 시도는 모델이 곡을 읽고 4음 구상을 했으나 `circlr_apply`가 `MCP tool call requires approval, but approval policy is never`로 거절됐다. 그 직후 QA 곡 revision **0**, dirty=false, 원본 manifest SHA 불변을 확인했다. 승인되지 않은 편집을 성공으로 계산하지 않았다. 두 번째는 QA fixture 폴더만 추가하고 `--approve-for-me`를 사용했다. 승인된 MCP 호출 trace의 순서는 `snapshot → inspect → apply → inspect → undo → inspect → apply → save → open → job → snapshot → inspect → export → job`이고 모두 완료했다. 모델이 실행한 shell 명령은 동봉 skill/reference를 읽는 것에 한정됐다. 실제 모델 ID는 JSONL에 노출되지 않아 확인하지 못했으며 CLI 기본 모델로 기록한다.

| 독립 검사 | 결과 |
| --- | --- |
| 대상 | projectID `6E6D17A0-CE9A-5680-9A74-61392F2A0C82`, 첫 use의 `유리 건반 MIDI` lane `98684265-29EC-58D3-A9D1-2F0C0675CCB2` |
| 음악 편집 | 기존 64개 note를 유지하고 beat **0.5, 1.0, 1.5, 2.0**에 pitch **53, 57, 53, 60** 네 개 추가. 첫 instrument lane 76개는 불변 |
| Undo·재적용 | revision **0→1→2→3**. Undo 직후 두 lane의 note 배열은 초기 inspect와 정확히 같았다. 최종 inspect는 첫 적용과 음악 값이 같고 생성 note ID만 새로 부여됐다 |
| 저장·재열기 | 동일 QA 문서의 open job 완료 후 revision **3**, dirty=false, 새 note 4개 지속. 최종 manifest SHA-256 `436aa3f53937eea3263daec3f63df34da10a0a7a73a66864a7758eadf34beb57` |
| 비대상 데이터 | manifest의 다른 use, tracks, sections, signal, assets, takes, patterns, global·album 값은 원본과 같았다. `circleColors` 집합도 같고 저장 시 배열 순서와 `hierarchyView`만 달라졌다 |
| WAV | export job completed. 70.0초·3,360,000 frames·48 kHz·stereo·24-bit·nonzero PCM, SHA-256 `28909d5462638df2b2eba56df91c099f58a244405b6d65e859c12a78d0af7e60` |

`codex.jsonl`과 `codex-write.jsonl` 및 두 prompt/config의 SHA, 초기 snapshot, 앱 사본은 ignored evidence에 보존했다. 두 모델 시도의 JSONL usage는 각각 input **232,974**(cached 181,504), **926,302**(cached 850,944) tokens이다. 상세한 snapshot/inspect를 매번 모델 문맥에 돌려주는 현재 MCP 응답은 토큰 비용이 크므로, 후속 R100 AI 작업에서 범위 선택형/요약형 읽기 도구를 우선한다. 이 QA의 음악적 평가는 옥타브 응답구의 노트·화음 관계 수준이며 실제 스피커 청취·믹스 품질을 뜻하지 않는다. 구형 Scarlett을 열거나 Play/Record를 누르지 않았다. 사용자 계정이 앱 안에서 로그인·대화하는 기능, turn별 STOP/권한 철회, 정확한 모델 ID와 0.80 전체 출고 gate는 여전히 열려 있다.

검사 후 QA 앱에 정상 종료 이벤트를 보내 해당 bundle 프로세스와 고유 Agent socket이 사라진 것을 확인했다. MCP 자식 전용 임시 `HOME` symlink도 정확한 대상 경로를 확인한 뒤 제거했다. QA 곡·WAV·trace는 격리 Support와 ignored evidence에 남겨 재검토할 수 있다.
