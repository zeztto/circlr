# 0.80 build201 · 복구·데모 사본·Agent 재연결 QA

상태: **복구·데모 전환 확인, 정상 종료 socket 결함 발견 / 최종 후보 아님** · 2026-09-24.

최종 Core 소스의 새 scratch 전체 Swift 테스트는 **1,112개 실행·11개 skip·실패0**이다. 로그 `qa/generated/r80-build201-secure-tests.log` SHA-256은 `6ac3ea7a9d6d66265811ef36861b0cd64f6ada40686fd32333932ff86192cc68`이다. Python MCP server **28/28**, 통합 **39/39**, agent kit **9/9**, 취소 fixture **8/8**을 확인했다. App target의 독립 socket smoke는 active listener·bind-only·stale 경로를 자동 삭제하지 않고 다른 socket으로 교체된 뒤 늦은 deinit이 새 경로를 지우지 않음을 확인했다. 로그 SHA-256은 `88e0dbd653b762d11e51c3b537f4547bf66cd8b11f5b2dc465cc35aa25a0ea58`이다.

0.80.0/build201 앱·다섯 helper를 패키징하고 `codesign --verify --deep --strict`를 통과했다. `qa/generated/r80-build201-release-build.log` SHA-256은 `7dfc3f2d1b16e6ad452f792ca82eafa77c7e2dc8ceccc5b3ae5f6d756f7e0c3f`다. [QA 패키지 기록](generated/port-focus/build201/package.json)의 원본 앱 tree SHA-256은 `1ae61b6d1ce2fff760455454a86fd73f1ce16890a4fcff9ccd893d75d57cfa07`이다. 앱을 실행한 뒤 UI에서 **0.80.0 · 빌드 201**을 확인했다.

QA 전용 `Agent/agent.sock`을 소유한 별도 test listener가 실행 중인 상태에서 앱을 열었다. listener를 종료하고 이 테스트가 만든 경로만 접속 거절·inode를 확인해 해제하자, 앱이 재시도해 같은 QA socket에서 `snapshot` 요청에 `ok=true`, `runtime.build=201`로 응답했다. 구버전 앱의 정상 deinit을 모사한 범위이며, 실제 구버전 프로세스 두 개를 함께 실행한 증거는 아니다.

실제 앱에서 `f0r h3r` 데모 → ⌘N → **만들기** → **변경 버리기**를 두 번 반복했다. 각 데모 로드 시 전용 `Demo Projects`의 사본 수가 12→13, 전환 완료 뒤 13→12로 돌아왔고 방금 만든 두 사본이 모두 없어졌다. QA의 `recovery-v2.json`도 남지 않았다. legacy `recovery.json` SHA-256 `0c7507e9686665d048b82c5b5780d86e36dfda94fc1b91759c0f35702d8380e7`과 사용자 기본 `circlr/recovery.json` SHA-256 `688558b00e4160106065114e7a6305ffa1a24ea8f57636408f408e38e5148c38`은 그대로였다. 이는 **idle·pristine 데모 사본**에 관한 결과이며 재생·파형 reader·agent open 중 전환까지 입증하지 않는다.

마지막 새 곡에서 ⌘Q → **변경 버리기** 후 앱 프로세스와 v2 복구본은 없어졌지만 `Agent/agent.sock` 경로가 남았다. 다음 실행에서 새 버전은 이 경로를 안전상 자동 삭제하지 않으므로 MCP가 막힐 수 있다. 종료 시 명시적 socket 해제를 다음 후보에서 수정한다. 구버전 앱과의 교차 기동, crash-stale 복구 UX, 실제 청취·마이크·장시간 영상/전체 제작 동선은 열려 있다. `v0.80.0` 태그·Release는 만들지 않는다.
