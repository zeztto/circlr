# 0.80.0 build227 — 궤도 라벨 native QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build227-native
STATUS: FAIL — 큰 콘솔의 세로 화면에서 06 섹션 라벨 누락
COVERAGE: 서명 패키지 격리 실행, 번들 f0r h3r 사본, 정확한 720×900 기본 콘솔, 723×900 기본/180pt/40pt/접힘, 감상 모드 왕복, 좌하단 자유 영역 링 hit
FAILURES: 723×900·180pt 콘솔에서 06 ‘너의 도시 · Final Chorus’의 링은 보이나 이름표가 없음. snapshot labels에도 06 누락
BLOCKERS: 라벨이 모든 콘솔 높이에서 7/7 유지되도록 수정한 새 패키지의 native 재검증 필요
VERIFIED_ENVIRONMENTS: macOS native, circlr 0.80.0/build227, com.circlr.build227nativeqa, 고유 QA Application Support
ARTIFACTS: qa/generated/r80-build227-native/ (Git-ignored)
```

2026-09-24 KST. 서명된 `dist/써클러.app` build **227**을 QA 폴더에 복사하고 고유 bundle ID `com.circlr.build227nativeqa`로 ad-hoc 재서명해 실행했다. 앱 복구·에이전트 폴더는 `~/Library/Application Support/circlr-build227native-qa`, 데모 편집 사본은 `~/Library/Application Support/com.circlr.build227nativeqa/Demo Projects`에 한정됐다. 사용자 프로젝트·시스템 기본 오디오·물리 Scarlett 장치는 건드리지 않았고 재생·녹음·영상 녹화도 시작하지 않았다.

**기본 세로 화면 개선:** 정확히 **720×900pt**(Retina 1440×1800px)와 122pt 콘솔에서 01–07 이름이 모두 읽히며 06 `Final Chorus`도 우하단 navigation 위에서 완전히 보였다. `circlr_snapshot.playback`의 `canvasSize=[720,751]`, `workspaceViewport=[24,78,672,383]`(y 최대 461), 06 `rect=[504,425,192,36]`(아래 변 461)이다. navigation 상단은 화면에서 대략 canvas y475여서 약 14pt 간격이 있다. build226의 06/navigation 겹침은 이 상태에서 재현되지 않았다. 자기 시간 링·마디 눈금을 불투명 라벨로 덮는 사례도 기본 화면에서는 보이지 않았다.

**새 실패:** **723×900pt·180pt 콘솔**의 전체 창 `console-large-723x900.png`에서 06 원은 보이지만 이름표가 없다. AX에는 06 섹션과 시간 범위가 여전히 존재하므로 AX만으로 픽셀 가독성을 합격 처리하면 놓치는 결함이다. 같은 상태의 `snapshot-console-large-723x900.json`에서 `canvasSize=[723,751]`, `workspaceViewport=[24,78,675,325]`(y78–403), 라벨 7개는 **곡 1개+섹션 6개**이며 06이 빠져 있다. 06 원의 중심은 `[507.312,50.953]`, 반지름 `44.286`으로 viewport 상단 y78 바깥에 있다. 이는 viewport 축소 후 배치 후보가 없어지는 것과 일치한다. 작은 **40pt 콘솔**과 **접힌 콘솔**에서는 7/7 이름·링이 보이고, 감상 모드 왕복에서 텍스트 숨김/복원과 AX의 섹션 7개가 확인됐다.

**좌측 자유 영역 검사:** 이 변경은 navigation이 오른쪽에만 있어도 작업 viewport의 아래 경계를 화면 전체에서 y461로 자른다. 캔버스를 수직으로 팬해 05 원 중심을 `[115.786,470.722]`(반지름 `44.286`)로 옮겼다. 좌하단 자유 영역의 링 둘레를 클릭하면 AX에서 05가 선택되어, 이 한 사례의 **ring hit loss는 관찰되지 않았다**. 다만 05 라벨은 `[513,425,183,36]`까지 화면 오른쪽으로 이동했다. 원과 이름표가 약 397pt 떨어지고 긴 leader가 음악 연결선 위를 가로지른다. `pan-left-free-strip-720x900.png`와 같은 이름의 snapshot이 이 사용성 문제를 보여준다. 마우스 hover, 모든 링/시간 핸들, 임의 편집·Undo까지 포괄한 검증은 아니다.

CUA로 얻은 전체 창 JPEG를 자르거나 크기를 바꾸지 않고 PNG로 변환했다. 같은 이름의 `.ax.txt`, 진단 `snapshot-*.json`, 해시·치수 `inventory.json`을 ignored 증거 폴더에 보존했다. 앱이 **세로 1080pt 디스플레이**에 열린 관계로 1440×900 가로 native 화면은 이번 세션에서 만들지 못했다. 최초 1080×900 화면의 7/7 섹션 AX와 픽셀은 확인했지만, 이를 1440×900 결과로 부르지 않는다.

| 대표 PNG | 픽셀 | SHA-256 |
| --- | --- | --- |
| `demo-default-first-720x900.png` | 1440×1800 | `bc3c5460e5a734b9bcb75fe2bf474561479c4d7ac827ab0f2ea164731c36c3fd` |
| `console-large-723x900.png` | 1446×1800 | `7341158115ee75a801ca15e67371aa490ad91a06eb5dcf4ff41013a08e1fd377` |
| `console-small-723x900.png` | 1446×1800 | `6c466541fca63e30d76b1f1a8a87c89e93e95987d952fbb37ae52123de35716a` |
| `console-collapsed-723x900.png` | 1446×1800 | `d352dda2bd2037f5ebb81c09c49901d87b0d43d57b4a66055dc24ac54b11dbb0` |
| `pan-left-free-strip-720x900.png` | 1440×1800 | `0fb646bd8b4da38593b77e098a9a928579e5155cb5c81e5f2c55451177f58529` |

원본 패키지 실행 파일 SHA-256은 검증 전후 `8f4be4817b1ef192f5333c7dd16966465b41ad2c3f6bee6a46d668a785f1fb5a`다. QA 앱 재서명으로 복사본 실행 파일 해시는 달라졌다. 원본과 복사본 Resources **57/57개** 및 데모 리소스 **27/27개**는 SHA-256 일치한다. 양쪽 `codesign --verify --deep --strict` 통과. QA 데모 사본을 저장하지 않고 앱을 종료했으며 QA 앱/helper PID와 고유 agent socket은 남지 않았다.

따라서 **build227은 UI 출고 불가**다. 이 결과는 재생·물리 청취·Scarlett 호환성의 검증이 아니다.
