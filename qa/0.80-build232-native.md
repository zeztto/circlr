# 0.80.0 build232 — 팔로우 대상·케이블 native QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build232-native
STATUS: PASS (R80-02 SECTION/SONG 팔로우 회귀와 이 빌드의 대표 화면·첫 프레임); broader release gates remain OPEN
COVERAGE: 서명된 build232 격리 QA 앱, f0r h3r Groove 고밀도 섹션 재생, 내부 MIDI 케이블 선택·팔로우 재개, 기본 SONG 비교, 10.425초 MP4 첫 프레임, 1440×900/720×900pt 전체 창
FAILURES: 지정한 native 팔로우·선택선·첫 프레임 경로에서 재현된 실패 없음
UNVERIFIED: 물리 청취·실제 출력 음질, Scarlett 재생/호환성, 다른 곡·모든 줌 상태, 장시간 영상·A/V 지각 동기
VERIFIED_ENVIRONMENTS: macOS native, circlr 0.80.0/build232, com.circlr.build232nativeqa, 전용 Application Support
ARTIFACTS: qa/generated/r80-build232-native/ (Git-ignored)
```

2026-09-24 KST. 최종 `dist/써클러.app` 주 실행 파일 SHA-256 **`3cd92ae515f5726701365c621d09e5b5169ef8b20b28c2001bdf68e752729ee5`**를 확인했다. Git-ignored 폴더의 `써클러 build232 네이티브 검증.app`으로 복사해 bundle ID를 `com.circlr.build232nativeqa`로 바꾸고 문서 유형 등록을 제거한 뒤 ad-hoc 재서명했다. QA 실행 파일 SHA-256은 **`b9ba995ed73c1752483914d4582669c192f4f737d3a898b8b005724275811c9c`**다. 원본과 QA 사본 모두 `codesign --verify --deep --strict`를 통과했다. Resources **57/57개**, helper 실행 파일 **5/5개**가 byte-identical이었다. 번들 `f0r-h3r.circlr` 7파일의 상대 경로·내용 해시 tree digest도 양쪽 **`569edbe47193907c0452cbb273cef8671a425967ea708e322b9678e11630fcde`**로 같고, `manifest.json` SHA-256은 양쪽 **`a75ea9f2f3917f82184ba94c31e071905c0c6a1bfa2e90259ad554eaa5c794d7`**였다. 복구·agent는 `~/Library/Application Support/circlr-build232native-qa`, 데모 편집 사본은 `~/Library/Application Support/com.circlr.build232nativeqa/Demo Projects`에 격리했다. 원본 앱과 사용자 프로젝트를 열거나 수정하지 않았다.

앱의 출력 설정은 시작 시 시스템 기본값을 **Scarlett 6i6 USB**로 표시했다. 재생 전 **QA 앱 안에서만** `Mac Studio 스피커`를 선택했고 `circlr.playback.outputDeviceUID=BuiltInSpeakerDevice`를 확인했다. 재생 시계와 `마지막 재생 확인 장치: Mac Studio 스피커` UI를 확인했다. 종료 후 읽기 전용 장치 catalog에서도 시스템 기본값은 여전히 **Scarlett 6i6 USB**였다(`defaultUID` SHA-256 `d4bc96d8fc82c68ec973b2af8dca79a5f5ceb89e121c740e29c661043c3e5eed`). 시스템 기본 출력이나 Scarlett 장치 설정을 바꾸지 않았다. 실제 소리를 들었다는 판정은 하지 않는다.

`f0r h3r`에서 팔로우 메뉴의 기본 `✓ 현재 곡 서클`을 확인하고 **`현재 섹션 서클`을 명시적으로 선택**했다. Groove 섹션 내부로 이동해 캔버스를 6단계 축소한 뒤 섹션 단독 재생 중 `클럽 킥 MIDI · OUT MIDI 연주 → 클럽 킥 · IN MIDI 연주` 케이블을 선택했다. 재생 시계 **00:07.8**에서 AX가 선택 케이블과 `섹션 팔로우 재개`/`일시 중지`를 표시했다. [일시 중지 전체 창](generated/r80-build232-native/section-follow-suspended.source.jpg)은 약 **00:14.6**에서 민트색 연결선·끝점 링·케이블 편집 패널을 보인다. 재개를 누르자 AX 팔로우 값이 `켜짐`이 되었고 시계는 **00:19.3 → 00:26.3**으로 진행했다. [재개 뒤 전체 창](generated/r80-build232-native/section-follow-resumed.source.jpg)에서는 **Groove 섹션 카메라와 선택된 민트색 내부 케이블이 동시에 보였다**. 같은 시점 AX에서도 그 케이블은 `(selected)`였다. 따라서 지정한 R80-02 경로에서 선택 강조 유지와 SECTION 팔로우 복귀를 실제 픽셀·AX로 PASS 판정한다.

비교를 위해 대상 메뉴를 다시 `현재 곡 서클`로 돌려 같은 Groove 섹션을 재생했다. 같은 내부 케이블 선택 시 시계 **00:03.8**에 `곡 팔로우 재개`/`일시 중지`가 표시됐다([일시 중지 AX](generated/r80-build232-native/song-follow-suspended.ax.txt)). 재개 뒤 **00:13.1 → 00:20.3**에 팔로우 값 `켜짐`과 7개 섹션의 곡 카메라가 나타났고, 선택한 내부 케이블은 화면 밖으로 나갔다([재개 전체 창](generated/r80-build232-native/song-follow-resumed.source.jpg)). 이는 설정된 SONG 대상의 예상 동작과 일치한다. 이 비교는 SECTION 경로의 픽셀 가시성과 구분해 기록한다.

README 교체용 대표 이미지는 QA 앱을 한 번 종료한 뒤 **새 세션에서 번들 데모를 다시 연 정지 화면**이다. CUA `getScreenshot()`의 전체 창 JPEG 원본을 저장하고 `sips`로 크롭·합성·크기 변경 없이 PNG로 변환했다. 가로는 `LEN P27h-10`에서 **1440×900pt / 2880×1800px**, 세로는 `2460G4`에서 **720×900pt / 1440×1800px**다. 두 화면의 AX와 픽셀에 01–07 섹션, 00:00.0, `데모곡 불러오기 완료`가 있다. 디스플레이 설정은 변경하지 않았다.

| 전체 창 | PNG SHA-256 | JPEG 원본 SHA-256 |
| --- | --- | --- |
| [`landscape-1440x900.png`](generated/r80-build232-native/landscape-1440x900.png) | `8607b588fab51d3297c9da56c0b9ab22d3880fb29ca44c652f4070418b56c9c9` | `5a218c224f0cca3a2c22b0088cab2e25705961b2fda647214a69b12c3c0a2eab` |
| [`portrait-720x900.png`](generated/r80-build232-native/portrait-720x900.png) | `1ba1b5d5f424f8f54c6b175d11ac7ec5be368257a6b33e506b4dbaea4a7d9f5b` | `8331f7838229f091a9d3015003bf3570733dd1d146b1b15a8be5a7cf55c23680` |

선택된 곡 케이블 상태에서 짧은 실제 앱 [MP4](generated/r80-build232-native/selected-cable-opening.mp4)를 녹화했다. 앱은 **1034×1080 H.264/AAC · 313프레임 · 내부 누락 0**을 보고했다. 독립 AVFoundation 전체 디코드는 **10.4253125초·312프레임·P95 간격 38.33ms·최대 61.67ms·2-frame 초과 gap 0**이었다. 앱 보고와 디코드 프레임 수의 1프레임 차이는 측정 기준 차이로 기록하고 장시간 영상 PASS로 확대하지 않는다. 녹화 직전 [AX](generated/r80-build232-native/movie-preselection.ax.txt)는 케이블 선택을 보여준다. PTS **0.0**에서 독립 추출한 [첫 프레임](generated/r80-build232-native/selected-cable-opening-first-frame.png)을 픽셀로 확인했고, 선택 케이블 편집 패널·강조선·큰 끝점 링은 보이지 않았다. MP4 SHA-256 **`d08ad813da1f242eb546adc1e101e637c82e944a4e8123c0078328b8994996d8`**, 첫 프레임 PNG SHA-256은 **`de3911b4bb8dc5b221fc5339f8fd649776079c5051186e6d788113705b1c01ae`**이다.

마지막 QA 앱은 `⌘Q → 변경 버리기`로 종료했다. QA 경로의 앱·helper PID는 **0**, 전용 `Agent/agent.sock`은 **없음**을 확인했다. 종료 후 원본 실행 파일 SHA와 원본·QA 앱 deep strict 서명은 그대로였다. 원자료 해시와 전후 환경 상태는 [final-check.json](generated/r80-build232-native/final-check.json), 재현 가능한 파일 검사는 [SHA256SUMS.txt](generated/r80-build232-native/SHA256SUMS.txt)에 둔다. 물리 청취·Scarlett 호환성·장시간 영상은 이 native QA의 검증 범위 밖이다.
