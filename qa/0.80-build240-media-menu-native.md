# 0.80.0 build240 — 미디어 필터 native QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build240-media-filter
STATUS: failed (미디어 필터 PASS, 캔버스 ⌥Space modifier 회귀 P2)
COVERAGE: 약 700×600pt 창의 미디어 overlay·footer·옵션 scroll; 폴더/형식 Button 키보드·마우스; 라이브러리 ⌥Space preview와 overlay 밖 ⌥Space
FAILURES: overlay 밖 캔버스에서 ⌥Space가 전체 곡 재생을 시작함 — P2, build241 수정·재검 예정
BLOCKERS: 없음
RISK_CALLS: 종료 후 QA agent.sock 잔존은 시작 전 존재 여부·inode 미기록으로 원인 미확정; 후속 감사에서 build240 QA PID 30091 잔존이 발견되어 당시 '앱 0' 판정 철회; 물리 청취/VoiceOver/대형 목록 성능 미검증
VERIFIED_ENVIRONMENTS: macOS, dist/써클러.app 0.80.0/build240의 별도 ad-hoc signed com.circlr.build240mediaqa
ARTIFACTS: qa/generated/r80-build240-media-menu/ fixture·bundle-integrity·audio-before/after·final-integrity; CUA 앱 창 AX/screenshot은 세션에서 직접 관찰
DELIVERABLE: build240 필터 상호작용 PASS, 캔버스 단축키 P2 재현, build241 대상 좁은 재검 항목 확정
```

원본 `dist/써클러.app`은 `0.80.0/build240`, 실행 파일 SHA-256 `9cf614361e63d511f6caa5c8daf34d6f82b38ecba4921eae854b30eef698e1a9`였다. 고유 QA 앱 `.build/build240-media-menu/써클러 build240 미디어 메뉴 검증.app`을 복제하고 문서·URL 형식 등록을 제거한 뒤 ad-hoc 재서명했다. 원본과 복제본 모두 `codesign --verify --deep --strict` 통과, Resources 57개·helper 7개 byte 일치, 원본 실행 파일·동봉 데모 manifest SHA는 시작·종료 시 같았다. [복제 기록](generated/r80-build240-media-menu/bundle-integrity.json), [최종 기록](generated/r80-build240-media-menu/final-integrity.json).

라이브러리에는 이 QA 전용 [fixture](generated/r80-build240-media-menu/fixture-manifest.json)의 1초 48 kHz mono WAV와 1노트 Standard MIDI File을 각각 `audio-pack`, `midi-pack` 폴더로 추가했다. 두 파일은 ignored QA 폴더에만 있고 곡에 가져오거나 사용자 샘플 폴더를 등록하지 않았다.

| 경로 | 실제 조작·관찰 | 판정 |
| --- | --- | --- |
| 좁은 창 구성 | CUA가 **QA 앱 창만** 찍은 screenshot에서 700×600pt 목표 크기의 두 줄 상단바, overlay 상단 검색·필터, 결과 scroll, 하단 선택·미리 듣기·대상·가져오기 footer가 동시에 보였다. 형식/폴더 옵션은 각각 세 행과 독립 scroll bar로 표시되며 footer 위에서 끝났다. 겹침이나 잘린 `닫기 · Esc`는 보이지 않았다. | **PASS**, 이 창과 두 파일 조건. 정확한 screenshot 파일은 보존하지 않았으며 물리 화면 배율·더 긴 목록은 별도 검증. |
| Tab 초점 | 검색에서 Tab 1–6회를 누르면 `폴더 관리`→`폴더 추가`→`새로고침`→`닫기`→`검색 폴더 선택`→`샘플 형식 선택` 순서였다. 마지막 두 항목 모두 AX의 **focused button**으로 확인됐다. | **PASS.** build239에서 미확인이던 두 필터 focus가 build240에선 명시적으로 잡힌다. |
| 형식 필터 키보드 | 형식 Button에 Return→`모든 형식` 초점, ↓→`오디오`, ↓→`MIDI`, Return→필터 값 MIDI·결과 1개 MIDI 파일. | **PASS.** |
| 폴더 필터 키보드 | Shift+Tab으로 폴더 Button→Space→`모든 폴더` 초점, ↓→`audio-pack`, Return→현재 형식 MIDI와 교집합 0개. | **PASS.** 서로 다른 폴더·형식의 조합이 실제 목록에 반영됐다. |
| 형식 필터 마우스·외부 클릭 | 형식 Button 클릭→`오디오` 클릭으로 교집합 1개 WAV. 폴더 옵션을 다시 연 뒤 검색 필드를 클릭하자 옵션만 닫히고 `audio-pack`·`오디오` 값은 유지됐다. 열린 폴더 옵션에서 형식 Button을 누르면 형식 옵션으로 바로 전환됐다. | **PASS.** |
| Esc 단계 | 열린 옵션에서 Esc는 옵션만 닫고 해당 필터 Button으로 초점을 돌렸다. 두 번째 Esc는 라이브러리 overlay를 닫고 캔버스로 초점을 돌렸다. | **PASS.** |
| 라이브러리 ⌥Space | WAV가 선택된 상태에서 폴더 필터 Button과 옵션 행에 포커스해 ⌥Space를 눌렀다. UI가 `준비 취소`/`출력 준비 중`을 거쳐 `미리 듣기 정지`/`0.2초`로 진행했으며 필터 값과 전체 곡 play Button은 유지됐다. 다시 단축키를 누르자 준비 정리 후 `미리 듣기`로 복귀했다. | **PASS**, preview 명령 발화/정지와 modifier가 필터 선택으로 오인되지 않는 범위. 실제 스피커 청취 품질은 확인하지 않았다. |
| overlay 밖 ⌥Space | 두 번 Esc로 overlay를 닫고 캔버스 초점에서 ⌥Space를 눌렀다. 전체 곡 play Button이 `stop.fill`로 바뀌고 `00:00.5`/`재생 중`과 Scarlett 출력 연결 로그가 나타났다. 8초짜리 빈 곡이 끝나 자동 정지했다. | **FAIL · P2.** Space 전용 재생이 modifier를 무시한다. 담당 수정 후 build241에서 같은 경로 재검. |

종료 직후의 검사에서는 QA bundle 관련 process/helper가 0으로 기록됐으나, **후속 감사에서 정확한 build240 QA 앱 경로의 PID 30091이 약 28분째 실행 중인 것으로 발견됐다. 따라서 당시 `앱 0` 판정은 철회한다.** 담당자가 이 PID만 SIGTERM으로 종료하고 사라짐을 확인했다. 기존 프로세스 검색이 왜 누락했는지는 미확정이다. QA 전용 `~/Library/Application Support/circlr-build240media-qa/Agent/agent.sock`는 앞선 검사에서 **socket 타입 `srw-------`**로 남았고 `lsof -nU` 리스너는 0이었다. QA `selected.json`은 endpoint `agent.sock`, runID `EE844023-8169-42BB-B616-D4F00E104FC4`, mtime `2026-09-25 01:38:40 KST`였다. 종료의 정확한 시각과 socket inode, **시작 전 support/socket 존재 여부는 기록되지 않았다**. 따라서 이 파일이 이번 실행에서 생성됐는지, 종료 정리 회귀인지 원인을 단정하지 않는다. 당시 QA 경로의 socket 하나만 제거했고 사용자/default socket과 설치 앱은 건드리지 않았다. build241 재검에서는 앱 시작 전 socket 유무·inode, 선택 endpoint, 종료 시각과 종료 후 정확한 PID·bundle 경로를 연속 기록했다.

`system_profiler SPAudioDataType` [전](generated/r80-build240-media-menu/audio-before.txt)·[후](generated/r80-build240-media-menu/audio-after.txt)는 byte-exact로 같고 기본 입력·출력·시스템 출력은 Scarlett 6i6 USB였다. 위 후속 PID 종료 뒤 build240 QA 앱·helper·socket은 0이다. CUA 앱 창 screenshot만 화면 관찰에 사용했고 전체 화면 `screencapture`는 사용하지 않았다. README용 최종 가로·세로 사진은 별도 촬영 gate로 남긴다.
