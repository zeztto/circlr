# 0.80.0 build238 — 격리 native 화면·입력 QA

```text
TEMPLATE_VERSION: qa-response-v1
ROLE: qa
BUILD: 0.80.0 (238), dist/써클러.app
STATUS: FAIL — overlay 키보드 포커스 결함; 입력 캡처 pipeline과 반응형 화면은 범위 내 통과
RELEASE_GATE: build239 이상에서 4개 overlay Tab/Esc 재검 전 0.80 출고 불가
```

`dist/써클러.app`을 그대로 실행하지 않고 `.build/build238-native/써클러 build238 네이티브 검증.app`으로 복제했다. QA bundle ID를 `com.circlr.build238nativeqa`로 바꾸고 문서·URL 형식 등록을 제거한 뒤 ad hoc 서명했다. 원본과 QA 앱 모두 `codesign --verify --deep --strict`를 통과했고, 실행 전후 원본 실행 파일 SHA-256은 `188509986a08d0e37fc01e159a841d409da75998661dbcd0d88b307696881fd1`이다. 복제 앱의 Resources **57/57**, 실행 helper **7/7**은 원본과 byte-identical이다. [번들 검증 기록](generated/r80-build238-native/bundle-integrity.json).

| 항목 | build238 native 결과 | 근거·경계 |
| --- | --- | --- |
| 700×600pt 최소 창 | **PASS, 배치 한정.** 상단바 2행, navigation/media/sound/arrangement 네 overlay의 외곽·검색·닫기·주요 동작이 창 안에 보였다. | `generated/r80-build238-native/*-700x600.source.jpg`, 대응 `.ax.txt`. 캡처는 원본 창 전체 1400×1264px이다. |
| 720×900pt 세로 화면 | **PASS, 시각 배치 한정.** 상단바 간섭 없이 송 궤도와 7개 섹션이 보였고 하단 우측 캔버스 조작 도구와 콘솔이 분리됐다. | [원본 JPEG](generated/r80-build238-native/width720x900-overview.source.jpg), [AX](generated/r80-build238-native/width720x900-overview.ax.txt). |
| 1440×900pt 가로 화면 | **PASS, 시각 배치 한정.** 곡 fit 후 7개 섹션, 라벨, 포트·연결선, 콘솔 오른쪽 캔버스, 우하단 도구가 보였다. | [원본 JPEG](generated/r80-build238-native/width1440x900-overview.source.jpg), [AX](generated/r80-build238-native/width1440x900-overview.ax.txt). 1440×900은 2880×1800px 캡처다. |
| overlay 키보드 | **FAIL, P1.** media/sound/arrangement의 검색 필드에서 Tab 한 번 뒤 focused AX가 overlay를 떠나 `앨범 서클 캔버스`로 이동했다. media·sound에서는 그 상태의 Esc가 overlay를 닫지 못했고 검색 필드를 다시 포커스한 뒤 Esc를 눌러야 닫혔다. arrangement는 Tab→Esc 뒤 닫혔지만 포커스 이탈은 재현됐다. | [media](generated/r80-build238-native/media-after-tab-esc.ax.txt), [sound](generated/r80-build238-native/sound-after-tab-esc.ax.txt), [arrangement](generated/r80-build238-native/arrangement-after-tab-esc.ax.txt). navigation은 열린 상태와 검색 초점만 캡처됐고 Tab 뒤 focused AX는 저장되지 않아 별도 미확인이다. **네 overlay 모두 수정본 재검 필요.** |
| 입력 선택 | **PASS.** 앱 입력 목록에서 `CalDigit Thunderbolt 3 Audio`와 `Scarlett 6i6 USB`를 확인하고 CalDigit를 앱 전용으로 선택했다. 재열린 메뉴에 CalDigit checkmark가 표시되고 QA bundle preference에 선택 UID가 저장됐다. | [초기 목록](generated/r80-build238-native/input-menu.ax.txt), [선택 후](generated/r80-build238-native/input-selected.ax.txt). macOS 기본 입력·출력을 변경하지 않았다. |
| 실제 CAF 생성 | **PASS, 데이터 경로 한정.** `f0r h3r`의 `접속 · DJ Intro`/`클럽 킥 MIDI`에서 CalDigit 선택 후 오디오 take를 시작했다. UI가 `오디오 take 저장 완료`와 `테이크 1개`를 표시했고 CAF가 저장됐다. | [완료 AX](generated/r80-build238-native/record-saved.ax.txt), [CAF 분석](generated/r80-build238-native/caldigit-take-analysis.json), [CAF](generated/r80-build238-native/caldigit-take.caf). 48 kHz Float32 interleaved stereo, 32.0초, 1,536,000 frames, nonzero samples 2,766,795, peak 0.00061035, RMS 0.00012096. 레벨이 매우 낮아 음악 입력 품질·물리 청취 합격 증거는 아니다. |
| 환경 불변·정리 | **PASS.** `system_profiler SPAudioDataType` 전후 출력이 exact match이고 기본 입력·출력·시스템 출력 모두 Scarlett이다. QA 앱은 ⌘Q 후 QA 곡 변경을 버리고 종료했다. 격리 앱/관련 helper process와 `agent.sock`은 0개다. | [전](generated/r80-build238-native/audio-before.txt), [후](generated/r80-build238-native/audio-after.txt). 원본 앱/사용자 곡은 실행·수정하지 않았다. |

이 검증은 native UI의 배치·접근성 상태와 선택된 장치의 nonzero PCM 파일 생성을 확인했다. 파일 드롭 동작, 긴 녹음·장치 분리 복구, 재생 청취, 실제 compositor FPS, 영상 프레임, 모델 AI 작업은 이 실행에서 검증하지 않았다. 사용자가 외부에 있으므로 스피커/헤드폰 청취를 추정하지 않는다. 입력 신호의 매우 낮은 peak는 연결된 장치의 무음/환경 잡음일 수도 있으므로 악기·보컬 소스의 품질 판단에는 사용하지 않는다.

문서용 [가로 화면](../docs/images/circlr-song-build238.png)과 [세로 화면](../docs/images/circlr-song-portrait-build238.png)은 위 CUA 원본 JPEG에서 **크롭·확대·합성 없이** RGB PNG로만 변환했다. 각 PNG의 decoded RGB 픽셀은 대응 JPEG와 동일하다. [픽셀·SHA-256 provenance](generated/r80-build238-native/screenshot-provenance.json). 이 이미지는 build238 관찰 기록이며 overlay 포커스 수정본의 재검 캡처를 대신하지 않는다.
