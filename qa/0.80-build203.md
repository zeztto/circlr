# 0.80 build203 · 종료·재실행·Agent socket QA

상태: **종료 회귀 통과 / 0.80 출고 후보 아님** · 2026-09-24.

`./scripts/build-app.sh`로 0.80.0/build203 arm64 앱과 다섯 helper를 빌드·서명했다. 빌드 로그 `qa/generated/r80-build203-build.log` SHA-256은 `4fccb18447733b6dafc3e7a773ec54eaaae14367eda01fc427e8623548dcbbe9`다. 로컬 격리 QA 패키지 `qa/generated/port-focus/build203/package.json`의 원본 앱 tree SHA-256은 `e93b278d5a4d878865010210ee2cd4ec28ba64c9a08737206adf7f94d54d4f60`이며 QA 앱 및 실행 사본은 strict deep codesign 검증을 통과했다. 사용자 기본 앱과 별도의 `com.circlr.portfocusqa` 저장소만 사용했다.

실제 QA 앱에서 `f0r h3r`를 연 뒤 버전 AX가 **0.80.0 · 빌드 203**인 것을 확인했다. QA 전용 Unix socket의 `snapshot`은 `ok=true`, `runtime.build=203`, `bundleID=com.circlr.portfocusqa`, `dirty=true`로 응답했다. ⌘Q → **변경 버리기** 후 앱 프로세스가 사라지고 QA `Agent/agent.sock`과 `recovery-v2.json`이 모두 없어졌다. 다시 실행했을 때 복구 창 없이 시작했고 `snapshot`은 `ok=true`, `build=203`, 새 빈 앨범 `dirty=false`로 응답했다. 변경 없는 ⌘Q 뒤에도 socket이 남지 않았다.

세 번째 실행에서 데모를 열고 ⌘Q → **취소**를 선택하자 같은 곡의 편집 캔버스로 돌아왔고 socket `snapshot`은 `ok=true`, `build=203`, `name=f0r h3r`, `dirty=true`를 유지했다. 이어 ⌘Q → **변경 버리기**로 정상 종료했고 socket과 v2 복구본은 남지 않았다. QA legacy `recovery.json` SHA-256 `0c7507e9686665d048b82c5b5780d86e36dfda94fc1b91759c0f35702d8380e7`와 사용자 기본 `circlr/recovery.json` SHA-256 `688558b00e4160106065114e7a6305ffa1a24ea8f57636408f408e38e5148c38`은 검증 전후 동일했다.

Core 최신 clean scratch Swift 1,112개·11개 skip·실패0과 MCP server 28/28, 통합 39/39, agent kit 9/9, stage cancel 8/8은 [build201 기록](0.80-build201.md)의 소스 검증이다. App target의 독립 `AgentSocketSafetySmoke`도 active listener·bind-only·stale·교체 후 deinit 보존을 통과했다(`qa/generated/r80-build203-agent-socket-safety.log`, SHA-256 `88e0dbd653b762d11e51c3b537f4547bf66cd8b11f5b2dc465cc35aa25a0ea58`). 이후 app 종료 경로 변경은 이번 app 빌드·native 종료/취소/재실행으로 확인했다. 영상 저장을 기다리는 비동기 종료의 편집 경합은 독립 보안 리뷰에서 새 P1/P2가 없었지만 native 동시성 재현은 하지 못했다.

후속 독립 리뷰에서 **임시 경로 없이 공개 경로에 bind한 직후 inode를 읽는 경합**을 발견했다. 구버전 앱이 그 사이 경로를 교체하면 자기 socket이 아닌 inode를 소유한 것으로 기록할 수 있어 build203을 최종 후보로 삼지 않는다. build204에서 전용 임시 경로와 원자적 no-replace 게시로 수정·재검증한다. crash-stale socket의 안전한 복구 UI, 실제 내장 출력 청취, 마이크 녹음, 장시간·정확한 1080p 영상과 한 곡 전체 제작 동선도 계속 열려 있다. `v0.80.0` 태그와 Release를 만들지 않는다.
