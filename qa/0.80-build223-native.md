# 0.80.0 build223 — 반응형 궤도 native QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build223-native
STATUS: FAIL — 720×900 기본 콘솔에서 7개 섹션 중 04/06/07 이름이 화면상 숨음
COVERAGE: 번들 f0r h3r 첫 로드, 1440→720 창, 콘솔 40/122/180pt·접기, 감상 모드 왕복, 수동 확대·팬의 창/콘솔 전환 보존
FAILURES: 720×900 첫 리사이즈의 04/06/07 픽셀 라벨 누락; 40pt/접기에서도 04 누락 지속
BLOCKERS: 재생·녹음·실제 청취·Scarlett 장치 동작은 이번 화면 QA 밖
RISK_CALLS: AX 01–07과 서클은 유지돼 음악 데이터 소실은 아님. 팬으로 04가 나타나므로 작은 viewport의 라벨 후보/edge 배치 문제로 보이나 구현 원인은 소스 감사 필요
VERIFIED_ENVIRONMENTS: native macOS, circlr 0.80.0/build223, 격리 com.circlr.build223nativeqa bundle/Support
ARTIFACTS: qa/generated/r80-build223-native/ (Git-ignored)
DELIVERABLE: 전체 창 JPEG 원본·PNG·AX·SHA-256 증거 및 종료 감사; 다음 owner development lead
```

2026-09-24 KST. 서명된 `dist/써클러.app` build **223**을 고유 bundle ID로 복사·ad-hoc 재서명해 실행했다. 원본 `Demos/f0r-h3r.circlr/manifest.json`에는 저장된 `hierarchyView`가 없고 섹션은 7개다. 시작 화면의 `f0r h3r`을 눌러 단일 곡 궤도와 01–07 서클이 곧바로 열린 것을 확인했다. 1440×900 전체 창에서는 섹션 이름 7/7, 곡 이름, 연결, 상단 컨트롤이 보였다. AX도 `0.80.0 · 빌드 223`, 곡, 섹션 01–07을 보고했다.

같은 창을 **1440→720×900**으로 줄이면 상단 컨트롤은 두 줄로 재배치되어 겹치지 않는다. 서클 7개와 AX 섹션 7개는 유지되지만 기본 **122pt 콘솔**에서 화면에 보이는 이름은 01/02/03/05뿐이고 **04 Club Chorus, 06 Final Chorus, 07 DJ Outro** 라벨은 표시되지 않는다. 안정 후 재관찰해도 동일하다. 해당 서클들은 화면 안에 있으므로 원이 통째로 잘린 것은 아니다. 라벨은 viewport 밖으로 갈 수 있는 쪽의 서클에서 주로 누락된다. **40pt 콘솔 및 콘솔 접기**에서는 06/07이 나타나지만 04는 여전히 빠진다. **180pt 콘솔**에서는 다시 04/06/07이 빠진다. 수동 확대 두 단계 뒤 `H` 화면 이동 모드에서 오른쪽으로 약 70px 팬하면 **04 라벨이 나타난다**. 이 증거는 음악 데이터/AX 누락보다 작은 viewport의 라벨 후보와 edge/겹침 처리 문제를 가리킨다. 정확한 소스 원인은 별도 감사가 필요하다.

감상 모드 `⇧⌘V` 진입 시 전체 텍스트·컨트롤을 숨기고 서클·연결만 표시했다. 다시 `⇧⌘V`를 누르면 일반 캔버스와 컨트롤이 복원됐다. 수동으로 `+` 두 번 확대하고 `H`로 팬한 뒤 창을 720→1440→720으로 변경하고 180pt 콘솔을 다시 펼쳤다. 곡 링의 확대 반경과 팬 오프셋이 자동 첫 로드 구도로 돌아가지 않고 유지됐다. 콘솔이 커지면서 아래쪽 원/라벨은 콘솔 뒤에 가려졌지만 이것을 카메라 리셋으로 판정하지 않았다. `manual-zoom-pan-720x900.*`와 `manual-return-720-console-large.*`가 해당 화면/AX다.

CUA `getScreenshot()`의 전체 창 JPEG bytes를 `.source.jpg`로 저장하고 `sips`로 **자르기·크기 변경 없이 PNG 변환**했다. 같은 이름의 `.ax.txt`가 각 창의 접근성 상태다.

| PNG | 픽셀 | SHA-256 |
| --- | --- | --- |
| `qa/generated/r80-build223-native/demo-first-load-1440x900.png` | 1440×900 | `b11e0f6130d88238575321775a5b84742c41c741e4de8095b75229460cffec79` |
| `qa/generated/r80-build223-native/demo-resize-720x900.png` | 720×900 | `c602b5244ebb06a20c021b1c9c28937fa90a80291a8af0647d535bf942c543c9` |

기본 콘솔/리사이즈 결함 외의 전환 증거는 `console-small-720x900.*`, `console-large-720x900.*`, `console-collapsed-720x900.*`, `viewing-mode-720x900.*`에 있다. 배포 앱 **65/65개 파일**의 SHA-256이 검사 전후 같고, 배포/격리 Resources **57/57개**, 동봉 데모 **7/7개**도 같다(`baseline-app.json`, `final-check.json`). 원본 실행 파일 SHA-256은 **`17a6293add56acdcee8cfed6479dfa0ec75c52c7acfb4e50d18b5fc5c4a5e6a6`**다. 두 앱의 `codesign --verify --deep --strict`가 통과했다. 격리 데모 세션을 저장하지 않고 버린 뒤 앱/helper PID는 **0개**다. 사용자 프로젝트와 시스템 오디오 설정은 조작하지 않았으며 재생·입력도 시작하지 않았다.
