# 0.80 build232 · AI 중단 경계 독립 검토

```text
TEMPLATE_VERSION: code-review-v1
REVIEWED: AgentConsole.swift, AgentSessionGateway.swift, AppStore.swift, AppOwnedAgentTurnIntegrationTests.swift
VERDICT: 최종 diff에서 새 고확신 차단 결함 없음
```

읽기 전용 검토자는 콘솔의 내부 AI 소유 job만 활성 lease와 job ID가 일치할 때 `stopTrustedAgentTurn()`으로 보내고, 일반 공개 MCP job은 기존 `cancelAgentJob`으로 남는 것을 확인했다. 전자는 lease 철회→전용 socket 종료→소유 job 취소 순서이고, 후자는 권한 철회나 DAW `stop()`을 호출하지 않는다. 모든 전환은 MainActor에서 처리하고 비동기 게시에는 generation·trusted lease commit 검사가 있다. `trustedAgentJob`을 `@Published`로 바꾼 것은 버튼 라벨 상태의 재표시이며 외부 ingress 권한을 넓히지 않는다.

최초 리뷰 때 일반 job fixture가 가짜 실행 상태라는 한계가 있어 최종 테스트를 **실제 공개 `executeAgent` bounce job**으로 바꿨다. 다시 읽기 전용 검토한 결과 새 차단 결함은 없었다. [최종 집중 9개·전체 1,232개 PASS](0.80-build232.md)의 범위는 코드·fixture다. 재생 중 실제 transport 보존, SwiftUI 버튼 클릭/AX, 제품 내부 Codex 계정 대화는 이 리뷰나 자동 테스트로 입증되지 않는다. 검토자는 파일을 수정하거나 앱을 실행하지 않았다.
