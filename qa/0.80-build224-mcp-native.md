# 0.80 build224 · 격리 실제 앱 MCP 제작 QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build224-mcp-native
STATUS: PASS for the bounded native MCP DAW workflow; release-wide gates remain open
COVERAGE: typed MIDI step/Undo/stale rejection; save/native reopen; gain FX/active automation; 70-second WAV/PCM; two track bounces; final save/native reopen
FAILURES: Product failure 0 in this flow. Initial auxiliary QA script's graph JSON-shape assertion failed after a successful FX edit; the script was corrected in ignored evidence and resumed from revision 5.
BLOCKERS: actual playback/listening, microphone take, GUI authoring, AU/plugin and long-run behavior are not covered
VERIFIED_ENVIRONMENTS: macOS native dist/써클러.app 0.80.0/build224 copied to com.circlr.build224mcpqa with its own Support root
ARTIFACTS: qa/generated/r80-build224-mcp-native/ (Git-ignored), ~/Library/Application Support/circlr-build224mcp-qa/fixtures/
```

2026-09-24 KST. 배포 후보 `dist/써클러.app`을 **새 앱** `qa/generated/r80-build224-mcp-native/써클러 build224 MCP 검증.app`으로 `ditto` 복사해 bundle ID `com.circlr.build224mcpqa`로 바꾸고 ad-hoc 재서명했다. 이 bundle의 저장 영역은 `~/Library/Application Support/circlr-build224mcp-qa`이다. 기존 `circlr-integration-qa`, 사용자 앱/프로젝트, 시스템 오디오 기본 설정을 사용하거나 변경하지 않았다. 두 앱 모두 검사 후 `codesign --verify --deep --strict`를 통과한다. 배포 앱 실행 파일은 시작·종료 모두 SHA-256 `f35c111594cb4ddd2cf3ec490a5dc6520fcac90466184ad0675a1676dbbfd0e9`이며, 배포/격리 Resources **57/57개 hash 동일**이다.

번들 Bach Invention 8을 새 Support의 `fixtures/bach-build224-mcp.circlr`로 복사하고 **프로젝트 ID만** `A98B8F48-3E98-5E0B-AA3E-7CD07447B480`으로 바꿨다. 원본 manifest SHA-256은 전후 `a0350b4802b192bf3d7ba207a9a031f166c0c5092e60faf0f82f766da3257c34`로 같았다. 초기 QA 사본 manifest SHA-256은 `5cd3c4335f4669190d651ef688ae6ce7baa2d23a93b0c72700648dca99d20f7a`였다. 최종 QA 사본에는 bounce 오디오 파일 2개가 추가됐고, 원본에 존재하던 비 manifest 파일의 hash가 바뀐 것은 없다. 전체 사본 목록/해시는 ignored `final-inventory.json`에 있다.

## 실행 및 결과

`qa/verify-agent-journey.py`는 `com.circlr.integrationqa`와 그 Support 경로를 하드코딩한다. **원본 스크립트는 바꾸지 않고**, `qa/generated/r80-build224-mcp-native/verify-agent-journey-build224.py` 사본의 예상 bundle ID·Support·repo root만 고유 경로로 바꿨다. 해당 실제 앱의 `Agent/agent.sock`에서 stdio MCP adapter를 사용했다. 앱/소켓은 시작 후 약 2초 안에 준비됐다. 오디오 장치 시작/녹음/Play는 요청하지 않았다.

```sh
python3 qa/generated/r80-build224-mcp-native/verify-agent-journey-build224.py \
  --project "$HOME/Library/Application Support/circlr-build224mcp-qa/fixtures/bach-build224-mcp.circlr" \
  --evidence qa/generated/r80-build224-mcp-native/base-journey --build 224
python3 qa/generated/r80-build224-mcp-native/fx-automation-journey.py
```

첫 동선에서 27개 MCP tool catalog, 현재 build·bundle과 깨끗한 프로젝트를 확인했다. 첫 MIDI lane에 pitch 95 step 하나를 더해 노트 **76→77**, 오래된 revision 요청 거절, Undo 후 노트 배열의 정확한 복귀, 재적용·저장·native `open` 후 같은 ID/노트/revision/manifest 확인을 통과했다. 70초 master WAV는 48kHz, stereo, 24-bit, 3,360,000 frames, nonzero PCM이었다. 첫 track bounce는 새 오디오 서클을 만들었고 revision **4**로 저장됐다. 첫 WAV SHA-256은 `bbff5de94588bc2a877038115ce13c9451e4ecad2a37199aba8c834a9d9f849e`다.

두 번째 동선은 같은 사본의 두 번째 synth track `유리 건반`의 instrument→output 사이에 gain FX(0.65)를 삽입하고, 해당 FX에 활성 구간을 포함하는 gain automation **beat 0: 0.2 → beat 24: 0.5**를 적용했다. 각 단계에서 새 WAV로 export한 뒤 Python `wave`와 NumPy로 24-bit signed PCM을 직접 읽고 전체 sample의 최대 차이를 비교했다. 그래프 node와 automation 점은 `inspect`로 확인했고, FX/automation 저장·native `open` 후에도 같은 값을 확인했다. 이어 두 번째 track을 bounce해 오디오 서클을 만든 뒤 export했다. bounce 전후 master 최대 차이는 **1 LSB**다. 최종 revision **7**과 manifest SHA-256 `252a683f267264c354ff1ef8fabd93d87673db4c8afbce893f72e0377cfbc71b`는 마지막 저장·재열기 후에도 같았다.

| 단계 | WAV SHA-256 | RMS (24-bit integer) | 직전 PCM과 최대 차이 |
| --- | --- | ---: | ---: |
| 첫 bounce 후 FX 이전 | `306580a6bc47e4d9032ba65780adfbef8eee2f99a5c3bc0640b9e2c2ec85d2b1` | 412,111.28 | 기준 |
| gain FX | `f5e11beb17fb0eff65c654b7e4086a5817daeb60c0ebed5ca745945ac39a0ddc` | 392,707.64 | 445,872 LSB |
| 활성 gain automation | `740403a1716166d5622bdb9f5becd4292ee97364a08caa13239b385ab50b83d3` | 380,031.66 | 588,281 LSB |
| 두 번째 track bounce 후 | `4ba1c1359fb93bd7b04075872753ead36d2191d0d0855ec53d5552200b4aeecd` | 380,031.66 | 1 LSB |

위 네 WAV 모두 **3,360,000 frames, 48kHz/stereo/24-bit, nonzero**이다. 전체 raw 단계와 경로는 ignored `fx-automation-report.json`, `base-journey/report.json`, `.log`에 있다. 활성 automation의 PCM 차이는 실제 소리 처리 결과를 보여 주지만 음악적 완성도나 물리 청취 판정은 아니다.

두 번째 QA 스크립트의 첫 실행은 FX 편집이 성공해 revision 5가 된 직후, **QA 코드**가 effect JSON을 `content.effect.kind`로 잘못 가정해 assertion 실패했다. 실제 `inspect` 형태는 `content.effect._0.kind`다. 제품 오류가 아니므로 ignored 스크립트만 고쳤고, 같은 fixture의 dirty revision 5를 검증해 이미 만들어 둔 pre-FX WAV에서 이어서 실행했다. 원본 프로젝트 또는 배포 앱을 다시 패키징하지 않았다.

격리 앱은 모든 저장이 끝난 뒤 AppleScript quit으로 종료했다. 해당 `Agent/agent.sock`은 사라졌고 격리 앱/자식 audio helper PID는 **0개**다. `play`, `record`, 출력 선택/기본 장치 변경 API를 호출하지 않았다. 1.0 gate 중 **실제 입력 take·물리 재생/청취·GUI 전체 제작·AU/plugin·장시간 안정성**은 여전히 별도 검증이 필요하다. 구형 Scarlett의 현 macOS 호환성을 이번 오프라인 결과로 추정하지 않는다.
