# 0.80 짧은 MP4 실제 앱 검증 · build184

상태: **부분 통과 / 출력 신뢰성 gate 유지** · 2026-09-23. `com.circlr.integrationqa` 0.80.0/build184의 Bach Invention 8 QA 작업 사본으로만 녹화했다. 사용자 앱·프로젝트·시스템 출력 설정은 변경하지 않았다. 앱 SHA-256은 `df43e4604eb50680e574f2e3a03accd66bc836d6e884054fade157037e26483f`다.

같은 build184 앱의 이전 4회 시도는 `AVAudioEngine.outputNode`의 HAL 대기 뒤 timeout으로 끝났다([출력 조사](0.80-output-investigation.md)). 이번 시도는 선택된 Scarlett 6i6 USB가 출력 시작 상태에 도달했고 앱의 `영상 녹화 중`→`영상 저장 완료 · 139프레임` 상태를 확인했다. 따라서 **간헐 실패가 해결됐다는 증거가 아니다**.

생성물 `qa/generated/r80-build184-qa-recording.mp4`는 754KB, SHA-256 `88422fc24d8240039196b5ea7edec4f208d4cfb1af49235cb7790b3fa46e94e9`다. Git 제외 QA 자료다. `swiftc -parse-as-library qa/inspect-movie-timing.swift -o qa/generated/inspect-movie-timing`으로 빌드한 도구를 실행해 `qa/generated/r80-build184-qa-recording-inspection.json`을 보존했다. 별도 `afinfo` 및 `afconvert`로 AAC를 PCM으로 해독했다.

| 항목 | 관찰 |
|---|---|
| 비디오 | 1426×1080 H.264, 4.6307초, 표시 프레임 139개, 평균 30.02fps |
| 프레임 간격 | p50 33.33ms, p95/p99 43.33ms, 최대 43.33ms, 66.67ms 초과 0, 동일 PTS 0, 인접 화면 해시 동일 0 |
| 오디오 | stereo 48kHz AAC, 첫 PTS 0, 유효 222,272 PCM frames/4.6307초. 해독한 16-bit PCM의 peak 7,726, RMS 1,238로 무음은 아님 |
| 시작 동기 | 첫 video/audio PTS 모두 0ms. 중간·끝의 청감 동기나 재생 장치 출력은 이 측정만으로 확인되지 않음 |

앱 내 시간 표시가 약 4.9초였던 순간 녹화를 끝냈고, 파일의 인코딩 duration은 4.6307초였다. 두 값은 관찰 시점과 파일 마무리 시점이 달라 직접 오차로 판정하지 않는다. 이 영상은 현재 창 비율의 1426×1080 결과이며 1920×1080 목표는 아니다. 독립 플레이어의 실제 시청·청취, 고밀도 장면·감상 모드, 반복 출력 시작, 장시간 FPS와 중간/끝 AV 동기는 미검증이다. [0.80 계획](../docs/releases/0.80.0.md)의 Q80-06/07을 PASS로 바꾸지 않는다.
