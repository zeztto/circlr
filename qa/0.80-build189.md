# 0.80 build189 · 선택 궤도 native UI 회귀

상태: **R80-01 실패 · 개발 빌드 출고 금지** · 2026-09-23. `release/0.80.0`에서 빌드한 0.80.0/build189 앱을 `com.circlr.integrationqa`로 복사·ad-hoc 서명하고, 번들 `f0r h3r`의 프로젝트 ID만 바꾼 QA 사본을 열었다. 원본 앱과 QA 앱의 `codesign --verify --deep --strict`가 통과했다. 격리 앱 tree SHA-256은 `c47f205f13031ea219d2b27c146c0bd83563eb99ef72aeccc75e5003021eb4cb`, 작업 사본 tree SHA-256은 `4184917e7aed80c667ae8ab4c362fdb3ce05afd273d7e1e672285da4abe26df8`이다. 재현 가능한 로컬 package 기록은 `qa/generated/outward-orbit/build189/package.json`에 보관한다.

- build189 앱 빌드 시점의 전체 Swift 테스트는 **1,056개 실행·11개 skip·실패 0**이었다. 원시 로그 `qa/generated/r80-build189-full-suite.log` SHA-256은 `88f99f31b5bc42fda59a15a3c563a590f655b0a0ccd6f0eab852d3d8040`이다. 이후 내부 AI 프로토콜 리뷰 수정은 이 숫자에 포함되지 않는다.
- 빌드 로그 `qa/generated/r80-build189-build-app.log` SHA-256은 `20ee35bba37431b2d08ae9c41f8294ee640d47f12c3a64b2f6f16cf42ccad319`이다. 실제 앱 AX에서 **0.80.0 · 빌드 189**와 `f0r h3r` 열기를 확인했다.
- Native 재현: 앨범에서 `앨범 사운드`를 선택하면 중심 궤도와 대부분의 출력 위성이 보인다. 이어 `Left`로 곡, `Right`로 앨범 사운드를 키보드 선택하면 중심 궤도는 선택되지만 아래 트랙·효과 위성 여러 개가 캔버스 아래로 잘린다. 약 1659×1387 창에서 중심 궤도가 화면 y≈900으로 이동하고 하단 서클이 y>1380으로 벗어났다. AX에는 노드가 남아 있다. 즉 단위 테스트의 제한된 위성 fixture는 실제 밀도와 이동한 camera를 충분히 대표하지 못했다.
- 이 빌드에서 재생·물리 청취·녹화는 실행하지 않았다. 시스템 기본 출력은 건드리지 않았고, QA 앱은 종료했다. build188의 반복 무음 출력 증거와 이 UI 검사는 서로 다른 빌드·범위다.

원인 후보는 reveal이 선택 궤도에 맞춘 뒤 **기존 viewport와 교차하는 위성만** 포함한다는 것이다. 이미 화면 밖인 근접 위성은 제외된다. R80-01은 근접 출력 클러스터를 함께 맞추되 의도적으로 먼 outlier가 과도한 줌아웃을 강제하지 않도록 수정한 다음 새 앱에서 같은 키 순서로 재검증한다. 0.80 정식 Release와 사용자 설치 앱에는 build189를 게시하지 않는다.
