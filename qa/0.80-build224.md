# 0.80.0 build224 — 세로 송폼 라벨 가시성

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build224
STATUS: PASS for specified UI flow — 전체 Swift·패키지·격리 native 화면 통과; 0.80 출시 gate 별도
COVERAGE: 직접 섹션 라벨의 좁은 viewport 배치, 기존 충돌·선택 규칙, 앱/5 helper 서명
PASS: 대상 Swift 9개 실패0; 전체 Swift 1,189개 중 14 skip·실패0; build224 arm64 앱 strict deep signing
BLOCKERS: 실제 take·청취·장시간 영상·전체 제작/AI 동선
ARTIFACTS: qa/generated/r80-build224-targeted.log, qa/generated/r80-build224-swift-test.log, qa/generated/r80-build224-package.log
DELIVERABLE: 개발 브랜치 후보; tag/Release 보류
```

build223의 [native 실패](0.80-build223-native.md)는 720×900에서 콘솔 높이에 따라 직접 섹션 04·06·07 라벨을 생략했다. build224는 좁은 곡 궤도에서만 직접 섹션 라벨이 화면 내 빈 위치를 추가로 찾도록 한다. 원·다른 라벨·편집 도구와 겹치면 여전히 배치하지 않으며 기존 가까운 위치를 먼저 시도한다. 사용자가 원의 위치를 바꾸거나 텍스트를 축소하지 않는다.

`CanvasLabelLayoutTests|CanvasContextFitTests` 대상 **9개 실패 0**이며 마지막 소스의 `swift test` 전체 **1,189개 중 14 skip·실패0**이다. 0.80.0/build224 arm64 앱과 다섯 helper, 25파일 agent kit을 패키징했고 `codesign --verify --deep --strict` 통과했다. 앱 실행 파일 SHA-256은 `f35c111594cb4ddd2cf3ec490a5dc6520fcac90466184ad0675a1676dbbfd0e9`다. 격리 실제 앱의 가로/세로·콘솔 상태·감상 모드 판정은 [native QA](0.80-build224-native.md)에 분리한다. 화면 QA가 통과해도 0.80 전체 출고 gate는 별개다. [읽기 전용 리뷰](0.80-build224-review.md)는 새 P1/P2를 찾지 못했고 7개 밀집 라벨의 추가 배치 계산을 호출당 약 0.008ms로 측정했다. 실제 표시 FPS는 이 측정으로 증명하지 않는다.

[격리 native QA](0.80-build224-native.md)는 1440×900 첫 로드와 720×900의 콘솔 122/180/40pt·접기 상태 모두에서 섹션 서클·이름 **7/7**을 픽셀과 AX로 확인했다. 상단 조작 겹침 없음, 감상 모드 왕복, 수동 확대/팬 뒤 resize 보존도 통과했다. 화면 이미지와 원본 데모·패키지 65/65 파일 hash를 분리해 기록했고 QA 앱/helper는 종료했다. 재생·실제 입력·물리 청취는 이 화면 검증에 포함하지 않는다.
