# 0.80.0 build241 — 미디어 필터·단축키 native QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build241-media-shortcuts
STATUS: passed (실행한 필터·단축키 경로)
COVERAGE: 700×600pt 미디어 overlay의 필터·입력·Esc·포인터; 캔버스와 감상 모드의 ⌥Space; 평문 Space; 가로·세로 f0r h3r 전체 창; 고유 QA 앱 종료·socket·오디오 기본값
FAILURES: 없음 (실행한 경로)
BLOCKERS: CUA가 fn 키를 지원하지 않아 Fn+Space OS 전달 미검증; 실제 한글 IME 조합 미검증; 50k 목록 Tab 지연 미계측
RISK_CALLS: 재생 준비/취소만 확인해 물리 청취·오디오 품질을 판정하지 않음; 스크린샷은 UI·레이아웃 증거이며 실제 FPS 증거가 아님
VERIFIED_ENVIRONMENTS: macOS, dist/써클러.app 0.80.0/build241의 별도 ad-hoc signed com.circlr.build241nativeqa 및 com.circlr.build241photoqa
ARTIFACTS: qa/generated/r80-build241-native/ 번들·fixture·socket·오디오 전후 기록; qa/generated/r80-build241-photo/ 창 단위 JPEG·픽셀 출처·종료 기록; docs/images/circlr-song-build241.png 및 portrait
DELIVERABLE: build240의 캔버스 ⌥Space 재생 회귀 재현 안 됨; 두 QA 앱·socket 종료 확인; README 가로·세로 이미지 갱신
```

원본 `dist/써클러.app` 실행 파일 SHA-256은 `eebe74c188d588816de98dae44dcb19364039d452746e2ad1892683a0e89f98c`, 버전은 `0.80.0/build241`이다. 고유 QA 앱 `.build/build241-native/써클러 build241 네이티브 검증.app`으로 복제해 `com.circlr.build241nativeqa`를 지정하고 문서·URL 형식 등록을 제거한 뒤 ad-hoc 재서명했다. 원본과 QA 앱 모두 `codesign --verify --deep --strict` 통과, Resources 57개와 helper 7개 byte 일치, 원본 실행 파일 SHA가 종료 후에도 같았다. [복제 기록](generated/r80-build241-native/bundle-integrity.json), [최종 기록](generated/r80-build241-native/final-integrity.json).

QA 앱의 빈 곡에서 ignored [fixture](generated/r80-build241-native/fixtures/)의 1초 48 kHz mono WAV와 1노트 MIDI 파일을 각각 `audio-pack`, `midi-pack`으로 등록했다. 사용자 샘플 폴더는 등록하지 않았고 파일을 곡에 가져오지 않았다. CUA의 앱 창 screenshot에서 약 700×600pt(1400×1264px) 창의 두 줄 상단바, 라이브러리 검색·필터·목록·footer가 동시에 보이고 겹침/잘림은 보이지 않았다. 이 screenshot은 파일로 보존하지 않았으므로 픽셀 감사 자료는 아니다.

| 경로 | 실제 AX·화면 관찰 | 판정 |
| --- | --- | --- |
| Tab·Shift+Tab | 검색에서 Tab 1–7회가 `폴더 관리`→`폴더 추가`→`새로고침`→`닫기`→`검색 폴더 선택`→`샘플 형식 선택`→`모두 선택`으로 이동했고 각 항목이 focused였다. `모두 선택`에서 Shift+Tab은 형식 필터로 돌아왔다. | **PASS** |
| 형식 필터 | Return으로 옵션을 열면 `모든 형식` focused, ↓는 `오디오`, Return은 값 `오디오`와 WAV 1개. | **PASS** |
| 폴더 필터 | Shift+Tab→Space는 `모든 폴더` focused; ↓ 두 번으로 `midi-pack`, Return 후 `오디오`와의 교집합 0개. | **PASS** |
| 포인터 | 형식 필터를 클릭해 열고 MIDI 행을 클릭하자 값 `MIDI`와 해당 MIDI 파일 1개가 표시됐다. | **PASS** |
| 검색 입력 | 검색 필드에 `qa`를 설정하고 평문 Space를 보내자 값이 `qa `가 됐다. 이어 ⌥Space는 검색 필드 초점을 유지했고 곡 transport를 시작하지 않았다. | **PASS**, ASCII 입력·modifier 범위. 한글 IME 조합 이벤트는 미검증. |
| Esc 단계 | 열린 폴더 옵션의 Esc는 옵션만 닫고 필터 버튼에 초점 복귀, 두 번째 Esc는 라이브러리를 닫고 캔버스에 초점 복귀했다. | **PASS** |
| 캔버스 ⌥Space | overlay 밖 캔버스에 ⌥Space를 보낸 뒤 AX는 `play.fill`·`00:00.0`을 유지했다. build240의 전체 곡 재생이 재현되지 않았다. | **PASS** |
| 평문 Space | 캔버스 Space로 `stop.fill`·출력 준비 시작, 다시 Space로 준비 취소·`play.fill`·`00:00.0` 복귀. | **PASS**, 시작/취소 조작. 완전 재생과 물리 청취는 별도 범위. |
| 감상 모드 ⌥Space | `⇧⌘V`로 감상 모드에 들어가 ⌥Space를 보내도 AX 재생 버튼은 `play.fill` 그대로였다. 같은 단축키로 복귀했다. | **PASS** |
| Fn+Space | CUA `pressKey('fn+space')`가 `keyNotFound("fn")`을 반환했다. | **미검증** — OS 전달 여부를 판단하지 않음. |

동봉 `f0r h3r.circlr`는 첫 QA 앱의 파일 열기와 별도 촬영 QA 앱의 시작 화면 선택으로 로드되어 `0.80.0 · 241`, `00:00.0`, 섹션 01–07 궤도와 레이블을 앱 창에서 확인했다. 첫 앱이 놓인 세로 디스플레이의 가용 폭은 1080pt였다. 촬영 전용 앱은 macOS Window 메뉴의 `Move to LEN P27h-10`으로 가로 디스플레이에 옮긴 뒤 CUA로 **1440×900pt**와 **720×900pt** 창을 만들었다. 캔버스 이동 도구와 40pt 콘솔 높이로 정지된 일곱 섹션이 모두 보이게 했다. 50k 파일 목록의 Tab 지연은 이 native 회귀에서 계측하지 않았다.

촬영 전용 앱 `com.circlr.build241photoqa`는 원본과 Resources 57개·helper 7개가 byte-identical인 별도 ad-hoc 서명 복제본이다. [복제 기록](generated/r80-build241-photo/bundle-integrity.json). 정확한 QA 실행 PID `39279`에 속한 layer-0 `써클러` 창 하나의 CGWindowID `18365`와 bounds **1440×900pt / 720×900pt**를 각각 확인한 뒤 `screencapture -x -o -l 18365 -t jpg`로 **그 창만** 저장했다. 원본 JPEG를 CUA 앱 screenshot과 나란히 관찰해 정지한 곡 궤도·일곱 섹션·상단바·콘솔이 일치하고 다른 데스크톱 내용이 없음을 확인했다. CUA와 `screencapture`의 byte 동등성을 주장하지 않는다. JPEG 원본의 RGB 픽셀을 자르기·리사이즈·합성 없이 PNG로 변환했고 각 published PNG와 decoded pixel 동등성을 확인했다. [가로](../docs/images/circlr-song-build241.png) SHA-256 `5e4d9d1af49157576383bcc9c8c0ec20db5f802cbda05515243b744f72b1b491`, [세로](../docs/images/circlr-song-portrait-build241.png) `7fb023152febd033469febe39d6e4d3445e97d6f1989283215c8149c4031e54a`; 원본 JPEG 해시·RGB 해시·창 bounds는 [provenance](generated/r80-build241-photo/screenshot-provenance.json)에 보존했다.

시작 전 QA `~/Library/Application Support/circlr-build241native-qa/Agent/agent.sock`는 존재하지 않았고 production socket도 없었다. [시작 전](generated/r80-build241-native/support-before.json), 실행 중 endpoint `agent.sock`, runID `2D476435-5B62-40E1-9606-B88F1A51B05C`, socket inode `25763406`을 기록했다. [실행 중](generated/r80-build241-native/support-running.json). 종료 직전 정확한 QA 실행 파일 PID `36474`와 QA Agent 리스너를 확인했다. ⌘Q 후 CUA의 즉시 재관찰은 `procNotFound`를 반환했고, 후속 `ps -p 36474` 및 정확한 QA bundle 경로 검색에서 앱 0, QA 관련 helper 0, `lsof -nU`에서 QA Agent 리스너 0이었다. [종료 후](generated/r80-build241-native/support-after.json) QA socket은 **없고** `selected.json`만 남았다. production socket은 전후 모두 없었다. 이전 build240 잔존 PID 사건을 고려해 PID와 bundle 경로를 함께 검사했다.

`system_profiler SPAudioDataType`의 [전](generated/r80-build241-native/audio-before.txt)·[후](generated/r80-build241-native/audio-after.txt)는 byte-exact로 같았다. 앱 전용/시스템 기본 입출력은 변경하지 않았다. 이 문서는 UI 동작과 정상 종료 범위의 QA이며 전체 DAW·모델 E2E·성능·물리 청취의 합격 증거가 아니다.

촬영 QA 앱도 `⌘Q` 후 정확한 PID `39279`·bundle 경로 process·QA Agent socket·`lsof` 리스너가 모두 0이었다. 촬영 전 QA socket과 production socket은 없었고, 종료 뒤 `selected.json`만 남았다. 원본 실행 파일·데모 manifest 해시와 두 서명은 유효하며 촬영 전후 `system_profiler SPAudioDataType`은 byte-exact로 같다. [촬영 앱 종료 기록](generated/r80-build241-photo/final-integrity.json).
