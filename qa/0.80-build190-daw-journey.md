# 0.80 build190 · GUI FX/automation 제작 동선

상태: **한정 PASS / 0.80 릴리스 승인 아님** · 2026-09-23. 이 기록은 build191의 기능 검증 결과가 아니다. 실제 테스트 대상은 `release/0.80.0`의 **0.80.0/build190** 앱을 별도 QA 번들 `com.circlr.portsqa`로 복사·ad-hoc 재서명한 `써클러 제작 검증.app`이다. 기존에 실행 중이던 build187 QA 앱과 사용자 설치 앱은 건드리지 않았다. 시스템 출력/입력 설정도 바꾸지 않았다.

## 입력과 격리

- build190 번들 데모 `f0r-h3r.circlr`의 `manifest.json` SHA-256: `a75ea9f2f3917f82184ba94c31e071905c0c6a1bfa2e90259ad554eaa5c794d7`. 종료 후 다시 계산해 불변을 확인했다.
- QA 작업 사본: `~/Library/Application Support/circlr-ports-qa/fixtures/r80-build190-dawjourney-f0r-h3r.circlr`. 사본 생성 시 프로젝트 ID만 `1F28F59F-6369-5007-B3CB-FA5F2D4428C4`로 교체했다. 사본 생성 직후 manifest SHA-256: `ac4c2e21278b67a482051fbc805ebd85c319b1026e69311a1c2782cc7dd20052`.
- QA 앱 실행 파일 SHA-256: `60595276d4ff475a45cd22e18d617e520d40bc77c27cf49693f890d52f938684`. `codesign --verify --deep --strict` 통과. 격리 패키지 메타데이터는 Git 제외 `qa/generated/r80-build190-dawjourney-qa/package.json`에 있다.

## 실제 GUI 조작과 관찰

1. 앱을 `--open`으로 QA 사본에 열고 `앨범 사운드 → 마스터 헤드룸`으로 이동했다. Gain 숫자 필드가 **0.78 dB**였다. `0.60`을 입력·Return: UI **0.60 dB**, 음악 revision **r2**. `⌘Z`로 **0.78 dB/r3**, `⇧⌘Z`로 **0.60 dB/r4**를 확인하고 `⌘S`로 저장 완료를 확인했다.
2. `앨범 → f0r h3r 곡 → 접속 · DJ Intro → 오로라 패드 MIDI → 오토메이션`으로 이동했다. 편집 대상은 `오로라 패드` 악기의 **이번 사용** 볼륨이며 처음에는 점이 없었다. GUI의 `점 추가`로 첫 점을 만들고 수치 입력에서 **UI 1박/−6.00 dB**로 설정했다. 두 번째 점을 추가해 **UI 25박/0.00 dB**로 설정했다. 마지막 값은 `⌘Z`에서 −6.00 dB, `⇧⌘Z`에서 0.00 dB로 되돌아왔다. `⌘S` 저장 완료와 **r11**을 확인했다.
3. QA 앱을 `⌘Q`로 종료한 뒤 **새 프로세스**에서 같은 사본을 열었다. 자동화 편집기에 2개 점과 **1박/−6.00 dB, 25박/0.00 dB**, 적용 체크 상태가 보였다. 앨범 사운드의 마스터 헤드룸 편집기로 돌아가 **0.60 dB**도 확인했다. 두 번째 프로세스를 종료했다.

최종 QA manifest SHA-256은 `b487d2460cada07877048915d6026b285e13fa9895948d6ac970c40bf13bf4b8`, `musicRevision=11`이다. 저장된 `signal.nodes`의 `마스터 헤드룸` gain 배율은 **1.093453827297068 → 1.0715193052376064**였다. 첫 section use의 `instrument:A3483DFA-6E15-4FA9-933A-F14D5D7F9204` override에 enabled gain automation 2개가 저장됐다: 내부 beat **0/24**(UI의 1/25박), 배율 **0.5011872336272722/1**(−6/0 dB), linear. 원본과 최종 사본에서 프로젝트 ID·revision·편집 화면 상태·색 배열의 직렬화 순서·이 두 음악 편집을 제외하면 음악 콘텐츠는 동일했다. 색 항목의 순서만 바뀌었고 항목 집합은 같았다.

## 판정 경계

**R80-09의 GUI FX 수치 변경·Undo/Redo·저장·재열기와 GUI 자동화 점 생성·수치 변경·Undo/Redo·저장·재열기 일부는 PASS**다. 이 경로의 실제 PCM 변화, 물리 청취, 재생 중 편집, 오디오 take, stem 폴더 내보내기, AU 호환성은 이 실행에서 확인하지 않았다. 따라서 R80-09 전체와 R80-10, 0.80 출고 gate는 계속 열린다. 앞선 MCP 오프라인 렌더 증거는 [별도 범위](0.80-offline-daw-journey.md)이며 본 GUI 시험의 오디오 출력 증거로 전용하지 않는다.
