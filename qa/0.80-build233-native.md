# 0.80.0 build233 — 팔로우 컨트롤·native UI QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build233-native
STATUS: PARTIAL PASS — 실행한 native UI·MCP·패키지 검증 통과, 전 폭 × 전 팔로우 상태 조합과 물리 청취는 미검증
COVERAGE: signed packaged build233, 격리 QA 앱, 700/720/900/1440pt 실제 창, SONG/SECTION/PINNED following/off/suspended의 지정 조합, f0r h3r 재생, read-only circlr_context
FAILURES: 실행한 조합에서 재현된 UI·AX 결함 없음
UNVERIFIED: 4개 폭 × 3개 대상 × 3개 상태의 모든 조합, VoiceOver 실제 음성, 물리 청취·음질, Scarlett 재생/호환성, 장시간 재생·녹화 영상
VERIFIED_ENVIRONMENTS: macOS native, circlr 0.80.0/build233, com.circlr.build233nativeqa, 격리 Application Support
ARTIFACTS: qa/generated/r80-build233-native/ (Git-ignored)
```

2026-09-24 KST. `dist/써클러.app` 주 실행 파일 SHA-256은 **`880e6bcffc23ec8454e0c7df00ab574577caf912dd8567042eb1c6c4c51051b2`**다. Git-ignored `써클러 build233 네이티브 검증.app`으로 복사해 bundle ID를 `com.circlr.build233nativeqa`로 변경하고 문서 유형 등록을 제거한 뒤 ad-hoc 재서명했다. QA 실행 파일 SHA-256은 **`1b4fef82846e4dd257873d0cd8c0d17ed2c8d7165025e1b906986ada341398ff`**이다. 원본·QA 사본 모두 `codesign --verify --deep --strict`를 통과했다. 패키지 Resources **57/57개**, helper 실행 파일 **5/5개**가 byte-identical이었다. 번들 `f0r-h3r.circlr`의 7파일 tree SHA-256은 양쪽 **`569edbe47193907c0452cbb273cef8671a425967ea708e322b9678e11630fcde`**, `manifest.json` SHA-256은 **`a75ea9f2f3917f82184ba94c31e071905c0c6a1bfa2e90259ad554eaa5c794d7`**다. [bundle-integrity.json](generated/r80-build233-native/bundle-integrity.json)에 개별 해시를 기록했다. 복구·agent는 `~/Library/Application Support/circlr-build233native-qa`, 데모 사본은 `~/Library/Application Support/com.circlr.build233nativeqa/Demo Projects`에 격리했다. 원본 앱과 사용자 프로젝트는 열거나 변경하지 않았다.

실제 전체 창을 CUA로 **700·720·900·1440pt 너비, 공통 900pt 높이**로 조절했다. 700pt에서 SONG, SECTION, PINNED 각각 `following`과 `off`의 픽셀·AX를 확인했다. 예를 들어 [곡 켜짐](generated/r80-build233-native/width700-song-following.source.jpg)은 `곡 팔로우 끄기`/`켜짐`, [곡 꺼짐](generated/r80-build233-native/width700-song-off.source.jpg)은 `곡 팔로우 켜기`/`꺼짐`; [섹션 켜짐](generated/r80-build233-native/width700-section-following.source.jpg)은 `섹션 팔로우 끄기`/`켜짐`, [섹션 꺼짐](generated/r80-build233-native/width700-section-off.source.jpg)은 `섹션 팔로우 켜기`/`꺼짐`; [고정 켜짐](generated/r80-build233-native/width700-pinned-following.source.jpg)은 `고정 팔로우 끄기`/`켜짐`, [고정 꺼짐](generated/r80-build233-native/width700-pinned-off.source.jpg)은 `고정 팔로우 켜기`/`꺼짐`이다. 각각 같은 이름의 `.ax.txt`에 VoiceOver용 AX label/value가 있다. 700pt 화면에서 01–07 섹션, 재생·녹화·감상·서클 추가, 팔로우 버튼이 가려짐 없이 보였다.

재생 전 QA 앱 **내부의 출력 설정에서만** `Mac Studio 스피커`를 지정했고 `circlr.playback.outputDeviceUID=BuiltInSpeakerDevice`를 확인했다. 시작 시 앱은 시스템 기본값을 `Scarlett 6i6 USB`로 표시했다. 실제 재생 시 AX 시계가 00:02.6에서 01:06.6까지 진행했고 `마지막 재생 확인 장치: Mac Studio 스피커`가 표시됐다. 이는 출력 장치 연결과 앱 재생 상태의 관찰이며 **물리 청취나 실제 소리 품질의 판정은 아니다**. 읽기 전용 [종료 후 장치 catalog](generated/r80-build233-native/output-catalog-after.json)의 시스템 `defaultUID` SHA-256은 시작 때와 같은 `d4bc96d8fc82c68ec973b2af8dca79a5f5ceb89e121c740e29c661043c3e5eed`였다. 시스템 기본 설정과 Scarlett 장치는 조작하지 않았다.

720pt 재생 중 앨범 케이블을 선택한 [SONG 일시 중지](generated/r80-build233-native/width720-song-suspended.source.jpg)는 00:09.0에 `곡 팔로우 재개`/`일시 중지`를 AX와 픽셀로 표시했다. [재개](generated/r80-build233-native/width720-song-resumed.source.jpg) 후 00:16.3에 `곡 팔로우 끄기`/`켜짐`이 됐다. `현재 섹션 서클`을 명시적으로 택한 뒤 앨범 케이블을 선택한 [SECTION 일시 중지](generated/r80-build233-native/width720-section-suspended.source.jpg)는 00:25.1에 `섹션 팔로우 재개`/`일시 중지`를 보였다. [재개](generated/r80-build233-native/width720-section-resumed.source.jpg) 후 00:33.5에 `섹션 팔로우 끄기`/`켜짐`이 되고 Groove 섹션 내부 카메라로 이동했다. 이 케이스에서 선택한 것은 **앨범 레벨 케이블**이므로 재개 뒤 내부 캔버스에서 그 케이블은 보이지 않는다. 이를 내부 케이블 강조 유지 검증으로 확대하지 않는다.

Groove 섹션을 선택해 `선택한 서클 고정`으로 전환하고 내부 `클럽 킥 MIDI → 클럽 킥` 케이블을 선택한 [PINNED 일시 중지](generated/r80-build233-native/width720-pinned-suspended.source.jpg)는 01:00.5에 `고정 팔로우 재개`/`일시 중지`였다. [재개](generated/r80-build233-native/width720-pinned-resumed.source.jpg) 후 01:06.6에 `고정 팔로우 끄기`/`켜짐`으로 바뀌었고, AX의 케이블 `(selected)`와 픽셀의 민트색 선택선은 보였다. 900pt [정지 화면](generated/r80-build233-native/width900-song-following.source.jpg)과 1440pt 대표 화면에서도 SONG following, 재생·녹화·감상·서클 추가, 7섹션의 레이아웃과 AX 가시성을 확인했다. 이 둘의 SECTION/PINNED 및 suspended/off 조합, 700pt suspended는 실행하지 않아 미검증으로 남긴다. 녹화·감상·서클 추가는 **버튼 노출과 겹침**을 확인했으며 동작·산출물 검증은 아니다.

격리 앱의 `Agent/agent.sock`에 패키지 내 Python MCP 서버를 `--read-only`로 연결해 실제 `circlr_context`를 호출했다. [검증 요약](generated/r80-build233-native/mcp-context-check.json)의 tool `readOnlyHint=true`, `ok=true`, `project.id=ABC61552-5B23-45B4-82F0-7511B2D00DAA`, `project.revision=1`, `project.layoutRevision=0`이다. [응답 원본](generated/r80-build233-native/mcp-context-response.json)은 `project`, `activeArrangement`, `selection`, `compositions`, `arrangements`, `tracks`, `runtime`, `status`를 반환했다. 재귀 키 검사에서 `path`, `projectPath`, `mediaPath`, `asset(s)`, `pattern(s)`, `note(s)`, `sourceURL`, `fileURL`은 없고, 응답 문자열에 `/Users/`와 `file://`도 없었다. 이 평가는 응답 필드 노출 범위에 한정한다.

README 후보는 재생 QA 앱을 종료한 뒤 **새 세션에서 번들 데모를 다시 연 정지 화면**이다. CUA 전체 창 JPEG 원본을 저장하고 `sips`로 크롭·합성·크기 변경 없이 PNG로 변환했다. 두 이미지의 AX·픽셀에 01–07 섹션, 00:00.0, `데모곡 불러오기 완료`, Play·녹화·감상·서클 추가가 보인다. 가로는 `LEN P27h-10`에서 **1440×900pt / 2880×1800px**, 세로는 `2460G4`에서 **720×900pt / 1440×1800px**다. 디스플레이 설정은 변경하지 않았다.

| 전체 창 | PNG SHA-256 | JPEG 원본 SHA-256 |
| --- | --- | --- |
| [landscape-1440x900.png](generated/r80-build233-native/landscape-1440x900.png) | `fd77be3a6b97c3d1cb358d161dc53742f1ab63e56dcbf4449d51e5e3b0ca4a43` | `ce3c721a17aeff2f4b3dcfc32c88d6ba8352bb74fe388df494f680ea12092a0c` |
| [portrait-720x900.png](generated/r80-build233-native/portrait-720x900.png) | `961ba425f989d762e60225e03715b66e970a9a4733ea6918ae3af09f9d25540e` | `144c75d33e809ef175d6118cc62a42c5a0bcb5996ea434140d42c5d5c8e5802e` |

최종 QA 앱은 `⌘Q → 변경 버리기`로 종료했다. [clean-exit.json](generated/r80-build233-native/clean-exit.json)에서 격리 앱·helper PID **0**, 전용 `Agent/agent.sock` **없음**을 확인했다. source·QA deep strict 서명은 통과했다. 저장된 증거 파일의 해시는 [SHA256SUMS.txt](generated/r80-build233-native/SHA256SUMS.txt)에 둔다.
