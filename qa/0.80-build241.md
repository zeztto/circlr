# 0.80.0 build241 — 최종 키보드·대형 라이브러리 수정 후보

```text
TEMPLATE_VERSION: qa-response-v1
STATUS: AUTOMATED_PASS · KEYBOARD_NATIVE_PASS · PERFORMANCE_PENDING · RELEASE_BLOCKED
SOURCE: release/0.80.0 · CFBundleVersion 241
```

[build240 native QA](0.80-build240-media-menu-native.md)는 폴더·형식 필터의 키보드/포인터 조작과 700×600pt 구성을 확인했으나, 라이브러리 밖 캔버스 `⌥Space`가 곡 재생을 시작하는 P2를 재현했다. build241은 [Space 단축키 계약](0.80-playback-space-shortcut.md)에 따라 일반 Space만 재생 명령으로 해석하고 수정키·활성 텍스트 입력은 통과시킨다. 감상 모드의 Fn+Space도 local monitor와 캔버스 양쪽에서 macOS로 전달한다. 합성 키 테스트 뒤 독립 읽기 전용 리뷰는 새 P1/P2를 찾지 못했다. [최종 앱 격리 native QA](0.80-build241-native.md)는 필터의 키보드/포인터, overlay 밖 `⌥Space` 무재생 및 일반 Space 시작·취소를 확인했다. CUA가 Fn 키를 보낼 수 없고 실제 한글 IME 조합도 미실행이라 두 항목은 미검증이다. build241 정상 `⌘Q` 뒤 정확한 PID와 QA socket inode/리스너 0을 확인했다. build240의 과거 앱 종료 보고 오류와 후속 PID 정리는 [별도 기록](0.80-build240-media-menu-native.md)에 남긴다.

대형 미디어 목록에서는 build240의 Tab마다 50,000개 파일에 대해 포커스 문자열 최대 100,000개를 만들던 경로와, 선택 검증의 전체 목록 탐색을 제거했다. 결과 확정 시 ID→entry/표시 순서 인덱스를 만들고 Tab에서는 최대 64개 선택 ID만 검증한다. 실제 가져오기의 원본 파일 재확인과 목록 순서 검사는 유지했다. 50,000개 결과를 포함한 집중 테스트와 독립 코드 재검토에서 새 P1/P2는 없었다. 실제 대형 폴더의 화면 반응 속도는 native 계측 전까지 미확인이다.

최종 동일 소스 `./scripts/swift-local.sh test --scratch-path .build/build241-integration`은 **1,336개 실행·15 skip·실패 0**이었다. 로그 `/tmp/circlr-build241-swift.log` SHA-256 `32bbec8d8512e9b28ddb762820127213c6ab6996fc7bec5b0359c3a5a658dcd8`. Python MCP 서버 **47/47**, 프로젝트 MCP **39/39**, agent kit+model trace **23/23**, QA Python 문법, `git diff --check`가 통과했다.

`./scripts/build-app.sh`의 `dist/써클러.app`은 **0.80.0/build241**이다. arm64 본체·7개 arm64 helper와 `codesign --verify --deep --strict`가 통과했다. 본체 SHA-256 `eebe74c188d588816de98dae44dcb19364039d452746e2ad1892683a0e89f98c`, 패키지 로그 `/tmp/circlr-build241-package.log` SHA-256 `cfdaaff5cbdf16e6ada7febedda95cd80b7a3122c63a2565844cbe9d763ff2a4`. Demos **27/27**, Codex kit **26/26**(`__pycache__` 제외), `LICENSE`·`THIRD_PARTY_NOTICES.md` **2/2**가 소스와 byte-identical이다. `Resources/Assets.car`만 패키징 뒤 생성 상태에서 원본으로 복구했고 서명된 앱은 변경하지 않았다.

[외부 Codex→MCP 모델 QA](0.80-build239-model-native.md), [빈 곡 DAW 여정](0.80-build239-final-daw-native.md), [오디오 입력 지속성](0.80-build239-input-persistence-native.md)은 build239의 기능별 실제 앱 근거다. build241의 화면·장시간 영상·소켓 종료·물리 청취를 대신하지 않는다. [0.80 출고 gate](0.80-release-gates.md)의 미완료 항목을 PASS로 바꾸지 않고, 최종 tag/GitHub Release는 열어 두었다.
