# 0.80.0 build220 — Documents 경로 native QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build220-native
STATUS: failed (격리 QA .circlr의 첫 ⌘S가 30초 이상 정지)
COVERAGE: 서명 build220의 manifest.json 선택 열기, 첫 저장 시도, native stack, 패키지·원본·QA 파일·기본 출력 보존
FAILURES: ⌘S 중 ProjectStore.copyAssetBytes의 source Darwin.open 호출이 30초 이상 반환하지 않음; 저장 완료 없음; 빈 staging 잔류
BLOCKERS: 저장 정지로 .circlr 재열기, 일반 폴더 notes.txt 덮어쓰기 거부, 최종 창 촬영 미실시
RISK_CALLS: 이 Documents 경로 시험은 출고 차단. 커널 open 정지의 근본 원인은 미확정. 재생·청취·Scarlett 호환성 판단 없음
VERIFIED_ENVIRONMENTS: macOS 26.5.1, circlr 0.80.0/build220, 격리 bundle ID
ARTIFACTS: qa/generated/r80-build220-native/ (Git 제외)
DELIVERABLE: 선택 AX, 정지 sample, 전후 hash, 프로세스 정리; 다음 owner development lead
```

2026-09-24 KST. `dist/써클러.app`의 `CFBundleVersion` **220**과 패키징 프로세스 종료를 확인한 뒤 시작했다. 배포 앱을 격리 bundle ID로 복사·ad-hoc 재서명했다. 동봉 demo는 UUID만 바꿔 QA `.circlr` 사본과, 유효한 manifest 및 별도 `notes.txt`를 가진 일반 폴더 사본을 준비했다. 원본 demo·사용자 프로젝트에는 쓰지 않았다. 배포 실행 파일 SHA-256은 **`424c6229a17faed5ca4eb9534b823bc1bfa3ad65c9bd23bb964d941cf03488c2`**, `Assets.car`는 **`32870355f1eca383ddbd0c937bf7aabc7e5f887eadcd4c9cf887e0889ec842d6`**다. Resources **57/57개** 해시는 시작·종료 시 동일했고 배포/격리 앱의 strict deep 서명 검사가 통과했다(`baseline.json`, `final-check.json`).

Open 패널에서 QA `.circlr` 사본의 **`manifest.json` 파일**을 `⌘⇧G`로 직접 지정했다. AX는 해당 파일 행이 `(selected)`이고 URL이 QA 사본 내부 파일임을 표시한다(`manifest-selected.ax.txt`). Open 후 앱은 `f0r h3r 열기 완료`, 버전 0.80.0/build220을 보였다. 이것은 manifest 선택 열기에 한정한 PASS다.

첫 실제 **`⌘S`** 입력 시각은 `2026-09-24T00:24:48.017Z` (`save-start.txt`)다. CUA 호출이 약 16.7초 뒤 timeout을 반환했고, 30초를 넘긴 시점에도 저장 완료가 없었다. `save-hang-sample.txt`의 main thread **2,670/2,670 samples**는 `AppStore.save` → `ProjectStore.saveSessionReportingCleanup` → `ProjectStore.saveReportingCleanup` → `ProjectStore.copyAssetBytes` (`ProjectStore.swift:152`) → `_fcntl_overlay_open` → kernel `open` → `__open`에 머물렀다. source 파일을 여는 native 호출에서 반환하지 않았다는 관찰이며, OS·TCC·파일시스템 중 무엇이 근본 원인인지는 확정하지 않았다. `.circlr-save-*` staging에는 빈 `media/`만 남아 **디스크 사용 0 KB, 파일 0개**였다. 해당 staging은 ignored QA 증거로 남겼다.

QA 프로젝트의 미디어 **6/6개는 regular file**이고 source와 staging은 같은 볼륨이었다. 종료 전후 SHA-256 대조에서 동봉 demo **7/7**, QA `.circlr` **7/7**, 일반 폴더 **8/8** 파일이 모두 동일했다. `notes.txt`와 일반 폴더 manifest의 바이트도 그대로다(`final-check.json`). 일반 폴더의 실제 GUI 덮어쓰기 거부와 저장 후 재열기는 시도하지 않았으므로 PASS가 아니다.

30초를 넘긴 뒤 정확히 확인된 격리 QA 앱 PID에만 SIGTERM을 보냈고 앱은 종료됐다. 최종 앱/helper PID는 **0개**, 시스템 기본 출력은 읽기 전용 조회에서 전후 동일했다. 출력 장치를 바꾸거나 재생하지 않았다. 저장 정지로 1440×900/720×900 최종 화면은 촬영하지 않았고 build220 이미지 파일도 만들지 않았다. 이 보고서에는 앱/프로젝트 절대경로 및 장치 UID를 기록하지 않았다.
