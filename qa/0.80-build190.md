# 0.80 build190 · 출력 궤도 위성 포커싱 재검증

상태: **R80-01의 재현 시나리오 통과 · 0.80 출고 gate는 계속 열림** · 2026-09-23. build189에서 실패한 동일한 `f0r h3r` 작업 사본·약 1659×1387 창의 키보드 동선을 0.80.0/build190 격리 앱에서 다시 실행했다.

- `./scripts/build-app.sh`로 앱과 다섯 출력/효과/helper를 패키징했다. 원본·QA 앱의 `codesign --verify --deep --strict` 통과, QA 앱 AX의 `버전 0.80.0 · 빌드 190` 확인. QA 앱 tree SHA-256은 `1944f77126152d1bffd61e78584579465c5ab5ad38d0670f34027f2f799dbdbe`, 사본 프로젝트 tree SHA-256은 `8313451f389deb64a8f3638a2b4a6970683b6b65decff684fdbd87460a13aee8`이다. 로컬 package 기록은 `qa/generated/outward-orbit/build190/package.json`이고, 빌드 로그 SHA-256은 `cbb102991f6909bcb367e5032e3aee6ccff302e55b4f470cdd569d52e55f7f2f`이다.
- 앨범 사운드의 중심 서클을 선택하고 캔버스에 키보드 초점을 둔 뒤 `Left`로 곡, `Right`로 앨범 사운드에 복귀했다. build189에서 중심이 y≈900으로 밀리며 잘리던 출력·버스·효과 위성까지 화면 안에 보였다. AX는 앨범 사운드 선택과 트랙/효과/마스터 자식 항목을 표시했다. 시작/복귀 상태 모두 화면에서 하단 `출력` 서클이 보였고, 육안상 겹침 또는 부분 잘림은 없었다.
- 실제 `f0r h3r` manifest의 18개 앨범 사운드 직계 위성을 사용하는 `PlaybackFramingTests` **13/13 통과**. 먼 수동 배치 outlier를 제외하고 가시 클러스터를 맞추는 계약이다. 이는 이 데모와 이 창 크기의 keyboard path 통과이며 모든 줌·고밀도/가로·세로, 표시 FPS 또는 재생 중 팔로우의 증거가 아니다.
- 최종 소스의 전체 Swift 테스트 **1072개 실행·11개 skip·실패0**. 로그 `qa/generated/r80-build190-final-complete-suite.log` SHA-256은 `73d863aeee1d1e34403c33f72eed6144240a52296799b0aa3d0107d279524754`이며, 이 안에 독립 `CirclrCodexTests` **30/30**이 포함된다. MCP Python **39/39**, agent kit **9/9**, `git diff --check`와 문서 상대 링크 검사를 통과했다. 독립 코드·보안 재검토에서 현재 변경의 추가 P1/P2를 찾지 못했다. 이 코어는 앱·계정·모델 호출에 연결되지 않았다.
- 물리 청취·Scarlett/내장 출력 재시험·녹화는 이 UI QA에서 실행하지 않았다. QA 사본만 사용하고 시스템 기본 출력을 바꾸지 않았으며 앱을 종료했다. 정식 `v0.80.0` Release는 아직 만들지 않는다.
