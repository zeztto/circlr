# 0.80 build197 · QA 저장 폴더 격리 확인

상태: **수정 확인 / UI 개선 검증은 build198로 이월** · 2026-09-24.

`./scripts/build-app.sh`로 0.80.0/build197 arm64 앱과 helper를 패키징하고 strict deep ad-hoc 서명을 확인했다. 빌드 로그 `qa/generated/r80-build197-build.log` SHA-256은 `3424cee7f92f3cb59983bc1bc5d5f31d7ff62ff5daccf3d6f7dce31ecfd6ba25`다. 같은 소스의 `com.circlr.portfocusqa` 사본은 [준비 도구](prepare-port-focus-qa.py)로 만들었고 QA package 기록은 `qa/generated/r80-build197-package.json`에 남겼다.

실제 격리 앱은 build196에서 나타난 기본 폴더 복구 창 없이 시작 화면을 열었다. `f0r h3r` 데모를 불러와 **버전 0.80.0 · 빌드 197**과 케이블 AX 버튼을 확인했다. 곡 궤도의 케이블을 선택하면 OUT/IN 끝점, 편집·해제 버튼이 표시됐고 **편집**을 누르자 해당 연결의 편집 화면과 대상 검색으로 이동했다. 실행 후 앱 전용 socket·복구 파일은 `~/Library/Application Support/circlr-portfocus-qa/`에 생겼고, 기본 `circlr/recovery.json`의 mtime은 QA 전과 같은 00:37:28이었다. QA 데모의 미저장 변경은 앱 종료 때 **변경 버리기**로 정리했고 build197 앱·출력 worker 프로세스는 남지 않았다.

창은 약 702pt였고 RootView의 최소 폭은 700pt다. 당시 코드의 compact 조건(`panel width <560pt`)은 이 창에서 실행되지 않아 케이블 도구가 여전히 한 줄이었다. 따라서 두 줄 배치의 실제 화면 검증은 **build198**에서 해야 한다. 사용자 앱 데이터의 전체 무변경, 실제 340/375pt 창, 긴 라벨·모든 방향·키보드 전체 매트릭스를 이 결과로 주장하지 않는다.
