# 0.80.0 build242 — 로컬 배포 후보 검증

```text
TEMPLATE_VERSION: qa-response-v1
STATUS: LOCAL_ARCHIVE_PASS · REMOTE_RELEASE_PENDING · PRODUCT_PERFORMANCE_FAIL
PACKAGE: dist/circlr-0.80.0-build242-macos-arm64.zip
```

`dist/써클러.app` 0.80.0/build242를 `ditto -c -k --sequesterRsrc --keepParent`로 26MiB ZIP에 담았다. ZIP SHA-256은 `476803f0d95c8bdb3be986fa2a0a749c4ad266b58e4a44ce1db7dfac9c6a9b8c`; 같은 `dist/SHA256SUMS`의 `shasum -a 256 -c`는 PASS다. 파일을 ignored `qa/generated/r80-build242-package-unpack/`에 풀어 원본 앱과 **67개 파일의 상대 경로·SHA-256이 전부 동일**함을 확인했다. 압축 해제본의 `CFBundleShortVersionString=0.80.0`, `CFBundleVersion=242`, main exe SHA-256 `6e7f28166c30d9b2e638aa5d13cb94874dc58c6aca3f3f641c7b9d194bd8fa43`, `codesign --verify --deep --strict`도 PASS다. arm64 본체·7 helper, Demos 27/27, Codex kit 26/26 및 권리 파일 2/2의 소스 byte 일치는 [통합 검증](0.80-build242.md)에 있다.

압축 해제본의 실제 main 실행 파일을 `CFFIXED_USER_HOME`으로 고정한 ignored QA home에서 직접 실행했다. 고유 PID **44929**는 2초 뒤 살아 있었고 CGWindow의 동일 PID·layer 0 창 **1개**를 확인했다. 제품 Agent socket의 존재 여부는 실행 전후 동일했다. 정확한 PID에 SIGTERM을 보내 종료했으며, 이것은 **앱 초기 창 시작** 확인이다. 프로젝트 제작·MCP socket 준비·정상 `⌘Q` 종료의 증거로 확대하지 않는다. 별도 격리 build242 앱의 실제 재생·MP4와 정상 종료는 [장시간 native QA](0.80-build242-performance.md)에 기록했다. 첫 10초 smoke는 PID 생존과 격리 지원 폴더 생성까지 확인했으나 Agent socket이 없어 초기 창 확인에는 쓰지 않았다. 자세한 로컬 로그는 ignored `qa/generated/r80-build242-package-smoke/`에 있다.

이 ZIP은 **검토 후보**다. build242 MP4의 한 프레임 간격이 사전 최대 66.67ms를 넘고, 앱별 compositor FPS·물리 청취는 미확인이다. 승인 commit/tag·GitHub Release·원격 재다운로드 SHA 대조는 아직 수행하지 않았다. [출고 gate](0.80-release-gates.md)가 닫힐 때까지 정식 배포 자산으로 안내하지 않는다.
