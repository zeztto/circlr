# 0.80.0 build239 — 앱 전용 입력 take 지속성 native QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build239-input-persistence
STATUS: passed
COVERAGE: CalDigit 앱 전용 선택 → native CAF 녹음 → .circlr 저장 → 앱 완전 종료·재실행 → 패키지 재열기 → WAV export → 장치·원본 불변
FAILURES: none in exercised path
BLOCKERS: none; 마이크 TCC 창은 나타나지 않았음
RISK_CALLS: 매우 낮은 입력 레벨로 실제 음악/청취 품질은 미검증; build240 이후 패키지에 대한 결과로 확장 금지
VERIFIED_ENVIRONMENTS: macOS, dist/써클러.app 0.80.0/build239에서 복제한 ad-hoc signed com.circlr.build239inputqa
ARTIFACTS: qa/generated/r80-build239-input-persistence/ (ignored native CAF, .circlr, WAV, 분석·전후 오디오 기록)
DELIVERABLE: build239의 선택 입력 take 저장·재열기·export 지속성 gate PASS
```

원본 `dist/써클러.app` build239의 실행 파일 SHA-256은 `f87136cef6f5453bcfd7aa034cb75bf4d49873efea267a6aad8d0b166ace9314`였다. [추적 준비 스크립트](prepare-0.80-input-persistence.py)로 `.build/build239-input-persistence/써클러 build239 입력 지속성 검증.app`을 만들고 고유 bundle ID를 적용했다. 문서·URL 형식 등록을 제거하고 ad-hoc 서명했으며, 원본·QA 앱 모두 `codesign --verify --deep --strict` 통과, Resources 57개와 helper 7개 byte 일치, 원본 실행 파일·동봉 데모 manifest SHA는 전후 불변이었다. [복제 기록](generated/r80-build239-input-persistence/bundle-integrity.json), [최종 환경 기록](generated/r80-build239-input-persistence/final-integrity.json).

| Gate | Native 조작과 관찰 | 판정 |
| --- | --- | --- |
| 앱 전용 입력 선택 | QA 앱에서 120 BPM/4⁄4/4마디/건반 1트랙 빈 곡을 만들고 `섹션 1 · 건반`을 선택했다. 녹음 메뉴에 CalDigit Thunderbolt 3 Audio와 Scarlett 6i6 USB가 모두 표시됐다. CalDigit를 고른 뒤 재연 메뉴의 checkmark, QA bundle preference의 CalDigit UID를 확인했다. 시스템 기본 입력·출력·시스템 출력은 모두 Scarlett였다. | **PASS.** 시스템 기본 장치 변경 없음. |
| 실제 take 녹음 | UI가 `마이크 접근 확인 중`→`입력 장치 연결 중`→`오디오 녹음 중`→`녹음 파일 마무리 중`→`오디오 take 저장 완료`로 진행했다. 계획의 2–4초보다 UI 관찰 시간이 길어 7.285초를 녹음했으나 8초 섹션 안에서 정상 완료됐다. macOS TCC 창은 나타나지 않았다. | **PASS, 데이터 경로 한정.** [CAF 정보](generated/r80-build239-input-persistence/recorded-take.afinfo.txt), [PCM 분석](generated/r80-build239-input-persistence/recorded-take-analysis.json), [CAF](generated/r80-build239-input-persistence/recorded-take.caf): 48 kHz, stereo Float32, 349,696 frames, nonzero 629,234 samples, peak 0.00057983, RMS 0.000121. 매우 낮은 입력 레벨이라 청취 품질은 판정하지 않는다. |
| 저장·완전 종료·재열기 | `⇧⌘S`로 새 [QA 패키지](generated/r80-build239-input-persistence/qa-input-take.circlr/)에 저장하고 `저장 완료`를 확인했다. 패키지 media CAF와 녹음 CAF의 SHA-256이 `1daf0a1056076b158df260f68d0d824d62015b30aff314b56024371119bf8efb`로 같았다. manifest의 1개 asset·1개 take, 활성 `laneOverrides`의 clip assetID와 `graphEdits`의 오디오 노드 연결도 일치했다. `⌘Q` 후 QA process/helper/socket 0을 확인하고 같은 앱을 재실행했다. `⌘O`에서 처음 부모 폴더를 선택해 안내 오류가 났지만, 정확한 `.circlr` 패키지를 선택하자 곡 열기 완료와 오디오 서클 복귀를 확인했다. | **PASS.** [저장 참조·해시 분석](generated/r80-build239-input-persistence/save-integrity.json). 첫 Open 오류는 선택 대상 오류였으며 저장/재열기 실패가 아니다. |
| 재열기 뒤 WAV | 별도 MIDI 노트·리듬을 넣지 않은 빈 곡에서 `⌘E` 후 UI가 `WAV 내보내는 중`→`내보내기 완료 · qa-input-take-after-reopen.wav`로 진행했다. [WAV](generated/r80-build239-input-persistence/qa-input-take-after-reopen.wav)는 48 kHz, stereo 24-bit, 480,000 frames/10초였다. [분석](generated/r80-build239-input-persistence/wav-after-reopen-analysis.json)에서 nonzero 629,234 samples, 마지막 nonzero는 CAF의 마지막 frame 349,695, 나머지 export tail은 무음이다. CAF 대비 출력의 nonzero mask는 699,392개 샘플 전체에서 같았고, 기존 건반 트랙 gain 0.6을 적용한 값과 최대 오차 `4.77×10⁻⁸`이었다. | **PASS.** [CAF↔WAV 상관 분석](generated/r80-build239-input-persistence/caf-to-wav-correlation.json)으로 export 신호가 저장된 take에서 왔음을 확인했다. |
| 정리 | QA 앱을 종료하고 QA bundle process/helper/socket 0, QA 앱 strict 서명, 원본 strict 서명·실행 파일·동봉 데모 manifest SHA 불변을 확인했다. `system_profiler SPAudioDataType` [전](generated/r80-build239-input-persistence/audio-before.txt)·[후](generated/r80-build239-input-persistence/audio-after.txt)는 byte-exact였다. | **PASS.** 사용자 설치 앱·프로젝트와 macOS 기본 장치를 조작하지 않았다. |

화면 상태는 CUA로 **해당 QA 앱 창**의 accessibility tree와 앱 창 screenshot에서 확인했다. 전체 화면 `screencapture`는 앱 픽셀 증거에 사용하지 않았다. 캡처는 이 QA 세션에만 표시되고 별도 screenshot 파일로 보존되지 않았으므로 링크된 파일 증거는 CAF·프로젝트·WAV·장치/서명 분석에 한정된다. 물리 스피커 청취, 새로운 macOS 계정의 첫 TCC 허용 동작, build240의 동일 경로는 검증하지 않았다.
