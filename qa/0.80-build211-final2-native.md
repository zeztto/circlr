# 0.80.0 build211 최종 재패키지 — 실제 앱 38초 영상 QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build211-final2-native
STATUS: pass (아래 명시한 35~40초 녹화 조건)
COVERAGE: 최종 실행 파일 SHA 78ef7cae…0865인 서명 앱의 f0r h3r 수동 종료 MP4, UI/독립 pixel decode count, PTS, AAC PCM, 앱 전용 내장 출력, 실제 가로·세로 창, 종료 정리
FAILURES: 이번 조건에서 관측되지 않음
BLOCKERS: none
RISK_CALLS: 단일 38초 수동 종료는 자연 종료·루프·10분·1080p·다른 장치 재생을 포괄하지 않음. 물리 청취, 별도 플레이어 완주, 지각적 AV 동기는 미검증
VERIFIED_ENVIRONMENTS: macOS 26.5.1, 격리 com.circlr.build211final2qa, 0.80.0/211, 앱 전용 BuiltInSpeakerDevice
ARTIFACTS: qa/generated/r80-build211-final2-native/ (Git 제외)
DELIVERABLE: 최종 보안 수정 재패키지에서 UI 1138 = 실제 디코드 1138, 2프레임 초과 PTS gap 0, nonzero AAC PCM을 재확인. 다음 owner: development lead
```

2026-09-24 KST. 최종 보안 수정 뒤 재패키지 완료 신호를 받고 시작했다. 이 검사의 대상은 이전 `qa/0.80-build211-native-movie.md`의 실행 파일 `c58a4d80…`가 아닌, **최종** `.build/release-0.80-worktree/dist/써클러.app` 실행 파일 SHA `78ef7cae624439f34076abde3a2d11ac340b900df06477d1dcc6d29c84bb0865`다. 앱은 0.80.0/build211이고 원본 `codesign --verify --deep --strict`가 통과했다. 원본을 임시 디렉터리의 `써클러 build211 최종2 검증.app`으로 복사해 고유 bundle ID `com.circlr.build211final2qa`, 전용 저장소 `~/Library/Application Support/circlr-build211final2-qa`로 격리하고 ad-hoc 재서명했다. 사용자·기존 QA 앱의 bundle/storage는 사용하지 않았다.

| 패키지 | SHA-256 |
| --- | --- |
| 원본 앱 tree (검사 전·후 동일) | `2d1ff05b440e2a104e3d2cae03060afe3bc5d244d3b35aed782ba8e597b5475c` |
| 원본 실행 파일 (검사 전·후 동일) | `78ef7cae624439f34076abde3a2d11ac340b900df06477d1dcc6d29c84bb0865` |
| 격리 QA 앱 tree (재서명 직후) | `d703e1bf8cf20b4d24df64c6347b05e8347ac69638e06c0477dc90993391b115` |
| 격리 QA 실행 파일 | `8a6d83bb243c2849af4e1f7d8d46086fcf062de55a76c6a232ed0e8e38a2bb14` |

## 앱 출력과 독립 영상 검사

번들 `f0r h3r`을 열어 앱 설정에서만 `Mac Studio 스피커`(`BuiltInSpeakerDevice`)를 선택했다. 격리 MCP snapshot의 build/bundle은 `211`/`com.circlr.build211final2qa`; 재생 중 `outputSelectionKind=deviceUID`, `actualOutputDeviceName=Mac Studio 스피커`, `phase=ready`, `transport.didStart=true`, 진행하는 playback clock을 확인했다. 출력 준비 trace의 `queueStart` 완료는 선택된 장치의 약 0.15초 지점이다. `before.json`의 출력은 `idle`, `attempts=0`이다. 이번 증거에는 기본 Scarlett 장치로 실제 재생을 시작하거나 timeout/재시도를 겪은 기록이 없으므로 그런 실패를 주장하지 않는다. 재생 Stop 후에는 `phase=idle`, `playing=false`였다. 시스템 기본 출력 catalog의 UID는 검사 전·재생 중·저장 시·종료 전·앱 종료 후 모두 `AppleUSBAudioEngine:Focusrite:Scarlett 6i6 USB:10000B65:1,2`(`Scarlett 6i6 USB`)다. 시스템 기본 출력 설정을 바꾸지 않았다.

앱 Save 패널로 `qa/generated/r80-build211-final2-native/build211-final2-38s.mp4`를 지정하고 약 38초 뒤 ⇧⌘R로 녹화를 수동 종료했다. UI는 **`영상 저장 완료 · 1138프레임 · build211-final2-38s.mp4`**를 표시했다. 해당 실제 창 PNG `final2-save-notice-window.png` SHA-256은 `f07f0223b241d55ad0488b0eae142b3bcf6ca66446d3fda7db7951d0b2d7c48a`다. MP4는 **6,271,162 bytes**, SHA-256 **`0138ec69a9e150c081592062c0e9dacbe267e10b1997450cfcdbb88c3ec762a0`**다.

별도 AVFoundation decoder(`qa/inspect-movie-timing.swift`에서 컴파일)는 영상 1,138프레임을 **모두 실제 픽셀로 해독**했다. 이번 **37.980초 MP4는 UI 1,138 = 독립 디코드 1,138**로 일치한다. build210의 비슷한 37.299초 파일에서 관측한 UI 1,118 / 디코드 1,117 불일치는 이 최종 패키지의 동일 데모·수동 종료 조건에서 재현되지 않았다.

| 영상/시계 항목 | 독립 검사 값 |
| --- | --- |
| 영상 크기·길이·평균 | 1440×800 H.264, 37.980초, 29.963 fps |
| 첫/마지막 video PTS | 0 / 37.946667초; 파일 끝까지 33.333ms |
| 프레임 간격 p50/p95/p99·최대 | 33.33 / 38.33 / 45.00 / 60.00ms |
| 2프레임(66.67ms) 초과 | **0개**; 최대 60ms는 31.658초 및 34.560초 다음 간격 |
| 중복 PTS / 인접 픽셀 완전 동일 | 0 / 0 |
| 첫 audio/video PTS 차 | 0ms |

별도 프레임 추출기의 시작 **0.085초**, 중간 **18.480초**, 끝쪽 **36.778초** PNG에서 곡 궤도·서클 진행과 DJ Intro→Groove 섹션 전환을 눈으로 확인했다. 전체 인접 영상 픽셀 해시는 모두 달랐다. 최대 60ms PTS 간격은 파일의 계측치이며 시청 중 끊김이나 청감 동기 실패를 확정하는 값이 아니다. 이전 build211 재패키지 전 QA의 68.33ms 단일 간격은 이 파일에서는 재현되지 않았다.

`afinfo`는 stereo 48kHz AAC, 유효 오디오 **1,823,027 PCM frames**를 보고했다. `qa/inspect-movie-audio.py`가 `afconvert`로 별도 해독한 PCM 길이는 **37.979729초**, peak **0.732178**, RMS **0.098488**이었다. 1초 창의 시작 0초·중간 18.99초·끝쪽 34.182초 RMS는 **0.073679 / 0.108516 / 0.113516**으로 모두 nonzero다. 첫 A/V PTS는 모두 0이다. 수치상 오디오의 존재와 길이를 검증한 것이며 물리 스피커 청취나 중간·끝의 지각적 AV 동기를 검증한 것은 아니다.

## 최종 build211 실제 화면과 정리

`f0r h3r` 7개 섹션 곡 궤도 재생 중 최종 build211 격리 앱의 창 전체를 가로 1440×900, 세로 720×900으로 각각 촬영했다. CUA로 실제 앱 창 크기를 바꾸고 window capture에서 창 밖의 shadow만 잘랐다. 합성·구버전 화면 재사용은 없다. 두 PNG 모두 눈으로 내용과 창 전체 구성을 확인했다.

| PNG | 촬영 상태 | SHA-256 |
| --- | --- | --- |
| `final2-landscape-1440x900.png` | 가로 창 재생 약 00:15.7 | `daeb95edefa5193558172266976534625d8a3ad43d022b13c9bd931f7cebccfc` |
| `final2-portrait-720x900.png` | 세로 창 재생 00:08.1 | `bbe878e33320e704d0a024e6228ee1dbf180eb881f98a7bdad9a816b82d5d22a` |

재생 Stop 후 격리 앱 종료 대화상자에서 데모 세션의 저장하지 않은 변경만 버렸다. 종료 후 검사에서 해당 QA 앱/helper 프로세스는 **0개**이고 격리 QA 앱의 strict deep 서명은 여전히 통과한다. 원본 앱 tree·실행 파일 SHA는 복사 전과 동일하다. 격리 저장소에는 `Agent/selected.json`과 `recovery-v2.lock`만 남았다. 물리 청취는 수행하지 않았다.

근거: `qa/generated/r80-build211-final2-native/`의 `package.json`, `before.json`, `playing.json`, `movie-saved.json`, `final.json`, `movie-timing.json`, `movie-audio.json`, `afinfo.txt`, `video-codec-fourcc.txt`(`avc1`), `verify-final.json`, `frame-start.png`, `frame-middle.png`, `frame-end.png`, 위 MP4·실제 창 PNG. 이 검사는 최종 패키지의 한 번의 35~40초 수동 종료 시나리오에 한정된다.
