# 0.80.0 build218 — Open·저장 native QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build218-native
STATUS: failed (QA .circlr 저장 중 앱 응답 정지)
COVERAGE: 최종 서명 build218 격리 앱의 manifest.json 선택 열기, 저장 시도, 정지 stack, 패키지·곡 파일·출력 보존
FAILURES: ⌘S 후 FileManager.copyItem → clonefileat에서 30초 이상 진행 없음; 저장 완료 상태 없음; 0-byte staging 폴더 잔류
BLOCKERS: 앱 저장 정지로 .circlr 폴더 재열기와 일반 폴더 notes.txt 덮어쓰기 거부의 native 검증 미실시
RISK_CALLS: build218 출고 차단. 저장 원인은 native 호출 경계까지만 확인했으며 OS·파일시스템 근본 원인은 미확정. 재생/청취/Scarlett 호환성 판단 없음
VERIFIED_ENVIRONMENTS: macOS 26.5.1, circlr 0.80.0/build218, 격리 com.circlr.build218nativeqa
ARTIFACTS: qa/generated/r80-build218-native/ (Git 제외)
DELIVERABLE: 열기 AX, 정지 sample·파일/볼륨 inventory·전후 hash·프로세스 정리; 다음 owner development lead
```

2026-09-24 KST. `dist/써클러.app`의 `CFBundleVersion` **218**과 패키징 프로세스 종료를 확인한 뒤 시작했다. 배포 앱을 격리 bundle ID로 복사해 ad-hoc 재서명했다. 동봉 데모는 UUID만 바꿔 QA `.circlr` 사본과, 유효한 manifest에 별도 `notes.txt`가 있는 일반 폴더 사본을 준비했다. 원본 demo·사용자 프로젝트에는 쓰지 않았다. 배포 실행 파일 SHA-256은 **`881493bc005b1926cc4e1148e71406f449dd275392e184bd695dfbab580e213b`**, `Assets.car`는 **`fbbf9254e872dab7593fe378df2fe9b3cda2bf9b639cf1da441a2983ab87fb5a`**, 동봉 demo manifest는 **`a75ea9f2f3917f82184ba94c31e071905c0c6a1bfa2e90259ad554eaa5c794d7`**다. 번들 Resources **57/57개** 해시는 시작·종료 시 동일했고 양쪽 앱의 strict deep 서명 검사가 통과했다(`baseline.json`, `final-check.json`).

Open 패널에서 QA `.circlr` 사본의 **`manifest.json` 파일**을 `⌘⇧G`로 직접 지정했다. AX는 해당 파일 행이 `(selected)`이고 URL이 QA 사본 내부 파일임을 표시했고(`manifest-selected.ax.txt`), Open 후 앱은 `f0r h3r 열기 완료`, 버전 0.80.0/build218을 보였다. 이것은 manifest 선택 열기에 한정한 PASS다.

이어서 QA `.circlr` 사본에 실제 **`⌘S`**를 입력했다. CUA가 약 16초 뒤 timeout을 반환했고, 앱은 저장 완료를 표시하지 않았다. 약 3초 동안 채집한 `save-hang-sample.txt`의 main thread **2,504/2,504 samples**는 `AppStore.save → ProjectStore.saveSessionReportingCleanup → ProjectStore.saveReportingCleanup`의 `ProjectStore.swift:249`에서 `FileManager.copyItem → copyfile → clonefileat`에 머물렀다. 30초가 넘은 뒤에도 변화가 없었고, QA 상위 폴더의 `.circlr-save-*` staging 안에는 빈 `media/`만 남아 **디스크 사용 0 KB, 파일 0개**였다. 파일 복사가 실제로 끝나지 않았다는 범위에서 정지로 판정한다. `clonefileat`가 지연된 외부 원인은 확정하지 않았다.

QA 원본/대상 미디어는 같은 APFS volume(`st_dev` **16777230**)에 있다. QA `.circlr` 사본의 첫 미디어는 regular file **443,270 bytes**, staging 대상 `media/`는 디렉터리였다. 동봉 demo와 QA 곡은 각각 **7개 파일, 1,925,306 bytes**였다(`hang-inventory.json`). 앱 저장 중에도 QA 사본 manifest의 이전 timestamp/해시는 유지됐다. 사전·사후 전체 해시에서 동봉 demo **7/7**, QA `.circlr` **7/7**, 일반 폴더 사본 **8/8**이 모두 동일하다. 따라서 이번 시도에서 기존 QA 문서와 `notes.txt`의 변경은 관측되지 않았다. 일반 폴더의 실제 GUI Open·Save 거부 동작은 시도하지 않았으므로 PASS로 판정하지 않는다.

장시간 정지 후 확인된 격리 앱 PID에만 SIGTERM을 보냈고 앱은 종료됐다. 최종 앱/helper PID는 **0개**다. 0-byte staging 폴더는 재현 증거로 ignored QA 디렉터리에 남겼다. 시스템 기본 출력은 읽기 전용 조회에서 전후 동일했고 UID의 SHA-256은 **`d4bc96d8fc82c68ec973b2af8dca79a5f5ceb89e121c740e29c661043c3e5eed`**다. 시스템·앱 출력 장치를 바꾸거나 재생하지 않았다. 저장 정지로 1440×900/720×900 최종 창 촬영은 중단했으며 build218 이미지 파일은 만들지 않았다. 소스·다른 문서·README·CHANGELOG 수정, 커밋·푸시는 하지 않았다.
