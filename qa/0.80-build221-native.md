# 0.80.0 build221 — native Open·저장 QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build221-native
STATUS: passed for tested native flows; literal non-.circlr ordinary-folder overwrite remains untested
COVERAGE: Documents 격리 앱의 manifest.json 선택→같은 .circlr 폴더 확인→첫 ⌘S→폴더 재열기, notes.txt 포함 .circlr QA 패키지 저장 거부, 원본·미디어·패키지·출력 보존, 전체 창 두 크기
FAILURES: 관찰된 native 경로에는 없음
BLOCKERS: Open 패널에서 확장자 없는 일반 폴더는 탐색만 가능해 해당 경로의 직접 Save 덮어쓰기 시험은 하지 못함
RISK_CALLS: notes.txt 포함 .circlr 패키지의 실제 native 저장 거부를 확인했으나 확장자 없는 일반 폴더 경로까지 결과를 확장하지 않음. 재생·청취·Scarlett 호환성 판단 없음
VERIFIED_ENVIRONMENTS: macOS 26.5.1, circlr 0.80.0/build221, 격리 bundle ID, Documents의 QA 곡 사본
ARTIFACTS: qa/generated/r80-build221-native/ (Git 제외), docs/images/circlr-song-build221.png, docs/images/circlr-song-portrait-build221.png
DELIVERABLE: native AX/alert·전후 SHA-256·실제 전체 창 PNG·프로세스 정리; 다음 owner development lead
```

2026-09-24 KST. `dist/써클러.app`의 `CFBundleVersion` **221**, 패키징 종료, strict deep 서명을 확인한 뒤 실행했다. 배포 앱을 격리 bundle ID로 복사·ad-hoc 재서명하고, 동봉 demo는 UUID만 변경해 QA `.circlr` 곡을 만들었다. 별도로 유효한 manifest·미디어·`notes.txt`가 있는 일반 폴더도 QA 위치에 준비했다. 원본 demo·사용자 프로젝트에는 쓰지 않았다. 배포 실행 파일 SHA-256은 **`19d7a61907905c5c635136e0d8a77a1d107cbb42e4c50e5ebeecd5237be8d032`**, `Assets.car`는 **`3e119fdcd82fd664cad67ac0f0ce408174cb74dff89cf36364a2636db22428bc`**다. 배포 Resources **57/57개**와 동봉 demo **7/7개**의 시작·종료 SHA-256이 동일했고 배포·QA 앱 서명이 유효하다(`baseline.json`, `final-check.json`).

첫 Open 패널에서 QA 곡의 **`manifest.json` 파일 행 `(selected)`** 및 파일 URL을 AX로 확인했다. Open을 누르면 새 두 번째 패널이 나타나 **`오디오·MIDI 미디어에 접근하려면 같은 곡 폴더를 선택하세요`**라고 안내했다. 같은 QA **`.circlr` 폴더 행 `(selected)`** 및 폴더 URL을 확인하고 Open했다. 앱은 `f0r h3r 열기 완료`, 버전 0.80.0/build221을 보였다. OS 권한 prompt는 보이지 않았다.

그 상태의 첫 실제 **`⌘S`는 약 0.5초 안에 `저장 완료 · f0r-h3r-build221-qa.circlr`**를 표시했다(`save-start.txt`, native AX 관찰). QA manifest는 갱신됐고 미디어 **6/6개 SHA-256**이 그대로다. 미디어 참조 6개는 모두 상대 경로로 존재하며 manifest checksum과 실제 파일 SHA-256이 맞는다. QA 프로젝트는 manifest 1개와 미디어 6개, 총 **7개 파일**이다. 성공 경로에 `.circlr-save-*` staging 또는 `.circlr-backup-*`은 남지 않았다(`after-first-save.json`, `final-check.json`). 이후 Open 패널에서 저장된 `.circlr` 폴더 자체를 다시 선택해 `f0r h3r 열기 완료`를 확인했고, 재열기 후 **7/7개 파일 해시**가 저장 직후와 같았다.

확장자 없는 일반 폴더는 Open 패널에서 내부 탐색은 가능했지만 폴더 자체를 선택할 수 없었다. `다른 이름으로 저장…`의 Save panel도 열어 보았으나 대상 폴더를 지정하지 않고 취소했다. 보호 동작은 일반 폴더의 8개 파일을 **byte-identical**로 복사해 이름만 `with-note.circlr`로 만든 QA 패키지에서 시험했다(`with-note-before.json`). 이 패키지를 폴더 자체로 선택해 Open한 뒤 `⌘S`를 누르자 앱은 **`곡 폴더에 프로젝트 외 파일이 있어 덮어쓸 수 없습니다. 다른 이름으로 저장하세요`**라는 alert를 표시했다. `notes.txt`와 manifest를 포함한 **8/8개 파일**이 동일하고 staging/backup은 0개다(`with-note-after.json`). 원래 확장자 없는 일반 폴더 **8/8개**도 불변이다. 따라서 notes가 든 `.circlr` 패키지의 native 저장 거부는 PASS이며, 확장자 없는 일반 폴더의 직접 덮어쓰기 거부는 **native 미실행**이다.

저장 거부 시험 뒤 정상 QA 곡을 다시 열고 재생 정지 `00:00.0` 상태에서 곡 궤도의 일곱 섹션이 보이도록 축소했다. 콘솔 로그를 접고 개인 경로가 화면에 보이지 않음을 확인한 뒤 실제 **전체 창 1440×900 및 720×900**을 촬영했다. CUA의 원본 JPEG bytes는 ignored 증거로 저장하고, `sips`로 **자르기·크기 변경 없이 PNG 변환**했다. 픽셀 크기와 SHA-256은 다음과 같다.

| 파일 | 픽셀 | SHA-256 |
| --- | --- | --- |
| `qa/generated/r80-build221-native/build221-song-orbit-landscape-1440x900.source.jpg` | 1440×900 | `85f97d2b46925a4747a4573c9165eb9b0b015cbb42065ecb5b9b7684e9ccf57a` |
| `docs/images/circlr-song-build221.png` | 1440×900 | `a085a5812e88fd37e64463e3be3e4e35f456018ccb03f2f1267e6581780ad24e` |
| `qa/generated/r80-build221-native/build221-song-orbit-portrait-720x900.source.jpg` | 720×900 | `217edd47891dbafc26a41544a76894a6d75cc30283feb2c1b54987a88e441e8e` |
| `docs/images/circlr-song-portrait-build221.png` | 720×900 | `f5d4efc9ed6967a8c2b9880f5cfb15367531fd2a4af153f200dfad906b9b270a` |

QA 앱은 `⌘Q`로 종료했고 최종 앱/helper PID는 **0개**다. 시스템 기본 출력은 읽기 전용 조회에서 전후 동일했다. 시스템·앱 출력 장치를 바꾸거나 재생하지 않았다. 전후 QA 실행 파일, QA 미디어, 일반 폴더 및 note 패키지의 보존과 staging/backup 0개를 다시 확인했다(`final-check.json`). 이 보고서에는 앱·곡 절대경로 및 장치 UID를 기록하지 않았다.
