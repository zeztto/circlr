# 0.80.0 build239 — 선택창 키보드 경로 수정 통합

```text
TEMPLATE_VERSION: qa-response-v1
STATUS: AUTOMATED_PASS · KEYBOARD_NATIVE_PASS · RELEASE_BLOCKED
SOURCE: release/0.80.0 · CFBundleVersion 239
```

[build238 격리 native QA](0.80-build238-native.md)는 700×600pt의 미디어·음색·편곡안 검색창에서 Tab을 누르면 캔버스로 포커스가 빠지는 P1을 재현했다. build239은 네 선택창과 미디어 라이브러리의 섹션/트랙 하위 화면에 명시적인 Tab·Shift+Tab·Esc 경로를 추가하고, 편곡안의 키 입력 감시가 다른 컨트롤의 포커스를 다시 검색창으로 빼앗지 않도록 했다. 별도 읽기 전용 diff 검토는 정적 P0/P1을 찾지 못했다. [build239 격리 native QA](0.80-build239-native.md)는 네 선택창과 섹션/트랙 선택기의 실제 AX 포커스·Esc, 가로·세로 화면, 앱 종료·원본 불변을 확인했고 build238 P1을 재현하지 못했다. 실제 한글 IME 조합은 CUA의 입력 한계로 검증하지 못했다.

최종 동일 소스의 `./scripts/swift-local.sh test --scratch-path .build/build239-integration`은 **1,327개 실행·15 skip·실패 0**이다. 로그 `/tmp/circlr-build239-swift.log` SHA-256 `c325495a057c6abbc43b4f09d073f538425ea95003a8db9d97fc86fd0cf26a28`. Python `mcp.test_server mcp.test_endpoint` **47/47**, `Tests/test_mcp_*.py` **39/39**, agent kit **9/9**도 통과했다. `git diff --check`와 모델 E2E 하네스 문법 검사가 통과했다. 모델 실행 이후 하네스 수명주기·추가 쓰기·shell/file change·중복 실행 경계를 강화했고, 기존 JSONL의 독립 trace 재검사와 적대적 하네스 테스트 **11/11**이 통과했다. 설치 kit 전체 해시 검사는 다음 실행부터 적용되며 이 모델 turn에 소급되지 않는다.

`./scripts/build-app.sh`의 `dist/써클러.app`은 **0.80.0/build239**다. arm64 본체·**7개 arm64 helper**와 `codesign --verify --deep --strict`를 확인했다. 실행 파일 SHA-256 `f87136cef6f5453bcfd7aa034cb75bf4d49873efea267a6aad8d0b166ace9314`, 패키지 로그 `/tmp/circlr-build239-package.log` SHA-256 `d4b5df231b621e9fc7af6592cd9042e507cfd8cc72ea785e8e3b5db46ceeb0bf`. Demos **27/27**, Codex kit **26/26**(생성된 `__pycache__` 제외), `LICENSE`·`THIRD_PARTY_NOTICES.md` **2/2**가 소스와 byte-identical이다. 패키징으로 재생성된 작업 트리 `Resources/Assets.car`는 원본으로 되돌렸고, 앱 패키지의 아이콘은 보존했다.

이 자동·키보드 결과에 이어 [build239 모델→MCP native QA](0.80-build239-model-native.md)에서 로그인된 외부 Codex의 MIDI 한 step 편집, 앱 콘솔 표시, 독립 QA의 Undo·저장·재열기·nonzero WAV를 확인했다. 이는 1회 관찰된 제한 동선이며 모델 프로세스의 전면 격리나 앱 안 계정 대화를 뜻하지 않는다. 최종 빈 곡 DAW 제작, 실제 compositor FPS/장시간 MP4 또는 물리 청취는 여전히 PASS가 아니다. [0.80 출고 gate](0.80-release-gates.md)에 남은 항목을 닫기 전에는 tag/Release를 만들지 않는다.
