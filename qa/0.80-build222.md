# 0.80.0 build222 — 단일 곡 첫 송폼 화면

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build222
STATUS: PARTIAL — 기본 화면 native PASS; 세로 창 배치 후속 수정은 build223
COVERAGE: 저장된 뷰가 없는 단일 곡의 첫 선택, 저장된 뷰·다중 곡 보존, 초기 layout 명령, DAW/AI 독립 감사
PASS: Swift 전체 1,188개 중 14 skip·실패0; 마지막 camera 보강 후 targeted 12개 실패0; 앱/5 helpers build222 deep signing
BLOCKERS: 좁은 창 resize의 04 라벨 가림, 실제 take·물리 청취·장시간 영상·전체 제작 동선
RISK_CALLS: 전체 Swift 테스트 뒤 초기 camera 보강 1곳을 추가했으므로 최종 소스의 전체 회귀를 주장하지 않음
ARTIFACTS: qa/generated/r80-build222-swift-test.log, qa/generated/r80-build222-final-targeted.log, qa/generated/r80-build222-package.log
DELIVERABLE: 개발 브랜치 후보; tag/Release 보류
```

UI 감사는 번들 `f0r h3r`에 `hierarchyView`가 없고 root 곡이 하나인데, 데모 첫 화면이 앨범의 3개 원만 보여 송폼 7개 섹션을 숨긴다는 P2를 찾았다([감사](0.80-ui-ux-audit.md)). `StudioWorkspace.openingSelection`은 저장된 뷰가 없는 단일 leaf 곡에만 곡 주소를 돌려준다. `AlbumCanvas`는 이를 첫 복원에 사용하고, 뷰 크기가 생기기 전에 도착한 명령은 소비하지 않는다. 첫 명시적 카메라가 설정된 뒤 기본 앨범 카메라가 나중에 덮지 않도록 초기화 상태를 기록한다. 다중 곡과 저장된 사용자 카메라는 기존 경로다. 독립 [코드 리뷰](0.80-build222-review.md)는 새 고확신 P1/P2를 찾지 못했다.

첫 수정 직후 `swift test`는 **1,188개 중 14 skip·실패 0**, 종료 코드 0이었다. reviewer가 지적한 초기 layout 재설정 경계를 보강한 뒤 `SavedWorkspaceTests` 관련 **12개 실패 0**으로 앱 source를 다시 컴파일했다. 마지막 소스의 arm64 release 앱/5 helpers/25파일 agent kit은 `./scripts/build-app.sh`로 패키징했다. `CFBundleVersion=222`, `codesign --verify --deep --strict` 통과, 앱 실행 파일 SHA-256은 `7f961c102f572b84aacd3d2adec003a71068b616dad1a42e08ce81ba7fe7de7e`다. 상세 로그는 위 경로에 있고 생성물은 Git에서 제외한다.

장치 독립 DAW 감사에서는 Bach 70초 nonzero WAV와 18초 stem 2개를 만들고 원본 manifest hash를 유지했다([오디오 감사](0.80-daw-audio-audit.md)). AI/MCP 감사에서는 Python 84개와 Swift 대상 22개 실패 0을 확인했지만, 앱 내 Codex 계정 turn과 trusted `save/export/open`은 미연결이고 마지막 빌드의 실제 앱 E2E는 수행하지 않았다([에이전트 감사](0.80-agent-journey-audit.md)). 구형 Scarlett의 실제 세대·호환성과 HAL timeout 원인은 확정하지 않는다. CalDigit 입력도 별도 경로에서 timeout이 있었으므로 실제 녹음 take는 장치 독립 호스트에서 따로 확인해야 한다([입력 조사](0.80-selected-input-investigation.md)).

[build222 격리 native QA](0.80-build222-native.md)는 `f0r h3r` 첫 로드가 1440×900에서 곡 궤도와 일곱 섹션으로 열리는 것을 확인했다. 720×900에서는 서클과 AX 항목은 남지만 04 Club Chorus의 화면 라벨이 숨었다. 이 세로 화면 결함을 build223의 viewport context-fit으로 이어서 수정한다. build222는 출고 후보가 아니다.
