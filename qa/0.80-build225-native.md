# 0.80.0 build225 native packaged-app smoke

STATUS: PASS for startup, general MCP snapshot, and clean shutdown only. This is not an audio-device, project-editing, or trusted-turn end-to-end test.

2026-09-24 KST. The signed `dist/써클러.app` was copied to the isolated `qa/generated/r80-build225-native/써클러 build225 네이티브 검증.app`, assigned bundle ID `com.circlr.build225nativeqa`, and ad-hoc re-signed. It used only `~/Library/Application Support/circlr-build225native-qa`. No user project was opened, and no Play, record, audio-device selection, or system-default change was requested.

- The copied app launched. Within two seconds its dedicated `Agent/agent.sock` accepted the stdio MCP adapter. MCP initialize negotiated protocol `2025-11-25` and listed 27 tools.
- `circlr_snapshot` from the running app reported bundle `com.circlr.build225nativeqa`, version `0.80.0`, build `225`, one visible window, revision 0, and `dirty=false`.
- The packaged source executable SHA-256 before and after this check was `f8a0d8dc4d71b2b9ab325437713e8611e8a8afa675017f77f5852b232a1f7cd0`. Its QA copy has a different executable hash because re-signing changes the Mach-O signature. All 57 packaged Resources files, including bundled demo assets, had matching SHA-256 in source and QA copies. Both bundles passed `codesign --verify --deep --strict` after the check. Inventory: `qa/generated/r80-build225-native/inventory.json`.
- AppleScript Quit completed without a permission prompt. The isolated socket disappeared; no isolated app/helper process remained. The QA support directory retains only its recovery lock and endpoint metadata.

This does not establish Scarlett compatibility, physical listening, input recording, long-running movie capture, GUI DAW editing, or an app-owned trusted Codex turn. Those release gates remain open.
