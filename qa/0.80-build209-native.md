# 0.80.0 build 209 — macOS packaged-app QA

2026-09-24 KST. Tested the packaged `dist/써클러.app` through an isolated, ad-hoc-signed copy (`com.circlr.build209nativeqa`, `circlr-build209native-qa` storage). Source executable SHA-256: `90a84563c922da8c2a687aacbbbec93deb4177d563613cf225b98f730276165e`; source app tree SHA-256 at copy: `42416d06befdbc6a3ad0333eb5a8a49791ad90845e71a78fbe439080e987cc68`. The source executable hash was rechecked after QA. The isolated copy passed strict deep codesign verification. Raw state, screenshots, MP4, and analysis are in ignored `qa/generated/r80-build209-native/`.

| Check | Result |
| --- | --- |
| Initial `f0r h3r` Play | **PASS (observed).** Before Play, `followSettings.target=song`. The first Play displayed the song circle and all seven section circles; playback snapshot had `followedTarget=composition` while playing. |
| Explicit section follow | **PASS (observed).** Selected `현재 섹션 서클`; the menu kept its checkmark and the next Play snapshot had `target=section` and `followedTarget=section`. |
| Dense-section labels | **PASS (observed at fit scale).** During section playback, larger instrument/MIDI labels remained visible while small FX/output labels were hidden. Immediately after Stop, FX/output labels including `reverb`, `pan`, `delay`, and `… 출력` returned. Evidence: `section-lod-playing.png`, `section-lod-stopped.png`. Hover and zoom recovery were not separately tested. |
| Output and transport | **PASS for routing/state.** Selected `Mac Studio 스피커` inside the QA app only; the playing snapshots reported `actualOutputDeviceName=Mac Studio 스피커`. Play/Stop worked repeatedly. The system default remained Scarlett 6i6 USB (`AppleUSBAudioEngine:Focusrite:Scarlett 6i6 USB:10000B65:1,2`) in before, playing, and final catalog snapshots and a post-exit catalog read. Physical listening was not performed. |
| Window captures | **PASS.** Original app screenshots: [landscape 1440×900](../docs/images/circlr-song-build209.png), SHA-256 `5da43cd7c6500bd2ed7f628db58798b459d8c986892456f290a5315753451ca3`; [portrait 720×900](../docs/images/circlr-song-portrait-build209.png), SHA-256 `c71ee707adf8dffdf780c0d9a1ccc3ff94d45ecdf3891aae1618af809c369557`. Both show the song orbit during playback. The available display height was 900, so an approximately 700×1000 native window was not captured. |
| MP4 export and independent decode | **PASS for file integrity/timing/audio samples.** `build209-native-8s.mp4` SHA-256 `301e53ad00598cce9b3b19857d40838e6272831badfeb1d19e9ebdff5aa137ff`; AVFoundation decoded 7.805 s, 1440×800, 234 video frames (~29.98 fps), no duplicate PTS, no identical adjacent decoded frames, no gaps over two nominal frames. Frame interval p50 33.33 ms, p95 38.33 ms, p99 45.00 ms, max 51.67 ms. UI save notice said 235 frames; the independent decoder counted 234. This is a count discrepancy, not evidence of dropped visual content. AAC decoded to 48 kHz stereo PCM, 7.8048 s, peak 0.608, RMS 0.0743; start/middle/end windows were nonzero; first video/audio PTS offset 0 ms. Independently extracted frames near 0.085, 3.873, and 7.368 s showed the animated intro-section circles and bar progression. The MP4's section view is concentrated in the upper portion of the frame with substantial empty lower space; this remains a visual quality observation, not a timing or decode failure. |

The QA app and its helpers were closed after the run; the pre-existing build187 QA process was left running. No build209 recovery data was written (only its own `recovery-v2.lock` and `Agent/selected.json` remained). No production or older QA storage was modified by this slice. Open: physical speaker listening; explicit hover/zoom label recovery; 700×1000 native capture on a taller display. No confirmed new source bug was found in this bounded run.
