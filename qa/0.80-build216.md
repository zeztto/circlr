# 0.80.0 build216 · 섹션 우클릭 경계 메뉴 재검증

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build216
STATUS: PARTIAL · 자동 재현/수정·패키지·대상 native popup PASS, 0.80 출고 gate 별도
COVERAGE: build215 native FAIL 원인, NSMenu.update RED/GREEN, 전체 Swift, arm64 앱/5 helpers, strict deep 서명, 데모 동일성
FAILURES: 수정 후 자동 회귀에서 관측된 실패 없음
BLOCKERS: build215 Open panel의 일회성 manifest alert 원인, 실제 take·청취·장시간 영상·전체 제작/계정 AI
RISK_CALLS: 수정은 선택 서클 top-level context menu에 한정. 읽기 전용 리뷰에서 새 고신뢰 P1/P2 없음
VERIFIED_ENVIRONMENTS: macOS 26.5.1, release/0.80.0 개발 worktree, 0.80.0/build216 arm64 ad-hoc 패키지
ARTIFACTS: .build/build215-menu-red.log, .build/build216-menu-green.log, .build/build216-test.log, .build/build216-package.log (Git 제외)
DELIVERABLE: build216 개발 후보; v0.80.0 tag·GitHub Release 없음
```

2026-09-24 KST. [build215 실제 앱 QA](0.80-build215-native.md)에서 첫 Intro의 `순서 앞으로`, 마지막 Outro의 `순서 뒤로`가 이동 불가인데도 메뉴에서 활성으로 보였다. 첫 Intro의 항목 클릭은 no-op이었다. 기존 테스트는 메뉴를 만든 직후만 검사했다. 테스트에 AppKit의 popup 전 `NSMenu.update()`를 넣자 **첫 `[true,true]` vs 기대 `[false,true]`, 마지막 `[true,true]` vs 기대 `[true,false]`로 실패**했다(`.build/build215-menu-red.log`, exit 1). `AlbumCanvas.circleMenu`의 선택 서클 top-level 메뉴에 `autoenablesItems=false`를 적용한 뒤 두 집중 테스트가 모두 통과했다(`.build/build216-menu-green.log`, exit 0). 이 수정은 우클릭의 표시/활성 상태만 바꾸고 섹션 이동의 Core guard는 유지한다.

수정 후 `./scripts/swift-local.sh test --parallel --num-workers 4 --scratch-path .build/build216-test`는 **1,184개 항목을 끝까지 나열하고 종료 코드 0**이었다. 로그 SHA-256은 `d1a65e9dcdae83bb97c48c81937ec381f73ffd6ff35fe3e509817f4645228760`이다. 환경변수 없는 장치 실테스트 2개는 skip된다. Python 프로젝트 50/MCP 서버 41/agent kit 9는 build215 소스에서 모두 통과했고 그 뒤 Python 소스는 바뀌지 않았다. 독립 좁은 UI 리뷰에서 메뉴 다른 action·submenu·명령 팔레트의 새 고신뢰 P1/P2는 없었다.

`./scripts/build-app.sh`의 `dist/써클러.app`은 **0.80.0/build216**이다. 실행 파일과 오디오 helper 5개는 arm64, `codesign --verify --deep --strict` 통과. 실행 파일 SHA-256 `04c63d791b7bd3a5c7549573aabe0626e3b9fc33b9e9e3dc821b2e04da16d5da`, 패키지 로그 SHA-256 `7510853c52e56ba5a27bc4de5277cb9ef63c15f1f904596c91ce2c35aec7a6b4`, 동봉 `f0r h3r` manifest SHA-256 `a75ea9f2f3917f82184ba94c31e071905c0c6a1bfa2e90259ad554eaa5c794d7`로 소스와 일치한다. `plutil -lint Resources/Info.plist`와 `git diff --check` 통과.

[build216 최종 앱 native QA](0.80-build216-native.md)에서 실제 720×900 popup의 **Intro 앞으로 disabled / Outro 뒤로 disabled / 중간 Lift 양방향 enabled**를 AX와 원본 PNG로 확인했다. `⇧⌥→` 이동과 `⌘Z` 원복의 순번·시간도 확인했다. QA 앱·helper는 종료 후 0, 원본·QA 데모와 시스템 기본 출력은 불변이다. build215의 첫 `⌘O`에서 발생한 `manifest.json` missing alert는 디스크 파일이 존재했고 절대경로 재선택 후 열렸으나 원인은 미확정이다. build216은 처음부터 절대 경로로 열어 그 조건을 재현하지 않았다. 선택 UID 녹음은 명시적으로 미지원이며, Scarlett 정상 입출력·실제 청취·0.80 출고 승인을 주장하지 않는다. 필수 QA가 열려 있어 tag·Release를 만들지 않는다.
