# 0.80.0 build230 — 격리 native 화면·MIDI 녹음 경계 QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build230-native
STATUS: PASS (검증한 화면·MIDI 시간·녹음 중 전환 차단에 한함)
COVERAGE: 서명 build230 격리 실행, 번들 f0r h3r 사본, 720×900pt 및 1440×900pt 전체 창, MIDI 녹음 시계, 녹음 중 새 앨범 거절, 종료·서명·리소스 불변
FAILURES: 위 범위에서 관찰되지 않음
UNVERIFIED: 물리 MIDI 입력·오디오 녹음·Scarlett 호환성·오디오 청취·영상 녹화·긴 편집 세션
VERIFIED_ENVIRONMENTS: macOS native, circlr 0.80.0/build230, com.circlr.build230nativeqa, 고유 QA Application Support
ARTIFACTS: qa/generated/r80-build230-native/ (Git-ignored)
```

2026-09-24 KST. 최종 `dist/써클러.app`의 주 실행 파일 SHA-256 **`68541bd055c9a52494ee9b7c059e79996b59c4a4a8a9a3f73c6b591d15227b2c`**를 확인한 뒤 별도 QA 앱으로 복사했다. QA 사본의 bundle ID는 `com.circlr.build230nativeqa`, 복구·agent 경로는 `~/Library/Application Support/circlr-build230native-qa`, 데모 편집 사본 경로는 `~/Library/Application Support/com.circlr.build230nativeqa/Demo Projects`다. 문서 유형 등록을 제거하고 ad-hoc 재서명했다. 사용자 프로젝트나 production 앱은 열지 않았다. 두 앱 모두 `codesign --verify --deep --strict` 통과했고, 원본과 QA 사본의 Resources **57/57개** 및 보조 실행 파일 **5/5개**가 byte-identical이다. QA 사본 주 실행 파일 SHA-256은 `2f82309eadc2f893b653069c2305ed00abafd58e0933b28186f1e4162e39fd9e`다.

시작 화면에서 번들 **f0r h3r**을 열었다. 720×900pt 세로 창과 1440×900pt 가로 창에서 상단 컨트롤·곡 궤도·01–07 섹션 서클과 이름이 전체 창 픽셀 및 AX에서 보였다. 세로 창의 상단 컨트롤은 두 줄로 재배치되어 겹치지 않았다. 가로 화면은 시스템 디스플레이 설정을 바꾸지 않고 QA 창만 `Window → Move to LEN P27h-10` 및 창 경계 드래그로 조절했다. 가로 캡처는 데모를 연 뒤 창 크기를 바꾼 상태이며 최초 로드 기본 구도의 검증으로 확대하지 않는다.

대표 증거는 CUA `getScreenshot()`의 **전체 창 JPEG 원본**을 저장하고 `sips`로 **자르기·크기 변경 없이 PNG 변환**한 것이다. Retina 세로 PNG는 1440×1800px, 가로 PNG는 2880×1800px다. 각 `.source.jpg`와 `.ax.txt`, `resource-hashes.tsv`, `final-check.json`, `SHA256SUMS.txt`를 Git-ignored QA 폴더에 보존했다.

| 전체 창 PNG 절대 경로 | SHA-256 |
| --- | --- |
| `/Users/sungwoonjeon/Documents/ChatGPT/circlr/.build/release-0.80-worktree/qa/generated/r80-build230-native/portrait-720x900.png` | `46a92d3ea8d9d1187011c17c9a7bfa1179ca51364282d089f751294233aa7e8b` |
| `/Users/sungwoonjeon/Documents/ChatGPT/circlr/.build/release-0.80-worktree/qa/generated/r80-build230-native/landscape-1440x900.png` | `05baec715da6d34c72d97b223720a07c8570e0db05965869048f65516d163d34` |

MIDI 경계는 01 `접속 · DJ Intro` 섹션의 `클럽 킥 MIDI` 서클에서 검증했다. `MIDI 녹음` 버튼을 누르자 상단 시계가 **00:00.4 → 00:06.8 → 00:15.7 → 00:20.2 → 00:25.6**으로 진행하고 `MIDI 녹음 중` 상태가 표시됐다. 그 사이 프로젝트 메뉴의 **새 앨범**을 선택했을 때 곡은 `f0r h3r`로 유지되고 상태가 **`녹음 정지와 파일 마무리 후 다시 시도하세요`**로 바뀌었다. 녹음 종료 후 상단 시계는 **00:00.0**으로 돌아왔다. MIDI 입력을 보내지 않아 결과는 **0개 note**이고, 외부 장치 연주·실제 take 내용 품질은 검증하지 않았다. 차단 상태의 전체 창 증거는 `/Users/sungwoonjeon/Documents/ChatGPT/circlr/.build/release-0.80-worktree/qa/generated/r80-build230-native/midi-record-blocked-switch.png`(SHA-256 `4757acc0781db74e33b9f466b7a68b314e4b4c9ef5b5ce826ddcc033c0cac2e0`) 및 같은 이름의 `.ax.txt`다.

QA 사본 종료 때 **변경 버리기**를 선택해 데모 편집을 저장하지 않았다. 종료 후 원본 실행 파일 해시는 동일하고, QA 앱/보조 프로세스 PID와 고유 agent socket은 남지 않았다. 시스템 기본 오디오 설정을 변경하지 않았으며 Play, 오디오 Record, Scarlett 입력·출력 사용, 권한 허용을 수행하지 않았다. 따라서 이 PASS는 화면과 MIDI 녹음 중 문서 경계의 native 증거이고, 구형 Scarlett의 최신 macOS 호환성 판정은 아니다.
