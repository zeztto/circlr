# 0.80.0 build234 — 테이크·영상 녹화 native QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build234-native
STATUS: PARTIAL PASS — 실행한 패키지 UI·MIDI 시작/정지·앱 전용 내장 출력 재생은 통과
COVERAGE: signed packaged build234, 격리 QA 앱, 700/720/900/1440pt 상단 컨트롤, 대상 미선택/선택, MIDI take 시작/정지, Mac Studio 내장 스피커 앱 전용 재생, 새 번들 데모 전체 곡 스크린샷
FAILURES: 실행한 조합에서 재현된 UI 겹침·AX 상태 결함 없음
UNVERIFIED: 실제 MIDI 입력과 음표 저장, 오디오 입력·녹음 파일, 실제 청취·음질, MP4 산출물·장시간 프레임, Scarlett 재생·호환성, VoiceOver 음성
VERIFIED_ENVIRONMENTS: macOS 26.5.1, circlr 0.80.0/build234, com.circlr.build234nativeqa, 격리 Application Support
ARTIFACTS: qa/generated/r80-build234-native/ (Git-ignored), docs/images/circlr-song-build234.png, docs/images/circlr-song-portrait-build234.png
```

2026-09-24 KST. `dist/써클러.app`을 `.build/build234-native/써클러 build234 네이티브 검증.app`으로 복사하고 bundle ID를 `com.circlr.build234nativeqa`로 바꾸어 문서 유형 등록을 제거한 뒤 ad-hoc 재서명했다. 원본과 QA 사본은 `codesign --verify --deep --strict`를 통과했다. 주 실행 파일 SHA-256은 원본 `48e13ad54aedb965f64470ed313340a320c8f7cd84f172ef65b994505ca8dafb`, QA 사본 `3fe0b0deb5ab1b486fed41a060f8c35299bf568cbae67575c4bb933fadbbfdde`이다. 두 사본의 Resources **57/57개**와 helper **6/6개**는 byte-identical이다. [개별 해시](generated/r80-build234-native/bundle-integrity.json)를 남겼다. 복구·Agent는 `~/Library/Application Support/circlr-build234native-qa`, 데모 사본은 `~/Library/Application Support/com.circlr.build234nativeqa/Demo Projects`로 격리했다. 원본 앱·사용자 프로젝트는 열거나 변경하지 않았다.

실제 전체 창을 **700·720·900·1440pt 너비, 900pt 높이**로 조절했다. 선택한 `접속 · DJ Intro`에서 700·720pt는 `녹음`과 `영상`, 900·1440pt는 `테이크 녹음`과 `영상 녹화`를 두 번째 컨트롤 행 또는 단일 행에 표시했다. 각각 재생·팔로우·루프·감상 모드·서클 추가와 겹치지 않았다. [700pt](generated/r80-build234-native/width700-selected.source.jpg), [720pt](generated/r80-build234-native/width720-selected.source.jpg), [900pt](generated/r80-build234-native/width900-selected.source.jpg), [1440pt](generated/r80-build234-native/width1440-selected.source.jpg)에 같은 이름의 AX 텍스트가 있다. 선택 상태에서 `MIDI 테이크 녹음`과 `오디오 테이크 녹음` 메뉴가 모두 활성화되고 대상 `접속 · DJ Intro · 클럽 킥`을 표시했다. 대상 미선택에서는 [720pt 메뉴](generated/r80-build234-native/width720-no-target-menu.ax.txt)와 [초기 1440pt 메뉴](generated/r80-build234-native/no-target.ax.txt)에 `녹음할 섹션 서클을 선택하세요` 및 두 녹음 항목의 disabled 상태가 나타났다. 이는 메뉴 동작/접근성 확인이며 실제 오디오 입력 검증은 아니다.

선택한 섹션에서 MIDI 테이크를 시작하자 상단 버튼은 `MIDI 녹음 중`, 재생 버튼은 `녹음 정지`로 바뀌고 `영상 녹화 시작`이 disabled로 표시됐다. [활성 AX](generated/r80-build234-native/midi-active.ax.txt)와 [픽셀](generated/r80-build234-native/midi-active.source.jpg)을 저장했다. 상단 `녹음 정지`를 누르면 00:00.0으로 돌아오고 `MIDI 녹음 저장 · 0개 note` 로그가 남았다. MIDI 장치를 연주하지 않았으므로 음표 생성·저장 성공으로 해석하지 않는다. 오디오 테이크 시작은 macOS 기본 입력이 오래된 Scarlett인 상태에서 기기·권한·파형 확인이 필요해 실행하지 않았다.

시작 시 앱 출력 설정의 **시스템 기본값은 Scarlett 6i6 USB**였다. QA 앱 안에서만 `Mac Studio 스피커`를 선택했고 설정 키 `circlr.playback.outputDeviceUID=BuiltInSpeakerDevice`를 확인했다. 번들 데모의 실제 재생 시계는 `00:00.3`에서 `00:12.3`까지 진행했고 [AX](generated/r80-build234-native/playback-built-in.ax.txt)에 `마지막 재생 확인 장치: Mac Studio 스피커`가 남았다. 정지 뒤 재생 버튼과 00:00.0으로 복귀했다. 이는 앱의 출력 연결·재생 상태만 검증하며 물리 청취나 음질 판정이 아니다. 읽기 전용 [종료 후 장치 catalog](generated/r80-build234-native/output-catalog-after.json)에서도 macOS 기본 출력은 Scarlett 6i6 USB이고 `defaultUID` SHA-256은 이전 build233과 동일한 `d4bc96d8fc82c68ec973b2af8dca79a5f5ceb89e121c740e29c661043c3e5eed`이다. 시스템 기본 설정과 Scarlett 장치를 조작하지 않았다.

최종 README 캡처는 위 녹음 검증을 버리고 **번들 데모를 새로 연 정지 화면**이다. CUA 전체 창 JPEG를 저장한 후 `sips`로 크롭·합성·크기 변경 없이 PNG로 변환했다. 가로는 실제 `LEN P27h-10`의 **1440×900pt / 2880×1800px**, 세로는 실제 `2460G4`의 **720×900pt / 1440×1800px**다. 둘 다 01–07 섹션, 00:00.0, `데모곡 불러오기 완료`, 분리된 테이크·영상 녹화 컨트롤과 서클 추가를 표시한다. 디스플레이 설정은 변경하지 않았다.

| 전체 창 | PNG SHA-256 | JPEG 원본 SHA-256 |
| --- | --- | --- |
| [landscape-1440x900.png](generated/r80-build234-native/landscape-1440x900.png) | `4927f19664b128fd3aa767d6afdfc6318d9e35b06eb6e59ae486378dd8a27ca1` | `0ca5026ff2e55227f7a0638420143dba4b146d709d95a8df3567746bf895f05c` |
| [portrait-720x900.png](generated/r80-build234-native/portrait-720x900.png) | `3960f155a3428f704ca540b90817246fdac1377a3dbcdc050575c257d962cae2` | `dd42e1e4cdaa548fb8fac04e6d43ca8082be3fd38e29bd4bb45b0a5a07106df7` |

최종 QA 앱은 `⌘Q → 변경 버리기`로 종료했다. [clean-exit.json](generated/r80-build234-native/clean-exit.json)에서 격리 앱/자식 실행 파일 PID **0**, 전용 `Agent/*.sock` **0**을 확인했다. 저장된 증거의 [SHA256SUMS.txt](generated/r80-build234-native/SHA256SUMS.txt)를 남겼다.
