# 0.80 build208 · 격리 앱의 내장 출력 재생과 짧은 MP4

상태: **이 native 동선 부분 통과 / 0.80 출고 승인 아님** · 2026-09-24. `dist/써클러.app`의 0.80.0/build208을 `com.circlr.build208nativeqa`로 복사·ad-hoc 재서명해 실행했다. QA 앱의 저장 위치는 새 `circlr-build208native-qa`였고 기존 build187 QA 앱(PID 65003), 사용자 앱·프로젝트·복구본은 건드리지 않았다. 원본 앱 tree SHA-256은 `e8d033514c8070c0b698a95c396af924ec34ace308d5d7ed14c2c9a62faf0841`, QA 앱은 `d39336ac1acfe6a9e9be5dec9238fbc5853d53f049df11b477ffeafe2abf4374`다. 양쪽 strict deep codesign 검증이 통과했고, 원본 실행 파일 SHA-256 `4ca95b5907d7976f0089c2bcf15fd3e303d4fea1bad2508306dcd7e132321645`와 원본 app tree는 QA 전후 동일했다.

시스템 기본 출력은 전후 `Scarlett 6i6 USB` UID 그대로였다. QA 앱의 **출력 설정**에서만 `Mac Studio 스피커`(`BuiltInSpeakerDevice`)를 선택했다. `f0r h3r` 데모를 열자 AX에 `0.80.0 · 빌드 208`이 표시됐다. 같은 앱 세션에서 Play/Stop을 두 번 실행했고, MCP snapshot은 각각 실제 출력 장치 이름 `Mac Studio 스피커`, 출력 `ready`, 재생 시계 **43.72초 / 9.32초** 진행을 기록했다. 두 Stop 뒤 출력은 `idle`, 재생은 `false`였으며 마지막 앱 종료 뒤 build208 출력 helper 잔류는 0이었다. 이는 실제 장치 선택·출력 큐·재생 시계·정지의 증거이며 **물리 청취나 음질 판정은 아니다**.

| MP4 검사 | 첫 녹화 | snapshot 없이 재녹화한 목표 파일 |
|---|---:|---:|
| 길이 · 디코딩 프레임 | 20.727초 · 612 | **8.423초 · 250** |
| 디코딩 화면 | 1440×800 | 1440×800 |
| 프레임 간격 p95 · 최대 | 40.0ms · **345.0ms** | 43.33ms · **60.0ms** |
| 2프레임 초과 간격 · 동일 PTS · 인접 동일 화면 | 1 · 0 · 0 | **0 · 0 · 0** |
| 첫 비디오/오디오 PTS 차 | 0ms | 0ms |
| 48kHz stereo PCM peak · RMS | 0.732 · 0.0852 | 0.608 · 0.0737 |

목표 MP4의 SHA-256은 `a93a5fa51e6dd42f5c879922d9ff905ac0ec140ab5a848876489739332d77bba`다. `afconvert`로 AAC를 PCM으로 별도 해독해 시작·중간·끝 1초 구간 모두 nonzero임을 확인했다. AVFoundation은 목표 파일의 p50/p95/p99를 33.33/43.33/48.33ms, 평균 29.68fps로 측정했다. 시작 0.085초·중간 4.192초·끝 7.993초 프레임을 따로 디코딩해 실제 캔버스, 진행 위치 변화, 깨짐 없는 화면을 눈으로 확인했다. 첫 녹화의 345ms gap은 MCP snapshot 수행 시점과 겹쳤고 snapshot 없는 목표 녹화에서 재현되지 않았다. **원인을 snapshot으로 확정하지 않으며**, 녹화 프레임 품질을 무조건 PASS로 선언하지 않는다. 1080p·장시간 영상과 중간/끝 청감 동기는 검증하지 않았다.

전체 창 1440×900에서 데모 직후 [앨범 캔버스](0.80-build208-native-evidence/demo-load-canvas.png)와 재생 중 [곡 전체 팔로우 구도](0.80-build208-native-evidence/playback-song-focus.png)를 보존했다. 앨범의 세 서클은 화면 상단 중앙에 있고 하단에 큰 여백이 남는다. 곡 팔로우에서는 7개 섹션 서클이 대략 y=230–490에 모이고 그 아래가 비어 있어, 이전 README 첫 프레임과 같은 구도 우려가 **build208에서도 관찰**된다. 재생 중 섹션 팔로우의 라벨·케이블은 밀집해 보였다. 감상 모드 재생 전체 창과 섹션 팔로우 캡처도 로컬 원자료에 남겼다. 이 관찰은 레이아웃 UX 확인 항목이며 재생·MP4의 확정 소스 결함으로 분류하지 않는다.

원자료는 Git 제외 `qa/generated/r80-build208-native/`에 보존했다: `package.json`, 재생 전후 snapshot, 두 MP4와 timing/audio JSON, 감상·섹션 팔로우 화면, 디코딩한 시작·중간·끝 PNG. 첫 MP4 SHA-256은 `0223ee6f5c2a031caed3a881405a2a62d6177a8278f8b8c43232779dde4a267b`; 목표 MP4는 위 해시와 같다. QA 종료 시 `recovery-v2.json`과 build208 worker는 없고, build187 PID 65003은 살아 있었다. 사용자 `circlr/recovery.json` SHA-256 `688558b00e4160106065114e7a6305ffa1a24ea8f408e38e5148c38`, 기존 QA `circlr-portfocus-qa/recovery.json` SHA-256 `0c7507e9686665d048b82c5b5780d86e36dfda94fc1b91759c0f35702d8380e7`도 기존 기록과 같았다.

이 범위에서 재현된 새 소스 결함은 없다. 실제 스피커 청취, 반복 cold start, Scarlett 실패 후 복구, 1080p·장시간 영상, 완전한 GUI 제작·재열기·바운스 동선은 **OPEN**이다.
