# 0.80.0 build236 — 콘솔 고밀도 native QA

```text
TEMPLATE_VERSION: qa-response-v1
REPLY_TO_TEST_REVISION: 0.80-build236-native
STATUS: FAIL — 재생 중 고밀도 에이전트 로그 입력에서 앱 내부 animation frameCount 진행이 크게 감소
COVERAGE: 서명된 build236 격리 앱, 1440×900·720×900·700×900pt 전체 창, 500→501 로그 교체, 읽기 위치 고정·새 로그·최신 복귀, 700pt 폭/40pt 로그, 앱 전용 내장 출력 Play/Stop, 화면 캡처·격리 종료
FAILURES: 700×900pt/40pt 콘솔·500개 보관 로그 상태에서 Play 중 100건/15.28초 로그 입력: frameCount +166/15.28초 ≈10.9 count/s. 무입력 비교: +415/8.03초 ≈51.7 count/s.
UNVERIFIED: 화면 계측 FPS·녹화 프레임 안정성, 실제 물리 청취·음질, Scarlett 재생·호환성, VoiceOver 음성, 콘솔 키보드 PageUp/Home의 로그 포커스 동선, 실제 물리 MIDI/마이크 녹음
VERIFIED_ENVIRONMENTS: macOS 26.5.1 (25F80), circlr 0.80.0/build236, com.circlr.build236nativeqa, 고유 Application Support·Agent socket
ARTIFACTS: qa/generated/r80-build236-native/ (Git-ignored), docs/images/circlr-song-build236.png, docs/images/circlr-song-portrait-build236.png
```

2026-09-24 KST. 서명된 `dist/써클러.app`을 `.build/build236-native/써클러 build236 네이티브 검증.app`에 복사해 bundle ID를 `com.circlr.build236nativeqa`로 변경하고 문서 유형 등록을 제거한 뒤 ad-hoc 재서명했다. 원본·QA 사본 모두 `codesign --verify --deep --strict`를 통과했다. 원본 실행 파일 SHA-256은 `1d584821bc6ebe296449f850b73a4bce3271d6e76f75483444e8d7d757b43e74`; QA 사본은 `9c1f6aa4e21917e17e677eb278c12e89b9273a17b33f398be651a46844d343c6`이다. Resources **57/57개**와 보조 실행 파일 **7/7개**가 byte-identical이다. [번들 무결성](generated/r80-build236-native/bundle-integrity.json). 원본 앱이나 설치된 앱은 실행하지 않았다.

MCP 요청 전 QA 앱의 `snapshot.runtime` **bundleID/build/runID**, `selected.json`의 bundleID/runID, QA `agent.sock`의 실제 inode를 대조했다. 대상은 오직 `~/Library/Application Support/circlr-build236native-qa/Agent/agent.sock`이다. 데모 로드 직후 프로젝트 ID `82D1EC6C-0762-4632-A74C-86ADF8472944`, revision **1**, path 빈 문자열, dirty **true**를 기준으로 고정했다. 미존재 jobID를 `circlr_job`으로 조회하는 **read-only 실패 요청**만 사용해 activity를 만들었고, 각 요청은 동일 프로젝트/revision으로 `jobID를 찾을 수 없습니다`를 반환했다. 최종 스냅샷에서도 네 값이 같았다: [초기 상태](generated/r80-build236-native/initial-state.json), [종료 전 상태](generated/r80-build236-native/final-state.json).

500개 activity 보관 경계에서는 sequence **500→501**, retained **500**, oldest retained ID **1→2**로 바뀌었다. 실제 UI에서 `최신 로그` 버튼이 나타나지 않아 자동 팔로우가 유지됨을 확인했다: [500행](generated/r80-build236-native/console-500.ax.txt), [501행](generated/r80-build236-native/console-501.ax.txt). 이어 250건을 목표 20회/초로 전송했지만 실제 소요는 **25.04초, 약 9.98회/초**였다. 이 처리 중 콘솔을 휠로 위로 올리자 화면은 이전 행에 머물고 `최신 로그로 이동 · 새 로그 26개`가 나타났다. 남은 요청 뒤 **125개**로 증가했고, 버튼을 누르면 사라지며 최신 행으로 복귀했다: [휠 입력](generated/r80-build236-native/console-burst-wheel-up.ax.txt), [읽기 위치 유지](generated/r80-build236-native/console-burst-held.ax.txt), [최신 복귀](generated/r80-build236-native/console-latest-return.ax.txt). 휠+AX+스크린샷을 묶은 한 도구 호출이 **18.1초** 걸렸으나 그 안의 개별 UI 프레임 지연으로 분해할 수 없으므로 FPS 근거로 쓰지 않는다.

창 폭 **700pt**, 로그 높이 **40pt**에서도 새 요청 뒤 `최신 로그 1`이 입력줄과 구분되어 픽셀·AX 모두에 보이고 클릭 후 사라졌다: [700pt/40pt AX](generated/r80-build236-native/width700x900-console40-unread.ax.txt), [화면](generated/r80-build236-native/width700x900-console40-unread.source.jpg). 700pt는 **창 폭**이다. 앱의 로그 높이 설정 자체는 40–180pt이므로 700pt 로그 높이는 시험 대상이 아니다. `Page_Up` 키는 로그 스크롤의 키보드 포커스를 확보하지 못한 상태에서 한 번 입력해 결과가 없었으며 키보드 동선을 PASS/FAIL로 판정하지 않는다. VoiceOver 음성은 시험하지 않았다.

앱의 **출력 설정**에서 QA 앱 전용으로 `Mac Studio 스피커`를 선택했다. 설정 AX는 시스템 기본값을 `Scarlett 6i6 USB`, 선택을 `Mac Studio 스피커`로 표시했다: [출력 설정](generated/r80-build236-native/output-preference.ax.txt). `system_profiler SPAudioDataType`도 종료 뒤 Scarlett을 Default Output Device로 보고했다. 재생 중 UI 시계는 **00:20.7**, `마지막 재생 확인 장치: Mac Studio 스피커`를 표시했다: [재생 화면](generated/r80-build236-native/play-running.ax.txt). 앱 snapshot은 출력 `ready`, transport `playing`, 실제 장치명 `Mac Studio 스피커`였다. Stop 뒤 UI가 **00:00.0·정지**로 돌아오고 앱 snapshot 출력·transport 모두 `idle`이었다: [Stop AX](generated/r80-build236-native/play-stop.ax.txt), [종료 전 상태](generated/r80-build236-native/final-state.json). 이는 앱의 재생 상태·장치 라우팅 확인이지 물리 청취/Scarlett 적합성 판정이 아니다.

**출고 차단 성능 관찰:** 같은 700×900pt 창, 40pt 콘솔, 500행 보관, 같은 번들 데모 Play/앱 내장 출력에서 재생 중 read-only 실패 로그 **100건**을 연속 보냈다. 목표 20회/초와 달리 실제 **15.28초, 6.54회/초**였다. 전후 앱 `playback.frameCount` **2170→2336**(+166), `view.elapsedSeconds` **41.9396→57.2189**(+15.2793초), 출력은 계속 `ready/playing`이었다: [원시 측정](generated/r80-build236-native/play-100-events.json). 입력 없는 비교 구간 **8.03초**에서는 frameCount **2995→3410**(+415), 재생 시계 +8.0367초였다: [비교 원시 측정](generated/r80-build236-native/play-idle-baseline.json). 각각 약 **10.9** 대 **51.7 frameCount 증가/초**다. `frameCount`는 앱 내부 값이고 화면 계측 FPS나 실제 MP4 프레임률이 아니다. 다만 동일 화면에서 입력 유무에 따른 약 **4.8배** 차이이므로 고밀도 콘솔·재생 화면 성능을 0.80의 열린 gate로 둔다. 요청 처리율도 전송 목표보다 낮았으므로 20회/초를 견뎠다고 표현하지 않는다. 장시간 녹화와 실제 frame pacing 계측으로 원인을 좁혀야 한다.

**동일 세션 A/B 후속 검증:** 같은 격리 build236 QA 사본을 다시 열고 새 runID/socket/project를 확인했다: [재실행 신원](generated/r80-build236-native/ab-reopen-state.json). 창은 **700×900pt**, 로그 높이는 **40pt**, 앱 전용 출력은 `Mac Studio 스피커`, 시스템 기본값은 여전히 Scarlett이었다. read-only 실패 조회 **496건**으로 activity sequence를 정확히 **500**, 보관 행 수를 **500**으로 채웠다: [준비 기록](generated/r80-build236-native/ab-500.json). 각 조건 사이에 Play를 Stop→다시 Play해 같은 `접속 · DJ Intro`의 약 **5→21초/3→11마디**를 비교했다. 미존재 jobID 조회는 양쪽 모두 **100건**, 목표 **6.5건/초**로 pacing했다.

| 조건 | 실제 요청률·소요 | 앱 `frameCount` | 재생 시계 증가 | 출력/프로젝트 |
| --- | --- | --- | --- | --- |
| 콘솔 펼침, 40pt/500행 | 6.4984건/초 · 15.3884초 | 2413→2577, **+164 ≈10.66 count/s** | +15.4401초 | ready/playing · 같은 ID/revision |
| 콘솔 접힘 | 6.4979건/초 · 15.3897초 | 3615→4427, **+812 ≈52.76 count/s** | +15.4183초 | ready/playing · 같은 ID/revision |

[펼침 원시 JSON](generated/r80-build236-native/ab-expanded-100.json), [접힘 원시 JSON](generated/r80-build236-native/ab-collapsed-100.json). 같은 read-only 요청 수·입력률에서 접힘은 약 **4.95배** 많은 내부 frameCount를 기록했고 무입력 비교(약 51.7 count/s)에 가까웠다. 따라서 MainActor의 요청 처리만으로 이 감소를 설명하기는 어렵고, **펼쳐진 콘솔의 행 갱신·레이아웃·렌더 경로가 큰 기여 요인이라는 추론**이 가능하다. 이 A/B만으로 특정 `VStack` 구현을 단정할 수는 없다. 접힘에서 캔버스 영역도 커지는 차이가 있고, 실제 화면 FPS/CPU 프로파일은 계측하지 않았다. 재실행 앱도 Stop 뒤 출력·transport `idle`, clock 0, 프로젝트 ID/revision/path/dirty 불변이었다: [최종 상태](generated/r80-build236-native/ab-final-state.json). `⌘Q → 변경 버리기` 후 앱/helper/socket **0**, 원본·QA 서명과 실행 파일 및 데모 해시 불변, 시스템 기본 출력 Scarlett 유지를 다시 확인했다: [A/B 종료](generated/r80-build236-native/ab-clean-exit.json).

README용 이미지는 활동 생성 전 번들 데모 전체 창의 **1440×900pt/2880×1800px** 및 **720×900pt/1440×1800px** 캡처다. 두 화면 모두 7개 섹션과 상단 컨트롤이 보인다: [가로 AX](generated/r80-build236-native/width1440x900-overview.ax.txt), [세로 AX](generated/r80-build236-native/width720x900-overview.ax.txt). CUA 원본 JPEG를 자르기·리사이즈·합성 없이 RGB PNG로 변환했고 decoded pixel equality를 확인했다.

| 전체 창 | PNG SHA-256 | CUA 원본 JPEG SHA-256 |
| --- | --- | --- |
| [가로](../docs/images/circlr-song-build236.png) | `e62a57ef05231c98558a5edf478e7afabe237302231f9441f1f5eaf5d6af17fb` | `48c3a7f4b0f7c8384fbd7899e5e03e3b0caf03bf75d482922d10b91ca240d6f1` |
| [세로](../docs/images/circlr-song-portrait-build236.png) | `a8912204231e72db41ae2fa40c6db1d6ae81a1d1dc2ff6310b86afb16b4f1dc2` | `b2025990f17122197570ba86453b7d95410a246a2209f1102f0878f35ed2e524` |

QA 앱은 `⌘Q → 변경 버리기`로 종료했다. 종료 뒤 QA 앱/보조 프로세스 **0**, 전용 socket **0**, 원본/QA deep strict 서명 PASS, 원본 실행 파일 SHA 불변, 번들 데모 `manifest.json` SHA `a75ea9f2f3917f82184ba94c31e071905c0c6a1bfa2e90259ad554eaa5c794d7` 불변이다: [종료 기록](generated/r80-build236-native/clean-exit.json). 성능 gate가 열려 있으므로 이 문서는 정식 0.80 출시 승인이 아니다.
